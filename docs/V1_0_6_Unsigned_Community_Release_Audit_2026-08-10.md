# v1.0.6 无签名社区版发布审计

状态：**版本与文档已对齐，本地质量门通过，远端质量门和安装包证据待完成**

渠道：`community-unsigned`

企业签名候选边界：`releaseCandidate=false`；本次社区补丁不宣称真实签名或企业候选成立。

## 当前结论

- package、Cargo、Tauri、Windows 生命周期、性能、能力矩阵和社区发布策略统一为 `1.0.6`。
- EA-5C 作为本版准入基线：43 类格式分为 29 类外部编辑、8 类外部预览、6 类显式转换；37 类/85 扩展名默认应用候选只由用户逐项触发。
- `v1.0.5` EA-5B2B 安装态证据只作为上一版本基线，不冒充 `v1.0.6` 当前安装包证据。
- README 继续链接已发布的 `v1.0.5`，并明确 `v1.0.6` 正在准备；新附件通过哈希复核前不创建失效下载入口。

## 当前验证

- `npm run ci:patch-release` 本地完整通过，用时约 345 秒。
- Vue/Vite 生产构建处理 6225 个模块，现行格式、体验、外部工作区、默认应用和发布合同全部通过。
- Rust 锁定检查成功编译 `tauri-app v1.0.6`，生产依赖审计为 0 个漏洞。
- 社区策略仍保持 `qualityGatePassed=false`；只有本提交的远端 Quality Gate 成功后才进入安装包构建。

## 本阶段门禁

1. 完成本地 `ci:patch-release` 与远端 Quality Gate，冻结产品源码提交。
2. 从冻结提交构建 MSI/NSIS，确认版本资源为 `1.0.6`、签名状态为 `NotSigned`。
3. 生成安装器大小、SHA-256 与 `SHA256SUMS.txt`，把候选证据回写仓库。
4. 发布 GitHub Release 后重新下载三个附件并逐项复核。
5. 从已安装 `1.0.5` 执行应用内发现、确认、下载校验、覆盖安装和资料保留观察。

## 发布边界

- `v1.0.6` 尚未发布，当前公开稳定版仍为 `v1.0.5`。
- v1.0.4 及更早版本没有受控更新链，升级时必须手动下载安装 v1.0.5 或 v1.0.6。
- 安装包无 Authenticode 商业签名，必须保留未知发布者提示和 SHA-256 校验说明。
- 不发布旧 Tauri 私钥链要求的 `latest.json` 或 `.sig`；受控更新使用固定 GitHub Release 与附件摘要。
- Office 等价编辑、可选视频编解码器和企业签名不属于本补丁可扩大的承诺。

## 下一步

运行完整本地与远端质量门；成功后锁定源码提交并构建 `LongEdit_1.0.6_x64-setup.exe` 与 `LongEdit_1.0.6_x64_zh-CN.msi`。
