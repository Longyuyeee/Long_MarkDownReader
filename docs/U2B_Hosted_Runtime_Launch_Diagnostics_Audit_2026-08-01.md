# U2B 托管运行时启动诊断审计

日期：2026-08-01

状态：`diagnostics-hardened-awaiting-rerun`

发布状态：`releaseCandidate=false`

## 第二轮结果

GitHub 托管 U2 第二轮（run `30654676442`）使用产品提交 `4cf5df4`：

- 当前 0.7.0 未签名 NSIS 构建通过；
- 回滚 0.6.2 未签名 NSIS 构建通过；
- 0.6.2 新装、0.7.0 受控升级、Markdown OpenWith 登记、默认应用不变及 ProgID 命令检查均已越过；
- 已安装 0.7.0 启动后，WebView2 CDP 端口 `9343` 在原 24 秒窗口内没有监听，流程按 fail-closed 失败。

因此 U2A 的文件关联修复已经通过真实安装验证；当前未知项收窄为“托管 Windows 首次启动较慢”或“运行时进程在启动期退出/缺少可交互桌面”。

## 本阶段加固

- CDP 首次启动等待上限从 24 秒调整为 120 秒，覆盖托管机首次 WebView2 初始化的合理波动。
- 等待过程持续检查已安装应用进程；若应用提前退出，立即报告退出码，不再无信息等待到端口超时。
- 超时或提前退出时写入脱敏诊断 JSON，记录交互会话状态、Explorer 数量、WebView2 进程数、运行时版本、应用退出状态和端口状态。
- 工作流总超时从 45 分钟提高到 60 分钟，避免两次干净 Rust 构建挤占动态生命周期诊断时间。

## 下一步

第三轮 U2 必须读取真实诊断结果：若只是首次启动较慢则继续完成全生命周期；若应用退出则修复具体启动错误；若托管 runner 明确没有桌面/WebView2 运行条件，则把“安装、升级、关联、卸载、数据保留、降级与回滚”作为非交互子矩阵独立收口，并继续把真实 UI/CDP 验证保留给 Windows 10/11 可交互一次性机器，不得用托管限制冒充产品通过。
