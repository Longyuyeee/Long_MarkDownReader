# v1.0.23 外部故障状态：本机专项

## 本版边界

仅完成当前运行版本、开发目标、远端观察结果的分离，以及发布详情打开成功/失败不干扰更新状态。真实成功路径已有 `v123-local-version-observation` 的本机证据。本专项不添加图谱、格式编辑或退出行为改动。

## 安全的外部 API 故障注入

启动命令：`node node_modules/vite/bin/vite.js --config scripts/vite.version-audit.config.ts --host 127.0.0.1 --port 9000 --strictPort`，配合既有独立标识测试程序。

此专用 Vite 配置仅替换 `appUpdater.ts` 的两个外部依赖导入（Tauri invoke 与系统 opener），不修改组件、computed、更新状态流转或实际运行版本。桥接脚本从真实运行时读取版本和应用标识，仅允许 `com.longyuye.mdreader.version-audit` 的开发模式使用；始终拒绝安装。未涉及的 IPC 仍转发真实后端。

被 Git 忽略的 `version-audit.local/scenario.json` 选择外部结果：`check` 为 `offline` / `available` / `current`，`open` 为 `failure` / `success`。模拟更新明确写“验收模拟发布”，不是 GitHub 新 Release。修改场景后由用户界面的“检查更新”触发，不直接赋值响应式状态。

普通 `vite.config.ts` 和产品源码不引用该测试配置。专用配置拒绝 build，已实际运行构建命令确认报 `Version audit fixture cannot build production assets`（预期退出码 1）。8 项隔离/禁安装/错误传播/真实版本保留测试和 13 项产品状态回归通过。

## 观察顺序与判定

1. “我暂时没网，想检查更新”：手动检查应呈现失败并保留当前版本；不能伪装最新，也不能出现下载按钮。
2. “恢复网络后再试”：切到 current 后点击检查，应恢复已是最新，清除检查错误。
3. “有新版，先看看改了什么”：切到 available 后检查，当前版本保持 1.0.22、目标 1.0.23；弹窗不冒称已安装。
4. “发布详情打不开”：点击详情触发 opener 失败，应保留更新弹窗、可安装状态并独立解释失败。不要实际点击安装。
5. “再打开一次”：切到 opener success 后从同一按钮重试，应清除网页错误并保留更新状态。

本专项输出必须标注“原生操作 + 外部 API 故障注入”，不能作为真实断网、真实系统浏览器失败、正式安装或升级成功的证据。

## 当前执行状态

2026-09-23 重新选择恢复后的隔离窗口，以上五项已逐一点击、观察并保留原图及无障碍文本。先前最小化导致的暂停已解除，没有改用其他输入 API 绕过人工操作保护。

| 用户场景 | 实际观察 | 证据名 |
| --- | --- | --- |
| 暂时没网，检查更新 | 显示网络错误，当前仍 v1.0.22，无下载按钮 | offline |
| 恢复后重试 | 显示已是最新 v1.0.22，旧检查错误消失 | recovered-current |
| 发现新版，先看详情 | 弹窗明确 v1.0.22 → v1.0.23，包含验收模拟发布说明 | available |
| 发布详情打不开 | 独立解释失败，弹窗及下载入口保留，没有改为安装失败 | opener-failure |
| 再打开一次 | 网页错误消失，更新弹窗与可更新状态保留 | opener-retry |

证据目录为 `docs/evidence/v123-fault-states`：5 张逐张观察的未修改截图、5 份无障碍记录及 SHA-256 清单。产品提交 `ca8a6a0d`、测试桥接提交 `a744b66c`；真实原生版本仍为 1.0.22，远端 1.0.23 为模拟值。检查器 `node scripts/check-v123-fault-observation.mjs` 核对原始摘要和观察内容。本次没有安装、实际断网或制造系统浏览器故障，不能据此宣称正式安装通过。

观察完已通过前端关闭按钮退出隔离程序，并停止对应专项 Vite 服务；原已安装应用保持运行，用户资料未读写。没有修改产品代码或降低验收条件。当前版本身份专项的本机页面观察已收口，不再重跑这些已通过场景；下一步是原子版本迁移和独立候选安装验证。

## 收尾与下一版

完成本专项后冻结 1.0.23 的单一版本身份修复范围，执行版本迁移与固定候选包构建、安装验证，再更新 README / Release。不得替换已发布 1.0.22 附件。

下一版优先候选：统一 Alt+F4 与关闭按钮的退出偏好（已有本机现象和实际 CloseRequested 代码依据）；随后按实际复现选择搜索相似结果的路径/命中位置辨认或图谱恢复问题。本版不继续扩大范围。
