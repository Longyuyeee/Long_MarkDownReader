# v1.0.9 无签名社区版发布审计

状态：**自动更新重启恢复候选已通过质量门、打包与托管安装生命周期，待发布**

渠道：`community-unsigned`

发布门禁：`releaseCandidate=true` 仅适用于无签名社区版。v1.0.8 当前仍只提供手动下载安装；通过应用内更新完成覆盖安装后，需要用户手动重新打开 LongEdit。v1.0.9 发布后仍须执行真实 `v1.0.8 -> v1.0.9` 自动重启验收。

## 当前结论

- 官方 `v1.0.7 -> v1.0.8` 托管运行 `31486852139` 已确认下载、SHA-256、覆盖安装和 v1.0.8 二进制写入成功，但自动重启失败。
- v1.0.9 改为安装后延迟启动、进程持续存活检查、失败重试和脱敏重启日志；4 个 Rust 更新器测试及社区更新合同已通过。
- v1.0.8 Release 与历史证据保持不变，不替换公开附件；恢复修复使用新的补丁版本和新的哈希链。
- 冻结提交 `8f668bc402ca45b4c621193ba5b65ece63de51a7` 的 Quality Gate `31489160736` 已通过；本地 MSI/NSIS 构建、版本、未签名状态和 SHA-256 已核对。
- GitHub 托管安装生命周期 `31490343424` 已通过 22/22 生命周期和 18/18 安装态工作区检查，证据不包含用户正文。

## 发布顺序

1. 提交候选证据并通过第二次 Quality Gate。
2. 发布 v1.0.9 并远端复核三个附件。
3. 执行官方 `v1.0.8 -> v1.0.9` 12/12 更新链，必须证明自动重启进程可用。

## 发布边界

- 当前公开版本仍为 v1.0.8；v1.0.9 尚未发布。
- 安装包无 Authenticode 商业签名，保留未知发布者与 SHA-256 校验说明。
- 无签名社区候选已经就绪；企业签名候选仍为 `releaseCandidate=false`，不得混用本次证据。
- v1.0.8 更新链失败只允许本次恢复补丁作为显式例外，未来版本仍要求上一条官方更新链通过。
