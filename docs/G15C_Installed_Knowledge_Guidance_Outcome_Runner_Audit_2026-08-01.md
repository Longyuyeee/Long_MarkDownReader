# G15C 安装态知识建议效果验证器审计（2026-08-01）

## 结论

G15C 已把 G15B 的“匿名基线—关系治理—改善复查”接入隔离安装生命周期。安装版必须在原设置页面显示“保存基线”和“复查改善”，通过正式 Tauri 命令保存聚合基线，向固定合成资料库加入一个有关联的后续对象，再完成预览与创建新对比回执。对比必须报告改善，并通过路径、文件名和合成标题泄露扫描。

当前状态为 `installed-guidance-outcome-runner-integrated-hosted-execution-next`，`releaseCandidate=false`。本提交只证明验证器和机器合同已经就绪；GitHub 托管安装态执行、截图复核及证据接收尚未完成。

## 首次托管执行与修复

GitHub Actions 运行 `30692454820` 成功构建当前产品和 v0.6.2 回滚安装包，但在进入 G15C 前的 TXT 深链步骤失败。失败页面显示工作台、资料库和知识网络已经加载，地址仍为 `#/workspace`；日志证明验证器在应用配置初始化尚可能重定向路由时过早写入深链。这不是 TXT 编辑器或 G15B 对比命令失败，G15C 在该次运行中尚未执行。

验证器现要求初始地址稳定在工作台且知识网络组件已经挂载，再额外等待 750 ms 后开始第一个编辑器深链。失败运行、构建完成状态和 `g15cExecuted=false` 已写入机器政策。修复后的运行将复用运行 `30692454820` 已构建且上传的安装包，产品源码身份保持不变。

复用安装包的运行 `30693531173` 在 2 分 21 秒内通过完整生命周期。安装态 G15C 实际得到 5→6 个对象、6→7 条关系、5→6 个已连接对象、100% 覆盖率不变，结果为 `improved`；四项隐私标志全部为 false，8 项安装 smoke 和 18 项生命周期检查通过。

人工查看 `installed-knowledge-observation-settings.jpg` 后没有接收视觉证据：截图停留在设置页顶部，目标知识网络观察区域位于视口下方。元素存在与功能调用已经由机器证据证明，但该截图不能证明按钮文字、字号和风格。验证器现将目标行滚动到视口中央，校验宽度、高度和上下边界后再截图。当前状态因此为 `installed-guidance-outcome-functional-passed-visual-recapture-next`，截图复核仍为 false。

## 最终安装态验收

视觉重拍运行 `30693694231` 在 2 分 3 秒内再次通过 9/9 安装 smoke 和 18/18 生命周期检查。新证据声明目标区域 `viewportVisible=true`、`openedInCurrentWindow=true`。人工复核确认“知识网络匿名观察”说明、“保存基线”和“复查改善”全部清晰可见，按钮尺寸、边框、文字层级与相邻设置项一致。

最终聚合结果仍为 5→6 个对象、6→7 条关系、5→6 个已连接对象，结果 `improved`；未包含正文、对象标识、文件名或绝对路径。脱敏回执位于 `docs/evidence/g15c-installed-guidance-outcome/acceptance-receipt.json`，原始截图和完整运行证据只保留在 GitHub Actions artifact。

G15C 状态更新为 `hosted-installed-guidance-outcome-passed-real-user-follow-up-next`。这证明未签名 Windows 托管安装环境中的合成资料库流程与视觉入口，不代表真实用户资料库或签名 Windows 10/11 客户端已经验收。下一步仍是用户明确授权的真实资料库基线—治理—复查；`releaseCandidate=false` 保持不变。

## 验收范围

- 在安装后的当前应用窗口打开设置页，确认两个知识网络匿名观察操作均可见。
- 导出当前固定合成资料库的 G12 聚合基线。
- 写入一个固定合成 Markdown 对象，并以 `supports` 关系连接既有知识对象。
- 调用 G15B 预览与导出命令，要求对象、已连接对象和关系数量出现正向变化，结果为 `improved`。
- 扫描对比 JSON，拒绝资料库路径、fixture 文件名及合成标题。
- 保存设置页截图、基线、对比回执和安装态汇总证据；所有 JSON 均声明不包含用户正文。

验证器使用的是生命周期脚本创建的合成资料库，并非用户真实资料。它也不证明 Authenticode、Windows 10/11 客户端或发布批准。下一步通过 U2 工作流针对产品提交 `fffbc6af4508b6476610a5b34d704ac606efe2ee` 构建并执行安装包，人工复核设置页截图和聚合证据，再以独立提交关闭 G15C。真实资料库效果验证仍必须由用户明确授权执行。
