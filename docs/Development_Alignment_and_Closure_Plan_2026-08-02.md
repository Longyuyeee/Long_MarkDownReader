# 开发对齐与收口计划

审计更新：2026-09-01
当前开发目标：`1.0.21`
当前运行时版本：`1.0.20`
当前公开版本：`1.0.20`
当前阶段：**M8-9 v1.0.19 → v1.0.20 官方应用内更新观察**

接续说明（2026-09-01）：v1.0.20 已正式发布，annotated Tag 精确绑定候选 `a08ab6a5471f9f8b163403edadb554ea6446c3f4`；托管运行 `33461982887` 完成完整门禁、双包重建和 22/22、18/18、11/11、7/7 生命周期，失败 0。三个公开附件已回下载复核；唯一接续点为 M8-9 官方 v1.0.19 → v1.0.20 应用内更新观察，当前不得重复开发或重新打包 v1.0.20。

用户反馈插队修正（2026-09-01）：真实截图与源码确认普通图谱可缩到 10% 并错误切入持续刷新、文字压缩的社区泡泡层；高 DPI 桌面还会把节点详情铺满底部，原生布局/主题下拉粗糙且三种画布主题差异不足。开发版已增加普通图谱 58%～68% 密度下限、240 节点社区总览门槛和稳定缓存，详情收为右下 380px 卡片，三项顶部选择器改为应用浮层，并拉开专业/多彩/专注背景差异。真实 Windows/Tauri 83 节点/81 连接连续缩小 48 次停在 58% 中景，等待后帧号不变、连续循环 false；详情占视口 13.72%，运行时错误 0，45/45 源文件摘要不变。详细审计见 `docs/Post_v1.0.20_Knowledge_Graph_Interaction_Polish_Audit_2026-09-01.md`。本修正不改变 v1.0.20 公开制品，提交后仍回到 M8-9。

`v1.0.19` 已于 2026-09-01 正式发布；Tag 精确解析到产品候选 `9655b02142b64bd8e7f1ad4056a4b9c6f0367990`，Release `379909457` 的 NSIS、MSI 与 `SHA256SUMS.txt` 已从公开地址回下载并逐项复核。托管运行 `33418123151` 又以 12/12、失败 0 验证官方 v1.0.18 → v1.0.19 应用内更新。当前公开与运行时版本均为 `1.0.19`，开发目标为 `1.0.20`；用户真实资料库截图暴露的图谱可用性问题已进入 M8 纠偏，不继承 M3B 的历史验收结论充当本轮通过证据。

M1、M2、M3 与 M4A～M4D 的退出审计均已通过：薄弱格式深化、行动工作台、知识图谱语义/视觉/性能、跨格式定位/动作/转换和有界证据清理已经形成可复核闭环。M4E～M4F-6 已完成 v1.0.16 收口；M5-0～M5-9 已完成 ODP 有界编辑、质量债清零、v1.0.17 发布和官方更新观察。M6-0～M6-8 已完成图谱有界全屏、质量门、版本迁移、双安装包、WebView2、托管生命周期、最终制品冻结、公开 Release、远端复核与官方更新观察。v1.0.18 阶段剩余工作为 0；后续需另行启动 v1.0.19 范围选择审计。

M5-6 已通过。精确候选 `2b6235d420ceffd291dab72c4af17caffe464333` 在 GitHub 托管 Windows 重建 MSI/NSIS，并从公开 `v1.0.16` 完成升级、22/22 生命周期、18/18 安装态检查、11/11 路由和 7/7 管理回滚。托管安装器与本地构建摘要不同但版本、`NotSigned` 和行为一致；M5-7 只晋级托管产物，尚未创建 `v1.0.17` Tag 或 Release。

M5-9 已完成：官方 v1.0.16/v1.0.17 NSIS 与 v1.0.17 安装态主程序身份已冻结，一次性 Windows 工作流只使用公开资产并复用参数化更新 runner/probe。首轮修正 Release 候选文案残留，第二轮修正动画截图重影；最终运行 `33368732235` 为 12/12、0 失败，三张截图人工复核通过，9 个文件逐项哈希导入。v1.0.17 发布与更新链已经收口。

M6-0 已完成：真实代码与既有真实 Tauri 证据确认 F11 只隐藏应用界面、图谱没有全屏入口；社区交互仍是过滤子图而不是代理折叠；Schema 与 ODP 备注仍缺关键保真合同。唯一接续点为 M6-1 图谱有界全屏生命周期与真实桌面审计；不改图谱语义、不写回源文件、不提升版本。

M6-1 已完成：暗色/reduced 与浅色/calm 两套真实 Tauri 会话、三档窗口完成 6/6 全屏进入/Escape 退出与 2/2 路由卸载，运行时错误 0、源摘要不变；选择、相机、历史面板和缩略图保持。测试实际修正用户激活、动态 `aria-label` 和 Document 失活时的 Promise 拒绝。唯一接续点为 M6-2 v1.0.18 下一切片选择审计；当前仍不打包。

M6-2 已完成：剩余社区折叠、Schema、治理环和 ODP 备注均需新的高风险合同，不再扩大本补丁。唯一接续点为 M6-3 v1.0.18 质量债与发布就绪审计，完整 Rust 与 `ci:patch-release` 全绿后才允许提升版本和构建候选安装包。

M6-3 已完成：Rust 全仓 548 通过、0 失败、5 忽略；完整 `ci:patch-release` 首次暴露 v1.0.17 托管运行回执缺失与证据字段漂移，补齐真实运行 `33361759629` 并对齐 `releaseCandidate=false` 后完整复跑通过，生产依赖漏洞为 0。唯一接续点为 M6-4 v1.0.18 原子版本迁移与候选打包。

M6-4 已完成：44 个当前身份文件原子迁移到 1.0.18；精确候选 `5988c03c0167b00cb86ed9a5f3cfe85f0b280a6a` 完整发布门通过，真实 MSI/NSIS 的版本、大小、SHA-256 与 `NotSigned` 已逐项核验，当前 Tauri WebView2 通过 6/6 检查、11/11 路由和 TXT/JSON 保存重开。两张截图人工复核通过。唯一接续点为 M6-5 v1.0.18 托管安装生命周期；公开版本仍为 v1.0.17，未创建 v1.0.18 Tag/Release。

M6-5 已通过：GitHub 托管 Windows 运行 `33378338422` 从精确候选重建 MSI/NSIS，并以公开 `v1.0.17` 完成 22/22 生命周期、18/18 安装态检查、11/11 路由和 7/7 管理回滚，失败 0。14 张截图已人工复核；29 个脱敏证据文件的仓库规范树摘要为 `1dbb47325812f166608921528bb88f08275decf980450a7d36e1dfc8d12b3013`。唯一接续点为 M6-6；当前仍不得进入 Tag/Release。

M6-5 换机前置纠偏已完成：实际产品代码与 Git blob 未漂移，历史失败来自 Windows `core.autocrlf=true` 改写工作区文本字节。受控源码与 JSON 证据现只把 LF/CRLF 视为等价表示并同时核对大小与摘要，截图等二进制仍使用原始字节；该修正不改变功能、版本或发布门禁。原始 artifact 已下载、独立校验并以规范 JSON 导入。

M6-6 已通过：只晋级 M6-5 托管通过的 MSI/NSIS，公开名称、大小、SHA-256、`SHA256SUMS.txt` 和 artifact manifest 已冻结；企业签名候选仍为 false。

M6-7 已通过：`v1.0.18` Tag 解引用到产品候选 `5988c03c…`，Release `379760984` 为 latest、非草稿、非预发布；三项附件从公开地址重新下载后大小与 SHA-256 全部匹配。

M6-8 已通过：GitHub 托管一次性 Windows 运行 `33397305847` 只使用公开 v1.0.17/v1.0.18 NSIS，完成发现、用户确认、SHA-256、同目录覆盖、自动重启、首次启动、最新版状态、资料与配置保留，12/12、失败 0；三张截图人工复核，9 个脱敏文件逐项哈希导入。v1.0.18 发布与更新链全部收口。

M7-0 已完成：重新审查最初“可选 Schema、路径/行列定位、未配置零误报”需求与实际 JSON/YAML/XML/TOML、图谱和 ODP 代码，只选择 M7-1 本地 JSON/JSONC Schema sidecar provider/mapping 可行性审计。JSON 已有有界解析、稳定路径/行列和安全保存；M7-1 禁止联网与远程自动解析，只评估确定性本地 sidecar、预算、诊断来源和安全降级，不提升版本。YAML/TOML 投影、XSD、图谱代理折叠/治理环和 ODP 备注继续独立延期。

M7-1 已完成：离线 Draft 2020-12 内核对 JSON/JSONC、确定性同级 sidecar、1 MiB / 5 万节点 / 64 引用 / 200 诊断预算、内部引用、实例路径到行列、诊断脱敏和源文件只读均已证明；9 个定向测试与 28 个 JSON 回归通过。当前唯一接续点为 M7-2，把只读验证接入现有 Tauri 命令与 JSON 编辑器并完成真实宽窄屏验收；在此之前仍不提升版本、不打包。

M7-2 已完成：只读 Tauri 命令复用资料库路径授权，JSON/JSONC 编辑器已分离语法与 Schema 状态，显示 sidecar 来源、脱敏诊断和源码定位；外部文件、大文件模式、Schema 文件递归和所有外部引用保持关闭。13 个命令测试、9 个内核测试、前端构建及 1280×800 / 720×680 真实 Tauri 验收通过，运行时错误 0，源文档与恢复后 sidecar 摘要不变。唯一接续点为 M7-3 v1.0.19 质量债与发布就绪审计；完整质量门通过前仍不提升版本、不打包。

M7-3 已完成：全仓 Rust 559 通过、0 失败、5 忽略；完整 `ci:patch-release` 首次通过，6,275 前端模块、43 格式/91 扩展名、Rust 编译和生产依赖 0 漏洞保持成立。严格 Clippy 仍是 43 条历史债，新增 Schema 模块和命令 0 命中。唯一接续点为 M7-4 v1.0.19 原子版本迁移与候选打包。

M7-4～M7-8 已完成：44 项当前运行身份原子同步到 1.0.19；完整补丁门、真实 Tauri 6/6 + 11/11、本地 MSI 观察和 GitHub 托管双包均通过。托管 Run `33409497055` 从固定候选 `9655b021…` 重建 MSI/NSIS，并通过 22/22 生命周期、18/18 安装态检查、11/11 路由和 7/7 管理回滚；最终 ASCII 文件名、双包摘要和 `SHA256SUMS.txt` 已冻结。annotated Tag 精确落到候选，GitHub Release 与三个远端附件回下载复核通过；Run `33418123151` 再以 12/12 验证官方应用内更新、自动重启和资料保留。v1.0.19 发布链全部收口，下一动作是 v1.0.20 范围选择审计。

M8-1 已按用户真实截图完成第一轮代码纠偏，并确认这不是新增路线，而是对原始 M3B“专业视觉系统”和 M3C“稳定交互/性能”要求的缺陷修复：Canvas 改为按自身 CSS 视口和取整物理像素调整 backing store，避免分数 DPR 下逐帧清空；鼠标交互改为 pointer capture，单击只选择、双击才打开，社区进入延迟到释放且拖动优先；540 个孤立节点不再渲染成 540 个带双行文字的大社区圆；图例默认折叠并直接说明拖动、滚轮、单双击，筛选展开时隐藏图例以消除左上角叠压，图例字母徽标也已移除。前端生产构建、UX-37B 和 M3B-1 回归合同及 M8 定向纯函数/静态审计已通过。当前唯一接续点为 **M8-2 真实 Tauri 大量孤立节点与连接图视觉/交互验收**；该验收通过前不得宣称图谱问题收口或进入 v1.0.20 发布冻结。

M8-2 已通过真实 Windows/Tauri 验收：1.25 DPR、1440×900、540 个孤立 Markdown 节点下保持远景，社区摘要为 0；6 次交替滚轮事件期间 backing store 属性变更为 0，实际尺寸与 CSS 视口×DPR 取整一致；中键拖动改变镜头，单击命中节点后仍停留图谱且只选择 1 个节点；筛选面板可见时图例隐藏，默认折叠操作提示仍可读；运行时错误 0、540 份合成源文件摘要不变。人工截图复核确认原先整屏大圆与双行文字已消失。当前进入 **M8-3 工具栏信息架构与连接图字体/图标视觉精修**，重点处理顶部命令密度和中近景视觉，不回滚本轮已稳定的交互与性能合同。

M8-3 已通过真实 Windows/Tauri 对照：180 节点/540 连接图的 Canvas draw 峰值由 7.1 ms 降至最终候选复跑的 4.5 ms，普通进入由自动选择 1 个节点改为 0 个，状态环最多 24 个；视口裁剪、`Set` 选中查找、恒定字号与碰撞规避、平方根近景尺寸、克制节点材质均已落地。顶部命令收敛为明确的“图谱工具”入口，13 个探索/导出/布局命令在 248 px 原生弹层内全部单行；最终全图、300% 近景和工具弹层截图已人工复核，运行时错误 0。当前进入 **M8-4 v1.0.20 完整质量门、原子版本迁移、双包与公开发布**。

M8-5～M8-8 已完成：真实 Windows/Tauri 180 节点/540 连接精修验证将稳定后自动适配收口为 180/180 节点在界内，中景普通箭头改为 priority-only、状态环降至 12 个，420 ms 选择反馈结束后回到 `loopContinuous=false`，绘制峰值 5.1 ms、运行时错误 0。托管发布前两次真实失败分别暴露浅检出缺少历史 Tag 与 3 份历史证据的 CRLF 原始字节未固化；修正时未放宽检查，候选 `a08ab6a…` 重新通过本机和托管完整门禁。运行 `33461982887` 重建双包并完成 22/22 生命周期、18/18 安装态、11/11 路由、7/7 管理回滚；v1.0.20 Tag、Release 与三个公开附件回下载复核均通过。唯一接续点为 **M8-9 官方应用内更新观察**。

## 1. 审计结论

最初确定的专业知识工作台、完整 Excel 方向、新格式编辑器和更多主题预设仍然对齐。v1.0.3/v1.0.4 安装反馈形成的体验问题已经完成有界实现；当前 43 类格式、11 套能力配置、统一主题与安全保存边界均有自动化合同。

EA-5C 已完成外部打开与默认应用汇总：29 类直接编辑、8 类只读预览、6 类显式转换；37 类/85 扩展名默认应用候选只由用户逐项触发。当前 package、Cargo、Tauri 与发布事实源已提升到 `v1.0.15`；NSIS/MSI 已真实构建，托管 Windows 安装生命周期 22/22 与安装后工作区检查 18/18 已通过。GitHub Release 已发布，三个远端附件的大小与 SHA-256 已回下载复核；下一步是独立观察 `v1.0.14 -> v1.0.15` 官方应用内更新。

能力范围已在 XLSX 错误值、日期时间和按工作表知识索引阶段冻结。官方 `v1.0.9 -> v1.0.10` 应用内更新已在 GitHub 托管一次性 Windows 完成 12/12 检查；当前版本事实源和发布后更新链均已收口。

## 2. 需求对齐

- **完整 Excel 方向**：多工作表、公式、样式、图表、筛选、动态数组和部分透视结构已经形成；七种经典错误值及已有日期型单元格均已完成受限编辑与保存复读。宏执行、日期自动类型推断与少数复杂生产者对象仍是明确边界，不能宣称完全等价 Excel。
- **工作簿格式边界**：当前正式注册的工作簿扩展名只有 `.xlsx`；`.xlsm`、`.xlsb` 与旧 `.xls` 不属于软件内 XLSX 编辑承诺。宏工作簿和二进制工作簿只有在建立独立解析、保真与三生产者复开证据后才能登记。
- **新格式编辑器**：当前注册 43 类格式、91 个扩展名，其中 30 类为已验证、7 类为有限能力、6 类依赖外部程序；资料库内 PNG/JPEG/WebP/BMP 已在右侧媒体工作区支持旋转、翻转、精确裁剪、缩放、格式转换、JPEG 质量控制与隐私元数据清理并可靠另存，GIF/ICO/AVIF 与外部图片只读，视频使用按需流式读取。
- **更多主题预设**：3 套核心、4 套场景预设和 19 个外观组合仍由统一主题注册表驱动。
- **专业知识工作台**：资料库、搜索、标签、保存视图、引用、图谱、历史、备份和文件上下文保持统一工作流。

## 3. 当前开发状态

- UX-40 至 UX-49 已完成文档切换、侧栏、文件标记、横向导航、画布交互、Table 看板、下拉控件、命令条、自动更新和媒体播放收口，P0、UI-1、UI-2、UI-3 与 UI-4 均已完成。
- 43 类格式映射到 11 套发布能力配置，产品界面与发布审计消费同一事实源。
- Markdown、文本、日志、代码、结构化文件、表格、Office、PDF、图形和媒体均保持显式保存或明确只读边界。
- package、Cargo、Tauri、Windows 生命周期、性能策略、能力矩阵和社区发布策略已同步到 `1.0.6`；Release `367990618` 已发布，Tag 绑定候选证据提交 `257c12e`，三项远端附件哈希一致。
- v1.0.6 安装器已由 GitHub 托管可丢弃 Windows 完成 22 项生命周期和 18 项安装工作区检查；证据绑定产品提交 `9349c334` 和 runner 安装器 SHA-256。
- v1.0.5 到 v1.0.6 的真实应用内更新已由托管运行 `31406703253` 完成 11/11 检查；更新确认、官方 NSIS SHA-256、覆盖安装、首启、最新版状态、覆盖与卸载后的合成资料保留均通过。
- UX-01 至 UX-41 基础清单已全部回写为“已完成”或“已完成（有界）”；后续体验扩展继续由独立专项合同约束。
- XLSX 错误值编辑已从 `planned` 提升为 `limited`：七种经典错误值通过 OOXML `t="e"` 写入、撤销/批量路径和保存后语义复读；当前读取引擎无法复读的 `#GETTING_DATA` 及其他扩展错误继续阻断类型化写回。
- XLSX 日期时间编辑已从 `planned` 提升为 `limited`：已有日期单元格提供规范 ISO 编辑值，日期、日期时间和时间输入经真实日历校验后按 1900/1904 日期系统转回 Excel 序列，原数字格式保持不变；普通空白格不隐式转型，持续时间和 `1900-02-29` 保持只读边界。
- XLSX 已接入资料库持久索引与实时回退搜索：仅按工作表读取缓存单元格值，搜索结果可直接定位工作表；公式执行、外部刷新和源文件写入均不属于索引链。文件、工作表、行列、非空单元格及字符均设有固定预算。
- v1.0.7 无签名社区版已发布：冻结源码质量门、安装包哈希、托管安装生命周期、候选证据门禁和 GitHub Release 远端复核均已完成；`1.0.6 -> 1.0.7` 托管应用内更新也已通过 11/11 检查。
- v1.0.7 发布后的 CF-1 增量已补齐 10 个代码/Web 格式族、32 个注册后缀的主动创建合同；自动化模板、白名单、原子新建、重名保护及隔离桌面创建/显式保存/关闭重开证据均已通过，阶段状态为 accepted。
- UX-51 已修复自动更新后不重新启动与外部文件占用主窗口：隐藏助手等待 NSIS 成功后重启应用；37 类外部编辑/预览格式创建独立 `external-*` 顶层窗口。真实 Tauri WebView2 同时保留主资料库、TXT 和 JSON 三窗口且零运行时错误。
- v1.0.8 Release `368514584` 与三个远端附件已复核；官方更新运行 `31486852139` 在覆盖安装成功后未自动重启。v1.0.9 已实现延迟、存活检查与重试，冻结提交 `8f668bc` 的 Quality Gate `31489160736`、本地打包和托管安装生命周期 `31490343424` 均通过；Release `368579987` 及三个公开附件也已远端复核。
- 官方 `1.0.8 -> 1.0.9` 更新运行 `31495885209` 已通过 12/12：测试脚本未手动启动新版，更新助手自动拉起 v1.0.9 并通过持续存活检查；覆盖安装与卸载后的合成资料均保留。
- UX-52 已把默认打开管理收回格式能力页：37 类格式可逐项启用、关闭并查看真实默认状态；Windows 保护的最终默认选择只在用户点击后进入系统确认，不直接写入 `UserChoice`。
- v1.0.10 冻结提交 `558a16d` 的源码 Quality Gate `31554131290` 已通过；托管安装生命周期 `31556348980` 完成 22/22 生命周期和 18/18 安装后工作区检查。候选证据门禁 `31558312102` 通过，Release `368970200` 与三个公开附件已完成远端复核。
- 官方 `1.0.9 -> 1.0.10` 更新运行 `31872858203` 已通过 12/12：用户显式确认、官方 NSIS 摘要、同目录覆盖、自动重启、更新后最新版状态以及覆盖/卸载后的合成资料保留全部通过；九份脱敏证据已导入并受 SHA-256 清单保护。
- v1.0.11 冻结提交 `73932f3` 的 Quality Gate `31902257921` 已通过；本地隔离构建和托管 U2 `31902477250` 均通过。Release `371253676` 已公开，Tag `v1.0.11` 绑定候选证据提交 `4615cfe`；三个远端附件重新下载后的大小与 SHA-256 全部一致。
- 官方 `v1.0.10 -> v1.0.11` 更新运行 `31933205654` 已通过 12/12：用户显式确认、官方 NSIS 摘要、同目录覆盖、自动重启、更新后最新版状态以及覆盖/卸载后的合成资料保留全部通过；九份脱敏证据已导入并由 SHA-256 清单保护。
- v1.0.12 已发布并完成官方更新观察：托管运行 `32441493681` 使用公开 `v1.0.11`/`v1.0.12` NSIS 完成 12/12、0 失败；用户确认、SHA-256、同目录覆盖、自动重启、最新版状态和覆盖/卸载后的合成资料保留均通过。
- v1.0.13 候选范围已冻结为主界面版本/更新入口、用户可见术语清理、43 格式事实审计和 Windows 证据采集兼容性；当前必须重新通过源码质量门、安装包构建、托管安装生命周期和远端复核。
- v1.0.13 发布前界面复核已关闭更新提示过大与设置分类导航浮动：三类切换导航位置偏差为 0 px，更新卡片在 1000×700 和 480×700 均完整显示且无页面溢出；证据已纳入候选范围。

## 4. 发布边界

- v1.0.6 采用无 Authenticode 签名社区渠道，可能出现未知发布者或 SmartScreen；正式 Release 已附带并远端复核 SHA-256。
- v1.0.5 是新受控更新链首版。v1.0.4 用户需要手动迁移一次，之后支持应用内检查、确认、校验和覆盖安装。
- 不发布旧 Tauri 更新器要求的 `latest.json` 或 `.sig`；更新信任来自固定 GitHub Release、严格附件名和 GitHub 附件 SHA-256。
- 企业签名候选仍为 `releaseCandidate=false`，与用户批准的无签名社区发布分开。

## 5. 接手后的顺序

1. P0（已完成）：修正 README 与审计文档的当前能力表述，并增加注册表到公开说明的自动一致性门禁。
2. P0（已完成）：官方 `1.0.9 -> 1.0.10` 应用内更新已完成真实托管验证、视觉复核与证据导入。
3. P1（已完成）：PNG/JPEG/WebP/BMP 已完成旋转、翻转、精确裁剪、缩放、格式转换、JPEG 质量、EXIF 方向归一、元数据清理、可靠另存和真实 WebView2 宽窄屏验收；外部图片与 GIF/ICO/AVIF 保持只读。
4. P1（已完成）：PDF 标准表单安全子集、永久脱敏图片型副本、矢量文字水印和四字段文档属性均已完成真实桌面与独立输出验证；43 类格式、91 个扩展名、知识图谱与思维导图/图形工作区已按“可直接编辑 / 有界可靠副本 / 预览或外部依赖”完成总收口。详见 `shared/p1-final-capability-closure.json`。
5. P1：按真实 Word/WPS/LibreOffice 复开证据补强 DOCX 常用排版、图片和页眉页脚安全子集。
6. P1：关闭 XLSX 动态数组与透视结构的外部生产者证据门禁，再评估有限写回。
7. P1/P2：依次深化 PPTX 常用对象、结构化文本 Schema、图形/思维导图跨格式转换和 ODF 可行性。

P1 总收口已完成，v1.0.12 发布、远端复核和官方更新链也均已完成。当前执行 v1.0.13 质量门与无签名社区版发布；Excel/WPS/LibreOffice 三生产者数组写回继续属于外部证据门禁，不能回写为当前发布缺陷。
# 2026-08-21 v1.0.13 候选状态

紧凑更新提示与设置导航稳定性修复已完成真实 UI 审计，冻结提交 `520588b78607ca12160d27802a412d2a5474418b` 已通过完整 Quality Gate `32443494918`。本地无签名 MSI/NSIS 已构建并核验，托管安装生命周期 `32444502244` 通过 22/22 生命周期与 18/18 安装后工作区检查。当前只剩候选证据提交复验、GitHub Release 发布和远端附件哈希复核；发布后再单独观察 `v1.0.12 -> v1.0.13` 应用内更新。

发布收口：`v1.0.13` 已于 2026-08-21 发布，标签指向 `d46e9f6884734a668851ab6fc24111525184029b`；三个公开附件已重新下载并通过大小与 SHA-256 复核。下一步是独立的 `v1.0.12 -> v1.0.13` 应用内更新观察。

# 2026-08-27 M3 图谱阶段进度

M3-0 的 100/1000/5000 节点真实性能基线已经建立；M3A-1 已把 11 类对象、12 类关系及用户图例收敛到统一语义事实源，并通过跨格式真实 Tauri 宽窄窗口验证。当前执行 M3A-2 邻居聚焦与返回；最短路径、社区发现、视觉镜头和大图生命周期继续按独立阶段推进。公开/运行时版本保持 `1.0.15`，开发目标保持 `1.0.16`，当前不是发布候选。

M3A-2 已完成选中节点的 1/2/3 跳邻居诱导子图、持续范围说明和显式返回全图，真实跨格式图谱完成 17/17 → 3/3、6/7、8/9 → 17/17 往返且源文件不变。当前接续点为 M3A-3 独立验证的双节点最短路径。

M3A-3 已完成生产 BFS 与独立距离 oracle 对照、双节点路径 UI、无路径状态和返回全图；真实图谱完成 17/17 → 4/3 → 17/17。当前接续点为 M3A-4 路径边全部证据展示与来源位置回跳。

M3A-4 已完成路径 3 边的原始方向、关系类型、3 条 mention、结构证据边界和来源定位；同一边两条 mention 全部显示，`Brief.md` 第 3 行真实回跳通过。当前接续点为 M3A-5 社区发现与稳定性。

M3A-5 已使用 Graphology Louvain 完成稳定社区 ID、主题摘要、筛选与返回。真实 17 节点/17 边图谱得到 5 个社区（4/4/3/3/3），模块度 0.670，同一资料库重复构建 ID 一致，筛选 4/3 并恢复 17 节点通过。原始 M3A 在社区之后仍要求双节点比较、邻居固定和选择历史，已纠正此前提前指向 M3B 的接续偏移；当前进入 M3A-6 双节点比较。

M3A-6 已完成属性、共同/独有邻居和直接关系证据比较。真实 Canvas 节点对得到共同邻居 1、左右独有各 1 和 `supports` 直连；Markdown 节点对保留两条平行关系、3 条 mention 并回到 `Brief.md` 第 3 行。当前进入 M3A-7 邻居固定与选择历史，随后执行 M3A 退出审计。

M3A-7 已按原始专业工作台设计把局部关系图固定到编辑器右栏并跟随活动标签，同时增加会话内最多 20 条的全局图谱选择历史。真实桌面完成选择 `1 → 17 → 0`、回退/前进 `17 → 1 → 17`，固定局部图谱为 6 节点/7 边并跟随到 `Brief.md`；Windows `\\?\` 路径等价性缺陷已修复。当前进入 M3A-8 语义探索退出审计，通过后才进入 M3B。

M3A-8 已使语义探索阶段收口。退出审计修复了社区与邻居、路径、历史范围静默叠加，真实同会话按 `neighbor → path → community → comparison → history → global` 完成全部语义探索与编辑器右栏局部图谱，宽窄屏、零错误、源文件不变和返回资料库通过。当前进入 M3B-0 专业视觉基线选择审计，先记录修正前事实，不直接堆叠视觉效果。

M3B-0 已完成专业视觉修正前审计。实际代码仅以固定缩放阈值显示边、全部标题和字形；真实 28% 远景语义归零，既有 1,000/5,000 节点默认视图标签重叠，社区、路径、导航、聚类镜头和减少动效均未达到原始 M3B 范围。当前进入 M3B-1 语义缩放与社区远景，先建立密度感知的信息层级与社区摘要，不提前做无语义光效、全套镜头或 M3C 性能重构。

M3B-1 已完成密度感知远中近景、稳定社区摘要、聚合连接和可访问的社区进入/返回。三主题与三档窗口重跑中发现并修复 720×680 社区摘要继承宽屏偏移而越界的问题，现有画布边界门禁；真实资料库零错误且未修改。当前进入 M3B-2 社区轮廓与语义层级，延续远景主题身份到中近景，不提前进入路径、导航或 M3C。

M3B-2 已完成不参与布局的稳定社区凸包色带，中景显示社区名/成员数，近景弱化轮廓，远景继续摘要。暗/浅/高对比三主题和三档窗口均验证 5 个轮廓完整包覆当前成员，零错误、源摘要不变；同时纠正了窄屏安全区不足时远景摘要偶发越界。当前进入 M3B-3 路径与关系视觉表达选择审计，先冻结实际差异与最小范围，不提前进入导航镜头或 M3C。

M3B-3 已完成选择审计。可靠的路径和证据基础之上，实际缺口仍是 network 直线、多重/互反边重叠、Canvas 无标签/路径专属表达、证据面板遮挡和图谱未消费 `reduced`。当前进入 M3B-4，先做确定性曲线/平行路由、切线箭头、选中路径静态标签和面板/相机共存，再进入方向动画；导航镜头与 M3C 继续延后。

M3B-4 已完成屏幕与 SVG/PNG 共用的确定性曲线路由、平行/互反关系分离、切线箭头、选中路径静态标签和面板安全取景。三主题、三档窗口真实复测在多轮截图纠偏后通过 17 条曲线、2 条平行路由、4 节点/3 边路径和 3 个关系标签，零错误且源文件不变。当前进入 M3B-5 选中路径方向动效与减少动效合同；缩略导航、聚类镜头、状态环和 M3C 继续延后。

M3B-5 已完成只作用于活动最短路径的起点到终点方向流动，事实箭头和静态标签保持独立。三主题 × calm/reduced 的真实 Tauri 宽窄屏像素对照确认普通模式移动、减少动效完全静止，失焦暂停/恢复、1 顺向/2 逆向事实段、3 标签/3 证据、零错误和源安全均通过。当前进入 M3B-6 导航与镜头系统选择审计；先审计现状再选择最小增量，M3C 继续延后。

M3B-6 已完成导航与镜头系统选择审计。本阶段没有修改产品运行代码：现有全图适配和社区过滤往返可靠，但无适应选择、受控聚焦、缩略导航、聚类折叠或图谱全屏。真实 Tauri 对照确认搜索聚焦会重启布局并在 260ms 后继续变化；720px 横向工具栏末端命令还差约 12px 才完整可达。当前进入 M3B-7 适应选择与减少动效安全聚焦，同时关闭窄屏命令可达性缺口；其余镜头与 M3C 继续延后。

M3B-7 已完成适应当前可见选择、可中止有界聚焦、详情面板安全取景、查询/结构过滤布局语义分离和 reduced 即时到位。真实 calm/reduced 分别为约 344ms/43 帧和约 38ms/0 帧，目标替换取消旧任务 1 次；两会话分别真实框选 3、4 个节点，最终姿态与独立 oracle 一致。三档窗口末端命令完整可达，零错误且源文件不变。当前进入 M3B-8 剩余导航系统选择审计。

M3B-8 已完成剩余导航选择审计。结合 100/1000/5000 节点真实证据、当前三档 Tauri 几何和实际代码，下一最小增量冻结为 M3B-9 有界语义缩略图与视口导航。社区能力明确为过滤子图而不是聚类折叠；全屏仅有平台 API、无产品命令且不能解决方位。真实测试还发现相机诊断初始为空，M3B-9 必须从实时相机状态无前置操作初始化。聚类折叠、全屏、状态环与 M3C 继续延后。

M3B-9 已完成有界语义缩略图、实时视口框和点击/拖动/键盘导航。5000 节点纯函数输入最多采样 600 个无标签点；点击复用有界/reduced 相机，初始相机无需前置操作即同步。四个真实主题/动效会话、三档窗口、覆盖层碰撞、远景入口、零错误和源安全均通过。当前进入 M3B-10 剩余专业视觉系统选择审计；聚类折叠、全屏、状态外环和 M3C 不得误报完成。

M3B-10 已完成剩余专业视觉系统选择审计且未修改产品运行代码。真实节点时间、当前图度数和真实知识脉搏证明近期修改与关系强度可同步派生，而治理状态仍缺少逐节点合同。当前进入 M3B-11 克制的近期修改与关系强度节点外环；治理扫描接线、聚类折叠、全屏和 M3C 不得并入或误报完成。

M3B-11 已完成克制的近期修改与关系强度节点外环。固定 oracle、真实七档时间、当前图度数、四主题/动效 Tauri、选择/路径优先、远景与思维导图隐藏均通过；治理调用为 0，源文件不变。当前进入 M3B-12 专业视觉系统退出审计，决定结束 M3B 或只选择仍有充分增益的剩余能力；M3C 继续独立验收。

M3B-12 已完成专业视觉系统退出审计。四个真实 Tauri 会话同流程覆盖对象/关系图例、远中近层级、社区轮廓与摘要、曲线/平行边、路径标签和动效、面板安全相机、缩略导航、状态环与三档响应式；首次测试合同的平行路由范围和相机诊断字段错误已按真实结果修正并全量重跑。聚类折叠、全屏和治理外环不再作为 M3B 退出阻塞，M3B 收口；当前进入 M3C-0 大图性能基线选择审计。

M3C-0 已完成真实 100/1,000/5,000 节点性能基线选择。5,000 节点最终可显示且可返回，但布局稳定 `185195ms` 和缩放 `8010ms` 分别超出 `120000/5000ms` 预算；三个规模稳定后均仍约 `132` 次/秒绘制。1,000 节点 20 次生命周期没有持续 heap 增长，三档源文件不变且零运行时错误。测试探针、墙钟计时和平移误入社区子图均已按真实结果纠偏后完整重跑。当前进入 M3C-1，只处理稳定后脏帧刷新与生命周期循环控制；空间索引、Worker、裁剪/缓存和保存视图仍保持独立。

M3C-1 已完成稳定脏帧和生命周期循环控制。真实 100/1,000/5,000 节点稳定绘制均从 `132` 降到 `0 次/秒`，失焦/资料库为 `1/0`，恢复焦点不重启布局；5,000 节点缩放从 `8010ms` 降到 `65ms`。三主题 calm/reduced 路径回归证明连续动效未被误停。5,000 节点布局仍为 `186338ms`、最长任务 `15634ms`，因此进入 M3C-2 主线程阶段剖析选择审计；不得在没有阶段数据前直接选择 Worker 或裁剪方案。

M3C-2 已完成真实主线程阶段剖析。固定种子 5,000 节点在 120 秒预算内未稳定，预算观察点布局累计 `133038ms`、最大单次 `29481ms`，约占已归因主线程成本 99%；Canvas 累计 `948ms`，语义约 `1269ms`，后端构建 `7403ms`。探针自身约 `0.5µs/次`，源安全和返回通过。当前进入 M3C-3 Worker 有界力布局内核；裁剪/缓存不与首个布局修复混入。

M3C-3 已完成 Worker 有界力布局。100/1,000/5,000 节点稳定为 `4777/3542/10964ms`，5,000 节点主线程派发/应用最大 `7.6/26.7ms`，候选 `237165<=240000`；稳定绘制 0/s，活跃取消、陈旧结果忽略、20 次生命周期、源安全与返回通过。冷开发会话 100 节点首次可见 `2511ms` 诊断未达历史 2 秒目标，未放宽布局/交互预算。当前进入 M3C-4 大图性能退出审计。

M3C-4 已完成大图性能退出审计，M3 语义、视觉和性能三线正式收口。真实三档布局稳定 `2376/4037/10631ms`，稳定绘制与空闲长任务均为 0；20 次生命周期资源平衡且 heap 在 16MiB 预算内。完整 5,000 节点和 72 节点过滤社区均通过真实 Windows/Tauri 保存对话框导出 SVG/PNG；复杂 SVG 解码转 PNG 的实际失败已改为同事实源直接 Canvas 光栅化并重跑通过。当前进入 M4-0 跨格式工作流与版本候选入口审计，先做需求/代码/证据选择，不提前提升版本或标记候选。

M4-0 已完成代码事实审计并按要求暂停在实现之前。当前搜索实际为 6/7：DOCX/ODS/ODP/PPTX 有精确定位，OPML 无内部节点定位，Table 内部行未被索引；三个入口仍各自维护定位分派，图谱和工作台也未覆盖全部 M1 对象。搜索返回上下文已真实保留，CSV→Table 的披露较完整，OPML→Canvas 缺少转换前披露。恢复时从 **M4A-1 统一对象定位合同与 Table/OPML 搜索闭环** 接续；先完成统一路由映射和两种缺口格式，不同时扩展图谱、工作台、转换或版本发布。当前不改变 `releaseCandidate=false`。

M4F-4 已完成最终 artifact manifest 与发布就绪审计。真实下载的 NSIS/MSI 与托管回执大小、SHA-256、`NotSigned` 一致，公开 `LongEdit` 名称映射和 192-byte `SHA256SUMS.txt` 已冻结；M4F-3 换行摘要与旧版本 artifact/烟测预期均按真实差异修正。社区候选可发布，企业 RC 仍关闭，当前公开版本仍为 v1.0.15。下一步只执行 M4F-5 Tag、GitHub Release 和三个远端附件回下载复核。

M4F-5 已完成正式发布。`v1.0.16` Tag 解引用到安装包产品提交 `757d543…`，Release `379466292` 为非 Draft、非 Prerelease 且是 latest；三个附件从公开地址回下载后的大小、SHA-256 与签名状态全部符合冻结 manifest。首次 Tag 推送的 `workflow` scope 与单次长上传 Draft 回滚差异均已按事实纠正。社区发布门禁 9/9，企业 RC 仍关闭；下一步只执行 M4F-6 v1.0.15 → v1.0.16 官方应用内更新观察。

M4F-6 已完成：官方 v1.0.15/v1.0.16 NSIS 事实、一次性 Windows 工作流和参数化 runner/probe 已形成可重跑合同。首轮运行 `33350679455` 真实暴露 PowerShell refspec 变量边界错误且未执行安装器；修正后的运行 `33350758818` 完成发现、显式确认、SHA-256 校验、同目录覆盖、自动重启、最新版状态及数据保留，12/12 通过。三张截图人工复核通过，9 个 artifact 文件已逐项哈希导入；v1.0.16 更新链收口，下一步只进入 M5-0 v1.0.17 范围选择审计。

M4A 已完成退出审计：Table、OPML、DOCX、ODS、ODP、PPTX、Workbook 的 7 类搜索对象 7/7 精确打开并保留返回状态；7 类有界图谱对象从 Graph 与关系上下文各 7/7 打开，14/14 返回 Graph。真实图谱为 7 父、15 子、15 条 mention-free `contains`，同源身份稳定、错误 0、7 个源文件不变，普通 DOCX block 与 ODS cell 继续用于搜索而不入图。审计同时修复 PPTX 无 locator 父文件路由残留幻灯片关系焦点的问题。当前接续点为 **M4B-0 工作台对象行动选择审计**，保持 `releaseCandidate=false`。

M4B-0 已完成工作台对象行动选择审计。当前工作台直接写回只有 Markdown 待办，PDF 批注仅查看；真实内部 Table 的 2 条显式 boolean 任务行可按稳定 row ID 定位，但尚未进入待办。七类候选只选择 `.table.json` 布尔任务行，因其具备明确任务语义、稳定 row/column ID、签名与可靠写入；其他格式按无任务语义、只读、可靠副本或上下文事务边界延期。当前进入 **M4B-1 内部 Table 布尔任务行工作台行动闭环**，不混入转换或发布冻结。

M4B-1 已完成内部 Table 布尔任务行工作台闭环。实现严格限定资料库内不超过 8 MiB 的 `.table.json`、唯一显式完成列和严格 `true/false` 字符串，以稳定 row/column ID 并入既有待办；完成、恢复和撤销只定点替换目标 JSON 原始值，不重序列化其余内容。真实桌面取消、完成、逐字节撤销、恢复后重做、冲突拒绝、精准定位和宽窄屏全部通过，最终重跑首个行动入口 744ms、错误 0、最终源摘要不变。当前进入 **M4B-2 工作台对象行动退出审计**，不扩增新行动或进入转换/发布冻结。

M4B-2 已通过，M4B 在有界行动范围收口。Markdown 与内部 Table 是仅有的工作台状态写回族，PDF 未引用批注保持精确只读查看；同一真实 Tauri 会话完成两类写回和逐字节撤销、唯一批注定位、1280/480 响应式，首入口 540ms、错误 0、四份源摘要不变。其他 M1 格式不制造通用任务语义，继续遵守 M4A 定位及各自只读/可靠副本/上下文事务边界。当前进入 **M4C-0 受控转换工作流选择审计**，不直接实现全部转换或进入发布冻结。

M4C-0 已通过选择审计。实际转换矩阵修正为 CSV/TSV→Table、OPML→Canvas、图谱→Canvas、图谱→项目笔记；“设为思维导图中心”只是图谱派生视图切换，不是文件转换。四条真实工作流均生成首个与编号目标且源摘要不变；只有 CSV/TSV→Table 已具备接近完整的前置披露，当前仅缺损失说明与成功自动打开。下一步固定为 **M4C-1 CSV/TSV→Table 披露与自动打开闭环**，其余转换继续延期。

M4C-1 已完成 CSV/TSV→Table 的完整受控转换合同：转换前披露资料库相对来源、候选目标、不覆盖/编号策略和实际解析/投影损失，成功后自动打开后端返回的实际目标；原 CSV/TSV 不写入。真实 Tauri 覆盖首个与编号目标、目标内容复读、1280/480、0 运行时错误和源 SHA-256 不变。代码复核纠正了“保留源序列化元数据”的潜在误读：内部 Table 不保存源编码、BOM 和换行元数据，因此 UI 明确披露这些不会成为目标 JSON 的物理序列化格式。当前进入 **M4C-2 OPML→Canvas 投影披露闭环**；图谱输出、全局转换框架和发布冻结继续延期。

M4C-2 已完成 OPML→Canvas 的受控投影合同：写盘前披露来源、候选目标、不覆盖/编号策略，明确 outline 文本节点、标题+备注、`contains` 层级边和源文件节点的保留规则，以及 `head` 元数据、自定义属性、折叠状态、主题/布局/手工位置和双向同步的损失边界。真实 Tauri 首个与编号目标均自动打开并复读为 5 节点/4 边，源摘要不变，1280/480 和零错误通过。审计发现的窄 Canvas 标题/保存状态挤压已按容器宽度修复。当前进入 **M4C-3 图谱派生输出披露选择审计**；不得把图谱→Canvas 与图谱→项目笔记视为同一种无损转换，也不得提前进入发布冻结。

M4C-3 已完成两条图谱派生输出的代码与真实桌面选择审计。Canvas 是 1～4 层关系/文件节点的独立快照，项目笔记会生成追溯元数据和任务模板、按标题/路径排序并最多写入 100 个关联对象；两者均使用编号新目标、自动打开且来源不变，但均缺少写盘前披露。代码复核发现 Canvas 前端允许全部顶层对象，而实际局部图谱后端只接受 Markdown/PDF；因此下一步唯一选择 **M4C-4 图谱→项目笔记披露闭环**，Canvas 资格纠偏与披露继续独立延期。

M4C-4 已完成图谱→项目笔记受控派生合同：写盘前披露相对中心、1～4 层范围、同目录候选目标、不覆盖/编号/自动打开、追溯元数据、固定模板、标题/路径排序、100 个关联对象上限和独立快照语义。真实 Tauri 两个 102 关联对象中心分别生成首个与编号目标，两份均写入 100 个并记录省略 2 个；207 份既有 Markdown 摘要不变，1280/480 与零错误通过。当前进入 **M4C-5 图谱→Canvas 资格纠偏与快照披露**。

M4C-5 已完成图谱→Canvas 资格纠偏与快照披露。原始路线要求 PDF 和表格节点可携邻居进入 Canvas，因此局部图谱与命令统一支持 Markdown/PDF/CSV/TSV/开放 Table，前端仅开放对应顶层对象；普通 JSON、OPML、Canvas/Office 与内部对象保持关闭。真实 5/5 合法中心、正反入口、首个/编号目标、结构复读、1280/480、0 错误和 10 个来源摘要不变通过，并明确内部 locator 丢失会产生同源重复文件节点。当前进入 **M4C-6 受控转换退出审计**，在组合复验通过前不进入发布冻结。

M4C-6 已通过并关闭 M4C。同一真实 Tauri 会话组合复验 CSV→Table、OPML→Canvas、图谱→项目笔记与图谱→Canvas，四条路径的写前披露、取消不写盘、编号目标自动打开、磁盘复读和来源安全全部通过；10 个受保护文件摘要不变，运行时错误 0。审计同时修复侧栏窄窗口下确认框错误受内容区宽度约束及动画中提前截图的问题。当前进入 **M4D-0 临时产物与冗余证据清理选择审计**，先建立依赖与删除候选，不直接批量清理或进入发布冻结。

M4D-0 已完成仓库级选择审计且未删除文件。931 个新增路径中，179 个脚本与 573 个证据文件继续承担可重跑和历史事实；8 组相同 blob 均有行为或阶段语义。唯一选择 M3C-4 的四个可重建 SVG/PNG 导出负载，共 13,883,957 bytes，其完整验证指标已由 `tier-5000.json` 替代且无 checker/发布依赖。当前进入 **M4D-1 有界生成图谱导出产物清理**，删除范围不得扩大。

M4D-1 已按冻结清单精确删除 M3C-4 四个 5000 节点 SVG/PNG 生成负载，共 13,883,957 bytes；删除前逐项核对当次 `tier-5000.json` 的大小与 SHA-256，其他证据、脚本、夹具和发布文件均受保护。M3C-4 runner 现在只在性能 checker 通过后清除未来重建负载。当前进入 **M4D-2 临时产物与证据清理退出审计**，退出前不汇总版本结论或进入发布冻结。

M4D-2 已通过并关闭 M4D。Git 树对照证明 M4D-1 只有四个授权删除、意外删除 0，冻结周期其余 927 个新增路径全部保留；四份替代指标继续一致，当前无遗留的 post-v1.0.15 SVG/PNG 原生导出负载，也无同条件遗漏候选。当前进入 **M4E-0 能力事实、残余风险与版本决策审计**；先对齐用户可见事实并形成是否进入版本提升的决定，不直接进入发布冻结。

M4E-0 已通过。9 个里程碑闭环、6 组实际代码能力、43 类格式/11 套能力配置以及 README、格式能力页、发布矩阵和开发版说明已一致；结论是累计用户价值足以进入 `v1.0.16` 发布冻结，但尚不是发布候选。完整 Office/ODF 等价、ODS/ODP 高阶编辑、系统媒体依赖、结构化格式高级模式、图谱延后能力和缺失外部生产者证据继续保持有界。当前唯一接续点为 **M4F-0 v1.0.16 发布冻结入口审计**：冻结产品提交，原子提升版本，完整执行发布、安装、安装态回归、哈希和远端发布门禁；任何失败均保持 `releaseCandidate=false`。

M4F-0 已通过。产品范围冻结到 `7e10c340fa0d528b598c9ca6be391c48b15a463f`；版本依赖扫描确认下一步需同提交迁移 44 个当前身份文件，五个 post-v1.0.15 历史基线继续固定为 1.0.15。旧的 R5E 只是真实阻断记录，已纠正为候选提交必须重跑 R5F 路由挂载与 R5G 桌面路由/I/O。九道发布门禁目前完成 1/9；当前唯一接续点为 **M4F-1 v1.0.16 原子版本迁移与候选元数据**，不混入产品功能、不构建安装包、不创建 Tag 或 Release。

M4F-1 已通过。冻结清单中的 44 个 package、Cargo、Tauri 和当前共享合同版本全部迁移到 1.0.16，五个历史基线保持 1.0.15；v1.0.16 社区策略的 Quality Gate、烟测、安装包、哈希、生命周期和远端发布事实全部归零，v1.0.15 候选与 Release 回执未被继承。代码同时纠正候选策略版本与当前公开版本必须相等的旧假设。九道门禁完成 2/9；当前唯一接续点为 **M4F-2 v1.0.16 候选 Quality Gate 与当前运行烟测**，失败即停止进入安装包阶段。
