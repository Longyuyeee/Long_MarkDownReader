# LongEdit 产品需求文档与开发路线图

> 文档类型：主产品需求文档（Master PRD）  
> 文档版本：1.1
> 建立日期：2026-07-18  
> 产品基线：LongEdit v0.7.0
> 文档状态：需求基线；当前能力、计划偏移与 UI 收口顺序以 [2026-08-02 当前开发对齐审计与收口计划](./Development_Alignment_and_Closure_Plan_2026-08-02.md) 为准
> 配套方案：[Professional_Knowledge_Workspace_Design.md](./Professional_Knowledge_Workspace_Design.md)
>
> 多格式管理补充基线：[Unified_File_Manager_Format_Requirements.md](./Unified_File_Manager_Format_Requirements.md)。该文档已重新对齐“在统一管理器内阅读和编辑 PDF、图表、思维导图、WPS/Office、TXT、JSON 及开发常用格式”的原始目标；涉及格式覆盖和后续优先级时，以该补充基线为准。
>
> 后续执行顺序与工程门禁：[Next_Development_Execution_Guide.md](./Next_Development_Execution_Guide.md)。

---

## 1. 文档目的

本文件用于统一产品目标、功能范围、优先级、开发顺序和验收标准，解决以下问题：

- 功能开发缺少统一主线，容易在图谱、Canvas、PDF、Excel 等方向之间跳转。
- 已完成能力与待完成能力没有明确边界。
- “支持某种格式”缺少可验证的产品定义。
- 需求、技术方案、测试和版本验收无法相互追踪。

后续每项开发必须关联一个需求编号；没有进入本 PRD 的功能默认不进入当前版本。

### 1.1 最高产品原则

> LongEdit 的基础需求是：**覆盖日常文件管理和基础编辑，并把这些内容进行成体系的管理。**

该原则高于具体阶段编号和单一格式的高级能力。后续路线必须优先保证常用格式能够被识别、阅读、搜索、基础编辑和安全保存，并通过工作台、目录与集合、标签、全文搜索、批注、引用、关系和可重建索引形成管理闭环。知识图谱、Canvas、仪表盘和 AI 用于增强这一闭环，不得成为基础功能的前置条件，也不得长期挤占格式覆盖主线。

详细格式范围、能力分级和重新排序后的路线见 [Unified_File_Manager_Format_Requirements.md](./Unified_File_Manager_Format_Requirements.md)。

## 2. 用户需求对齐

### 2.1 用户原始目标

| 原始目标 | 对齐后的产品要求 |
|---|---|
| 修复项目现有问题 | 建立安全、稳定、测试、性能和架构基线，不以新增功能掩盖基础缺陷 |
| 增强知识图谱存在感 | 图谱进入编辑器侧栏、搜索、导航、整理和内容生产流程，而非独立展示页 |
| 实现真正的思维导图 | 支持从 Markdown/图谱生成、自由编辑、连接、分组、撤销、多选和开放格式保存 |
| 支持图表、思维导图、Excel、PDF | 通过统一格式适配器接入；不同格式拥有明确的阅读、编辑、批注和索引等级 |
| 完整 Excel 等价编辑器 | 建设原生工作簿编辑工作面，覆盖公式、格式、图表、数据透视、验证、打印与可靠 XLSX 往返 |
| 新的文件格式编辑器 | 先进入统一格式注册、权限、打开路由和可靠写入框架，再按独立工作面逐项实现 |
| 更多主题预设 | 扩展可复用主题 token 和预设，并覆盖所有编辑工作面与深浅模式 |
| 做成专业管理系统 | 形成文档、关系、画布、数据、资料、搜索和仪表盘组成的统一工作台 |
| 审计类似产品并输出设计 | 竞品结论进入产品取舍和验收标准，不直接复制功能数量 |

### 2.2 对齐后的产品定位

> LongEdit 是一个以本地文件为可信数据源，以知识图谱为关系中枢，以开放画布为思考界面，以 PDF 和结构化数据为专业资料管理能力的本地优先知识工作台。

LongEdit 不再仅定位为 Markdown 编辑器。电子表格方向以完整 Excel 等价编辑能力为长期产品目标，但通过独立工作簿内核和渐进验收实现，不把整个 Office 套件的复刻混入同一里程碑。

### 2.3 核心价值顺序

1. 用户数据安全、透明、可迁移。
2. 用户能发现内容之间的关系。
3. 用户能用思维导图和 Canvas 主动整理关系。
4. PDF、表格、图表等资料进入同一搜索和关系网络。
5. 系统能支持长期、规模化的专业资料管理。

## 3. 成功标准

### 3.1 产品成功标准

- 从任意 Markdown 笔记不超过 1 次操作即可看到局部关系。
- 从 Markdown 或图谱不超过 2 次操作即可生成可编辑思维导图。
- Markdown、Canvas、PDF 批注和结构化数据均可被统一搜索和引用。
- 用户文件无需 LongEdit 也能被其他通用工具读取。
- 关键写入操作发生崩溃时不丢失已确认保存的内容。
- 10,000 文件知识库仍能完成增量索引和交互式搜索。

### 3.2 当前阶段不作为成功标准

- 多人实时协作。
- Word/PowerPoint 的完整 Office 排版兼容；这不降低电子表格工作面的 Excel 等价目标。
- 直接修改 PDF 原始页面内容。
- 云端数据库成为唯一数据源。
- 用 AI 自动生成不可解释、不可编辑的结果。

## 4. 目标用户与核心场景

### 4.1 目标用户

| 用户类型 | 主要任务 | 核心痛点 |
|---|---|---|
| 个人知识工作者 | 写作、研究、建立长期知识库 | 内容越来越多，但关系难以发现和维护 |
| 项目/产品经理 | 管理需求、会议、计划、图表和资料 | 文档、表格、脑图分散在不同软件 |
| 研究人员/学生 | 阅读 PDF、摘录、引用和组织主题 | PDF 批注无法进入笔记关系网络 |
| 技术用户 | Markdown、Git、代码和技术文档 | 希望本地可控、可版本化、可扩展 |

### 4.2 关键用户旅程

#### 场景 A：从笔记发现关系

打开笔记 → 右侧看到局部图谱 → 查看关系来源与上下文 → 打开相关笔记 → 修复断链或补充关系。

#### 场景 B：从内容形成思维导图

编辑 Markdown 标题/列表 → 点击“转脑图” → 自动生成层级 Canvas → 多选、拖拽、连线、分组 → 保存为 `.canvas`。

#### 场景 C：从图谱开展主题研究

在全局图谱选择主题 → 展开局部子图 → 发送到 Canvas → 添加网页、PDF、笔记和观点卡片 → 形成专题工作区。

#### 场景 D：PDF 研究

打开 PDF → 搜索和定位 → 高亮或区域批注 → 在 Markdown 中引用批注 → 图谱显示“笔记引用 PDF 批注”的关系。

#### 场景 E：数据管理与图表

打开 CSV/表格 → 编辑、筛选和排序 → 切换为图表或看板 → 把图表嵌入 Markdown/Canvas → 数据源保持唯一。

## 5. 范围和优先级规则

### 5.1 优先级定义

| 等级 | 定义 | 处理原则 |
|---|---|---|
| P0 | 安全、数据完整性、架构阻塞项 | 未完成前不得扩大高风险格式写入范围 |
| P1 | 产品核心差异化能力 | 当前主版本必须完成 |
| P2 | 专业工作流能力 | 基础对象和索引稳定后实施 |
| P3 | 增强与生态能力 | 不阻塞 1.0，可按验证结果排期 |

### 5.2 状态定义

- **已完成**：代码、测试和桌面构建均通过。
- **部分完成**：主流程可用，但仍缺少需求中明确的验收项。
- **未开始**：尚无可验收实现。
- **验证中**：已有技术原型，尚未确认产品或许可可行性。

## 6. 功能需求

### 6.1 工作区、安全和工程基线

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-BASE-001 | P0 | 所有知识库文件命令经过统一 WorkspaceGuard | 部分完成 | 路径穿越、符号链接越界、未登记根目录测试全部拒绝 |
| FR-BASE-002 | P0 | Markdown、Canvas 和配置采用可靠写入策略 | 部分完成 | 写入失败可恢复；不得留下损坏的目标文件 |
| FR-BASE-003 | P0 | API Key 进入系统凭据存储 | 已完成 | 配置文件、前端状态和日志不出现明文密钥；旧配置一次性迁移 |
| FR-BASE-004 | P0 | Rust 单体按命令、服务、格式适配器拆分 | 已完成 | `lib.rs` 只保留应用装配；业务模块拥有独立测试 |
| FR-BASE-005 | P0 | 建立 CI 与关键 E2E 门禁 | 部分完成 | PR 自动执行类型检查、Rust 测试、构建和关键保存流程 |
| FR-BASE-006 | P1 | 文件变化监听与增量刷新 | 未开始 | 外部修改在 1 秒内反映，冲突时不静默覆盖 |
| FR-BASE-007 | P0 | 外部单文件显式授权 | 部分完成 | 启动参数、单实例事件和后端选择器授权的文件可访问；任意前端路径调用被拒绝 |

### 6.2 Markdown 文档工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-MD-001 | P1 | 三种 Markdown 编辑模式及可靠保存 | 已完成 | 所见、IR、源码模式可切换并保持内容一致 |
| FR-MD-002 | P1 | 双向链接、标签、大纲和反向链接 | 已完成 | 链接可导航；反链显示来源与上下文 |
| FR-MD-003 | P1 | Markdown 标题/列表转 Canvas 脑图 | 已完成 | 忽略代码块；保留标题和缩进列表层级；源文档可返回 |
| FR-MD-004 | P2 | 结构化属性/frontmatter 编辑 | 未开始 | 属性可表单编辑且不破坏未知 frontmatter 字段 |
| FR-MD-005 | P2 | 块级稳定定位和引用 | 未开始 | 标题、段落或块具有可迁移锚点，可被 Canvas/PDF 引用 |

### 6.3 知识图谱与关系工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-GRAPH-001 | P1 | 全局关系网络与思维导图双模式 | 已完成 | 任意节点可成为中心，支持 1–4 层展开 |
| FR-GRAPH-002 | P1 | 编辑器内局部图谱 | 已完成 | 当前笔记显示 1–3 跳关系并可直接导航 |
| FR-GRAPH-003 | P1 | 图谱到 Canvas | 已完成 | 局部子图生成标准 `.canvas`，节点可打开原文 |
| FR-GRAPH-004 | P1 | 关系方向、类型和上下文 | 部分完成 | 每条边显示来源语法、方向、上下文和关系类型 |
| FR-GRAPH-005 | P1 | 标签、目录、日期和类型筛选 | 已完成 | 全局/局部图谱共享同一筛选模型 |
| FR-GRAPH-006 | P1 | 断链、歧义链接和孤立笔记治理 | 已完成 | 提供问题列表、定位、批量修复建议和处理状态 |
| FR-GRAPH-007 | P2 | 布局持久化和图谱导出 | 已完成 | 布局保存在设备状态；支持 PNG/SVG 导出 |
| FR-GRAPH-008 | P2 | 跨格式关系 | 已完成 | PDF 批注、Canvas、OPML、表格和图表能作为可寻址图谱节点与边 |

### 6.4 Canvas 与思维导图工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-CANVAS-001 | P1 | JSON Canvas 读写和互操作 | 已完成 | 标准 text/file/link/group/edge 往返不丢字段 |
| FR-CANVAS-002 | P1 | 创建、拖拽、缩放、连线、分组和着色 | 已完成 | 操作后自动保存并可重新打开 |
| FR-CANVAS-003 | P1 | 撤销重做、多选、框选和批量移动 | 已完成 | 支持 60 步会话历史，不污染 `.canvas` 文件 |
| FR-CANVAS-004 | P1 | 对齐和分布 | 已完成 | 多节点可左/顶对齐及水平/垂直均匀分布 |
| FR-CANVAS-005 | P1 | 吸附参考线与键盘微调 | 已完成 | 拖拽显示中心/边缘参考线；方向键可精确移动 |
| FR-CANVAS-006 | P1 | 边属性和标签编辑 | 已完成 | 可编辑方向、端点、颜色、标签和关系类型 |
| FR-CANVAS-007 | P1 | 思维导图分支折叠与自动布局 | 已完成 | 可折叠分支、重新布局且不覆盖用户手工位置 |
| FR-CANVAS-008 | P2 | 复制粘贴和跨画布复用 | 已完成 | 复制节点保留内部关系，不复制无关外部边 |
| FR-CANVAS-009 | P2 | 大画布渲染优化 | 已完成 | 1,000 节点基础操作可用，拖拽目标 ≥ 45 FPS |
| FR-CANVAS-010 | P2 | 图片、PDF 批注和数据图表节点 | 未开始 | 节点预览可用，双击打开源对象 |
| FR-MINDMAP-001 | P1 | OPML 树形与思维导图双视图 | 已完成 | 两种视图编辑同一开放文件，支持折叠、搜索和备注 |
| FR-MINDMAP-002 | P1 | 专业层级编辑 | 已完成 | 支持增删、缩进/减少缩进、拖拽重设父节点和撤销重做 |
| FR-MINDMAP-003 | P1 | OPML 往返、索引与 Canvas 投影 | 已完成 | 可靠写回拒绝外部冲突，语义索引无 XML 噪声，可创建标准 Canvas 快照 |

### 6.5 PDF 研究工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-PDF-001 | P1 | PDF 应用内阅读 | 已完成 | 支持缩略图、目录、缩放、页跳转和连续滚动 |
| FR-PDF-002 | P1 | PDF 文本搜索和复制 | 已完成 | 搜索显示命中数并定位；可复制可选文本 |
| FR-PDF-003 | P1 | 高亮、区域和评论批注 | 已完成 | 批注保存至 sidecar，不默认修改原 PDF |
| FR-PDF-004 | P1 | 批注引用到 Markdown | 已完成 | 点击引用可精确返回页码和批注位置 |
| FR-PDF-005 | P1 | PDF 与批注进入搜索和图谱 | 已完成 | 标题、正文文本和批注可检索；引用关系可见 |
| FR-PDF-006 | P2 | OCR 适配接口 | 已完成 | 扫描 PDF 可按需 OCR；原始文件保持不变 |
| FR-PDF-007 | P2 | 大文件渐进加载 | 已完成 | 100 MB PDF 首屏目标小于 2 秒 |

### 6.6 结构化数据、Excel 和图表工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-DATA-001 | P1 | CSV/TSV 原生查看与编辑 | 已完成 | 支持类型推断、编辑、排序、筛选、冻结和虚拟滚动 |
| FR-DATA-002 | P1 | 内部开放表格格式 | 已完成 | 数据与视图配置分离，文件可导出 CSV/XLSX |
| FR-DATA-003 | P1 | 表格多视图 | 已完成 | 同一数据源可切换表格、看板和图表而不复制数据 |
| FR-DATA-004 | P1 | 图表编辑器 | 已完成 | 支持柱、线、饼、散点等基础图表和字段映射 |
| FR-DATA-005 | P1 | 图表嵌入 Markdown/Canvas | 已完成 | 嵌入对象引用原数据源，源数据更新后同步刷新 |
| FR-DATA-006 | P1 | XLSX 导入、预览和导出 | 已完成 | 多 Sheet 分页预览、公式文本/缓存结果、样式读取、Sheet 转 Table 与受保护的原文件渐进式编辑均已完成；高级等价范围由 FR-DATA-009 跟踪 |
| FR-DATA-007 | P2 | Univer 等完整编辑方案验证 | 已完成 | 审计结论为默认不引入；商业需求获批后按隔离式适配层重新立项 |
| FR-DATA-008 | P3 | 数据透视和仪表盘 | 已完成 | 多图表可共享筛选条件并保存布局 |
| FR-DATA-009 | P1 | 完整 Excel 等价工作簿编辑器 | 部分完成 | 单元格/区域编辑、样式、公式重算、图表、数据透视、验证、冻结、排序筛选、打印与 XLSX 往返通过真实 fixture 差异门禁 |

> Excel 能力继续采用渐进交付，但目标基线已调整为完整等价编辑器。当前版本仍只能标注为 XLSX 预览/导入，后续每个兼容子集必须用真实工作簿 fixture 验收，不以“能打开”代替往返保真。

### 6.7 图表与流程图工作面

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-DIAGRAM-001 | P1 | Mermaid 图表工作室 | 已完成 | 源码编辑、实时预览、错误定位和模板选择可用 |
| FR-DIAGRAM-002 | P1 | 节点表单与源码双向同步 | 已完成 | 常用流程图表单修改生成可读 Mermaid，源码修改可刷新表单；高级语法无损保留 |
| FR-DIAGRAM-003 | P1 | SVG/PNG 导出 | 已完成 | SVG 保持矢量和文字；PNG 支持 1×/2×/3× 倍率与透明/主题/白色背景 |
| FR-DIAGRAM-004 | P2 | Canvas 图表节点 | 已完成 | 图表以标准文件节点和可编辑源码引用进入 Canvas，而非静态截图 |

### 6.8 统一对象、搜索和管理工作台

| 编号 | 优先级 | 需求 | 当前状态 | 验收标准 |
|---|---:|---|---|---|
| FR-INDEX-001 | P0 | 统一 KnowledgeObject/Relation 模型 | 已完成 | Markdown、Canvas、OPML、PDF、Table 通过适配器输出统一元数据 |
| FR-INDEX-002 | P0 | 可重建本地索引 | 已完成 | 删除索引后可从用户文件重建；索引不是唯一事实源 |
| FR-INDEX-003 | P1 | 跨格式全文搜索 | 已完成 | 搜索结果标明类型、位置、摘要和所属知识库 |
| FR-INDEX-004 | P1 | 文件类型注册与统一打开路由 | 已完成 | 新格式通过适配器注册，不继续向 LibraryMode 堆充分支 |
| FR-FORMAT-001 | P1 | 新文件格式编辑器扩展框架 | 已完成 | 每种新格式声明扩展名、能力、路由、权限和索引适配器，并可独立按需加载 |
| FR-TEXT-001 | P1 | 统一开发文本工作面 | 已完成 | CodeMirror 6 独立工作面、编码/换行策略、签名冲突、大文件范围、最近文件、系统外部打开、自动保存和统一标签均已交付；A5 已完成真实 Tauri 保存重开、冲突、大文件与进程重启验收 |
| FR-JSON-001 | P1 | 通用 JSON/JSONC 工作面 | 已完成 | A3/A5/A3R 已完成统一创建、源码工具、实时 Rust 校验、有界 JSON Path、树形预览、受门禁结构编辑、可重建索引、可靠保存、真实桌面重开、搜索和最近记录 |
| FR-DEV-001 | P2 | 常见代码与工程文件阅读编辑 | 部分完成 | A4 已交付 YAML/XML/TOML、常见配置和代表性代码格式的可靠轻量编辑，`.env` 默认遮罩且不索引，`.log` 提供有界只读、筛选、追尾和轮转；日志显式编辑、代码执行、调试和语言服务不在当前范围 |
| FR-PDF-008 | P1 | PDF 页面级编辑 | 基础子集已完成 | B0～B2C 已完成旋转、改序、排除、按页范围提取、多文件有序合并、指定位置插页、隔离副本验证、可靠另存、重开和兼容画像；高级正文/表单/签名不在本阶段，失败不破坏源文件 |
| FR-OFFICE-001 | P1 | DOCX 阅读与基础编辑 | 已完成 | C0～C2E 已完成结构化阅读、索引、受限文本/样式/图片说明编辑、可靠另存和 Word/WPS/LibreOffice 输出复开；完整排版等价不在本阶段 |
| FR-OFFICE-002 | P1 | PPTX 阅读与基础编辑 | 已完成 | C3A～C5D 已完成安全阅读、知识管理、受限文本/图片/形状/页面生命周期编辑、可靠新副本、外部 3/3 复开及 `basic-edit / copy` 发布矩阵 |
| FR-OFFICE-003 | P2 | WPS、旧版 Office 与 OpenDocument | 进行中 | E0/E1A 已完成；E1B ODT 解析、只读工作面、索引和定位实现完成，Word/LibreOffice 生产者及真实桌面证据通过，WPS 关闭候选自动接入已就绪；真实 WPS 环境门禁及 E1C/E2/E3 尚待完成，不虚假声明原格式完整编辑 |
| FR-MANAGER-001 | P1 | 统一格式能力展示与降级 | 已完成 | schema v2、能力等级、保存模式、最长扩展名路由、Library 文件树/状态栏、右侧工作面标签、最近文件、外部打开和 TXT/JSON/开发格式降级均已接通；新格式继续复用该契约 |
| FR-THEME-001 | P1 | 扩展主题预设系统 | 已完成 | 唯一注册表驱动设置、命令、编辑器和图表消费者；3 个核心 + 4 个场景预设、持久化、动效、WCAG AA 门禁和 12 张真实 Tauri 三档尺寸证据已建立 |
| FR-WB-001 | P2 | 专业主页/工作台 | 已完成 | 展示最近、收藏、待办、知识库健康度和常用 Canvas |
| FR-WB-002 | P2 | 集合、保存的搜索和智能视图 | 已完成 | 查询可保存并作为动态集合重复使用 |
| FR-WB-003 | P2 | 知识库健康度 | 已完成 | 展示断链、孤立内容、重复文件、未处理批注和索引状态 |

## 7. 非功能需求

### 7.1 数据安全

- 用户文件是事实源，索引、缩略图和布局缓存必须可删除、可重建。
- 所有写操作必须有明确授权根目录、格式白名单和大小限制。
- PDF 默认只读，批注使用 sidecar 文件。
- 任何自动转换都生成新文件，不静默覆盖源文件。
- 不在日志、配置或错误信息中输出密钥和完整敏感内容。

### 7.2 性能

| 指标 | 目标 |
|---|---:|
| 10,000 Markdown 文件首次索引 | < 30 秒 |
| 单文件增量索引 | < 300 ms |
| 全文搜索首屏 | < 150 ms |
| 2,000 节点全局图谱 | ≥ 45 FPS |
| 1,000 节点 Canvas 基础操作 | ≥ 45 FPS |
| 100 MB PDF 首屏 | < 2 秒 |
| 50,000 行 CSV 首屏 | < 2 秒 |

### 7.3 兼容性与可迁移性

- Markdown 保持 CommonMark/GFM 友好。
- Canvas 遵循 JSON Canvas 1.0，未知字段往返保留。
- 表格至少支持 CSV/TSV 导入导出。
- 批注 sidecar 使用版本化 JSON Schema。
- 新格式必须提供版本号、迁移策略和降级读取方案。

### 7.4 可用性

- 常用动作可在界面发现，不依赖记忆快捷键。
- 所有图形编辑动作提供撤销。
- 长任务显示进度并允许取消。
- 空状态说明下一步操作，而不是只显示“暂无数据”。
- 错误信息说明对象、原因和恢复方式。

### 7.5 测试和质量门槛

每个需求进入“已完成”必须同时满足：

1. 主流程和失败流程均已实现。
2. Rust/TypeScript 类型与单元测试通过。
3. 涉及保存的功能包含往返或恢复测试。
4. 前端生产构建和 Tauri 桌面构建通过。
5. 无新增高危依赖漏洞。
6. PRD 状态和设计文档同步更新。

## 8. 开发里程碑与顺序

### Milestone 0：需求冻结与工程地基（v0.7.0）

目标：先建立可安全扩展多格式能力的底座。

实施顺序：

1. FR-BASE-001 WorkspaceGuard 全覆盖。
2. FR-BASE-002 统一可靠写入与恢复。
3. FR-BASE-004 拆分 Rust `lib.rs`。
4. FR-INDEX-001 统一对象和关系接口。
5. FR-INDEX-004 格式注册与打开路由。
6. FR-BASE-005 CI、fixture 和关键 E2E。
7. FR-BASE-003 系统凭据存储。

退出条件：后续新增格式无需修改巨型页面和单体命令文件；保存、路径和迁移具备自动化门禁。

### Milestone 1：视觉知识闭环（v0.8.0）

目标：把图谱和 Canvas 从可用原型提升为稳定核心功能。

实施顺序：

1. FR-GRAPH-004 关系上下文和类型。
2. FR-GRAPH-005 统一筛选。
3. FR-GRAPH-006 断链/孤立内容治理。
4. FR-CANVAS-005 吸附参考线与键盘微调。
5. FR-CANVAS-006 边标签与关系属性。
6. FR-CANVAS-007 分支折叠和自动布局。
7. FR-CANVAS-008 复制粘贴。
8. FR-CANVAS-009 大画布优化。
9. FR-GRAPH-007 布局持久化与导出。

退出条件：用户可从发现关系、整理关系到输出脑图完成闭环；1,000 节点画布达到性能目标。

### Milestone 2：PDF 研究工作流（v0.9.0）

目标：让 PDF 成为可搜索、可引用、可关联的知识对象。

实施顺序：

1. FR-PDF-001 阅读器基础。
2. FR-PDF-002 搜索、文本层和复制。
3. FR-PDF-003 sidecar 批注模型。
4. FR-PDF-004 Markdown 引用与反向定位。
5. FR-PDF-005 搜索和图谱接入。
6. FR-CANVAS-010 PDF 批注节点。
7. FR-PDF-007 大文件优化。

退出条件：原 PDF 不被修改；批注可迁移、可搜索、可引用、可进入图谱。

### Milestone 3：表格、数据和图表（v0.10.0）

目标：完成从结构化数据到多视图和图表的闭环。

实施顺序：

1. FR-DATA-001 CSV/TSV 编辑器。
2. FR-DATA-002 内部 Table 格式。
3. FR-DATA-003 表格/看板多视图。
4. FR-DATA-004 图表编辑器。
5. FR-DATA-005 Markdown/Canvas 嵌入。
6. FR-DATA-006 XLSX 导入预览导出。
7. FR-DATA-007 完整 XLSX 编辑技术决策。

退出条件：同一数据源可编辑、筛选并切换图表，不复制数据；XLSX 有明确支持等级。

### Milestone 4：专业工作台（v1.0.0）

目标：把各工作面整合为统一管理系统。

实施顺序：

1. FR-DIAGRAM-001～004 图表工作室。
2. FR-INDEX-003 跨格式全文搜索完善。
3. FR-WB-001 专业主页。
4. FR-WB-002 保存的搜索与集合。
5. FR-WB-003 知识库健康度。
6. 全链路性能、恢复、迁移和无障碍验收。

退出条件：Markdown、Canvas、PDF、表格和图表处于统一导航、搜索、引用和关系系统中。

## 9. 当前实现盘点

### 9.1 已完成并验证

- Markdown、TXT、JSON/JSONC、YAML、XML、TOML、配置/代码文本、JSON Canvas、OPML、PDF、Open Table、Mermaid 和渐进式 XLSX 工作面。
- 全局/局部知识图谱、细粒度跨格式对象、关系治理、布局、导出，以及工作台/搜索/当前文件上下文中的日常关系摘要。
- 文件格式注册表、统一打开路由、可重建本地索引和索引优先的跨格式搜索。
- 专业主题预设、默认工作台、收藏/最近/待办/常用 Canvas 聚合。
- `WorkspaceGuard`、可靠写入、外部文件授权、凭据库和依赖漏洞治理。
- PDF 页面旋转、改序、排除、隔离验证和可靠另存安全子集。
- DOCX C1-2B1 结构化只读工作面、样式/编号/图片、附属正文、全局索引、对象定位和兼容画像。
- 当前 Rust 单元测试、前端生产构建、契约门禁和真实 Tauri Debug/WebView2 36 项检查、28 张截图通过。

### 9.2 已完成原型但仍需产品化

- XLSX 已形成渐进式编辑与分层保真门禁，但完整公式、图表、透视、打印和总体往返等价仍未完成。
- 本地索引仍采用全量重建，尚未实现文件级增量更新。
- 知识健康已覆盖断链、歧义、孤立笔记、精确重复文件、未处理 PDF 批注和索引状态；治理结果保持只读并可精确回到源对象。
- DOCX C0～C2E 已达到结构化阅读、索引、受限用户编辑、可靠另存、应用内重开和三生产者输出复开；完整 Word/WPS 排版等价仍不宣称。
- 真实桌面 E2E 已建立阶段性门禁；跨格式持续视觉回归和发布机矩阵仍需产品化。

### 9.3 尚未进入实现

- PPTX 母版、动画、SmartArt、复杂图表和完整 PowerPoint 等价编辑。
- WPS/OpenDocument/旧版 Office 的分级预览、转换或编辑方案。
- 安装、文件关联、升级、卸载和多机器发布矩阵。
- 1.0 之外的实时多人协作和云端事实源。

## 10. 需求追踪矩阵

| 用户核心诉求 | 对应需求 | 当前结论 | 目标里程碑 |
|---|---|---|---|
| 修复存在问题 | FR-BASE-001～007 | 安全与架构主基线完成，阶段性桌面 E2E 已建立；发布机矩阵待补 | v0.7.0 |
| 增强图谱存在感 | FR-GRAPH-001～008 | 统一对象、关系创作与治理核心完成 | v0.8.0 |
| 真正思维导图 | FR-MD-003、FR-CANVAS-001～009、FR-MINDMAP-001～003 | OPML 专业层级编辑与 Canvas 自由表达闭环完成 | v0.8.0 |
| 软件内编辑图表 | FR-DIAGRAM-001～004、FR-DATA-004～005 | Table 图表编辑与 Markdown/Canvas 实时引用完成 | v0.10.0～v1.0.0 |
| 软件内处理 Excel | FR-DATA-001～009 | CSV/Table 闭环与渐进式 XLSX 编辑完成；总体 Excel 等价仍部分完成 | v0.10.0～v1.0.0 |
| 软件内处理 PDF | FR-PDF-001～008 | 阅读、搜索、批注、引用、索引、图谱和离线 OCR 闭环完成；页面整理安全子集部分完成 | v0.9.0～v1.0.0 |
| 软件内处理 WPS/Office | FR-OFFICE-001～003 | DOCX C0～C2E、PPTX C3A～C5D 已完成基础阶段；WPS/OpenDocument/旧版 Office 待完成 | v1.0.0 |
| 专业管理系统 | FR-INDEX-001～004、FR-WB-001～003 | 统一索引、专业主页、动态集合与健康治理已完成 | v0.7.0～v1.0.0 |

## 11. 迭代实施记录

以下保留从 Sprint 0 开始的历史实施记录；当前开发入口以本节末尾的最新批次为准。

### Sprint 0：工程地基第一批

建议下一轮只处理以下范围：

1. 建立 `WorkspaceGuard`，替换 Canvas、图谱和知识库文件命令中的分散路径校验。
2. 建立统一 `atomic_write` 和崩溃恢复约定。
3. 抽取 `formats/canvas.rs` 与 `commands/canvas.rs`，开始拆分 Rust 单体。
4. 为 Canvas 读写、Markdown 转换、路径越界建立 fixture 测试。
5. 建立最小 CI：前端构建、Rust 测试、依赖审计。

本 Sprint 不新增 PDF、Excel 或图表功能。完成工程地基后再进入视觉知识闭环收尾，避免多格式能力继续堆入单体架构。

### Sprint 0 验收清单

- [x] 所有 Canvas 命令必须通过 WorkspaceGuard。
- [x] 原子写入失败测试证明目标文件可恢复。
- [x] Canvas 业务不再位于 `lib.rs` 主体中。
- [x] 至少包含合法、损坏、超限、越界四类 fixture。
- [x] 一条命令可完成全部 CI 检查。
- [x] 设计文档与本 PRD 状态同步。

### Sprint 0 实施记录

#### S0-1：WorkspaceGuard 第一批（已完成）

- 新增独立 `services/workspace_guard.rs`，统一知识库根目录规范化、相对/绝对路径解析、待写入目标解析和扩展名白名单。
- Canvas 创建、读取、写入、Markdown 转 Canvas、图谱转 Canvas 已全部迁移到 WorkspaceGuard。
- 全局/局部图谱从规范知识库根目录扫描，递归扫描跳过符号链接，防止通过链接读取库外内容。
- 新增库内路径、父目录穿越、库外绝对路径、安全待写入路径、扩展名和符号链接测试。
- Rust 测试总数由 8 项增加到 12 项。

FR-BASE-001 仍保持“部分完成”：下一批需要继续迁移 Markdown 读写、导入、重命名、移动、删除、搜索和历史版本命令。

#### S0-2：可靠写入与崩溃恢复（已完成）

- 新增独立 `services/reliable_write.rs`，统一同目录临时文件、磁盘同步、恢复备份、文件提升和残留清理流程。
- 同一进程中的写入统一串行化，避免自动保存、手动保存和影子保存产生交叉内容。
- Markdown、Canvas、应用配置、目录排序、历史版本、影子副本、图片和 HTML 导出已迁移到统一写入服务。
- Markdown、Canvas 和配置读取会识别中断写入留下的恢复备份；目标缺失时恢复旧文件，目标完整时清理残留备份。
- 新增 Unicode 内容往返、替换失败恢复、进程中断残留恢复和 8 路并发写入完整性测试。
- Rust 测试总数由 12 项增加到 16 项。

FR-BASE-002 仍保持“部分完成”：可靠写入核心已落地，后续仍需补充真实进程终止 E2E、磁盘空间不足和只读文件系统测试。

#### S0-3：Canvas 后端模块拆分（已完成）

- 新增 `formats/canvas.rs`，负责 JSON Canvas 结构校验、Markdown 标题/列表解析和标准 Canvas 文档生成。
- 新增 `commands/canvas.rs`，负责 Canvas 创建、读取、写入、Markdown 转换、图谱转换和 Tauri 命令边界。
- Canvas 格式测试迁移到格式模块，图谱转 Canvas 测试迁移到命令模块；测试总数保持 16 项且全部通过。
- 前端 IPC 命令名和参数保持不变，现有 `.canvas` 文件无需迁移。
- `lib.rs` 从约 76 KB / 1,630 行下降到约 60 KB / 1,351 行。

Canvas 命令目前通过受控接口调用尚未拆分的图谱和 Markdown 服务；后续拆分 `commands/graph.rs` 与 `formats/markdown.rs` 后移除该过渡依赖。

#### S0-4：Canvas 文件兼容性与边界 Fixture（已完成）

- 新增合法 Canvas、悬空连线损坏 Canvas、层级 Markdown 和路径越界输入四组实体 fixture；超限 Canvas 根据统一 20 MB 常量动态构造，避免提交大体积测试文件。
- 格式层覆盖合法文档、损坏关系、超限内容和 Markdown 层级转换；命令层覆盖真实文件读取、可靠写入和规范路径返回。
- 新增 Canvas 写入后读取的内容无损往返测试，以及 Markdown 文件经命令生成 Canvas 后再次读取和解析的完整回归测试。
- 路径越界测试同时覆盖 `../` 父目录跳转和知识库外绝对路径；超限测试确认拒绝发生在文件创建前。
- Rust 测试总数由 16 项增加到 21 项，全部通过。

至此 Sprint 0 的 Canvas 测试地基已完成；下一步进入 S0-5，建立一条命令完成前端构建、Rust 测试和依赖审计。

#### S0-5：统一质量门禁与最小 CI（已完成）

- 新增 `npm run ci:check` 统一入口，依次执行 TypeScript 类型检查与前端生产构建、锁定 `Cargo.lock` 的 Rust 测试、生产依赖安全审计。
- 新增 `test:rust` 和 `audit:prod` 子命令，既可单独定位问题，也由统一入口按固定顺序调用。
- 新增 Windows GitHub Actions 工作流，对每次 Pull Request 和 Push 自动执行 `npm ci` 与统一质量门禁；只授予仓库内容读取权限，并取消同分支上的过期运行。
- 本地完整执行统一命令通过：21 项 Rust 测试全部成功，前端生产构建成功，生产依赖漏洞为 0。
- 开发说明已加入 README，开发者和 CI 使用同一命令，减少检查规则漂移。

Sprint 0 工程地基第一批至此全部完成。FR-BASE-005 标记为“部分完成”，因为最小 CI 已落地，但真实 Tauri 进程级保存/崩溃恢复 E2E 仍需在后续迭代补充。

### Milestone 1 实施记录

#### S1-1：关系方向、类型与上下文第一批（已完成）

- 新增统一 Markdown Wikilink 解析器，为每次引用保留目标、别名、原始语法、Unicode 安全上下文、行号和关系类型；代码围栏及行内代码中的示例链接不进入图谱。
- 图谱边从单纯 `source/target` 扩展为带 `relationType`、`directed` 和多条 `mentions` 证据的关系对象；相同来源、目标和类型的重复引用会合并，但不会丢失上下文。
- 普通正文 Wikilink 使用 `links-to`；支持设计文档约定的 `relations` frontmatter 轻量类型，其中 `related` 作为无方向关系处理。
- 编辑器“引用”侧栏的局部图谱新增关系依据卡片，可查看链入/链出、关系类型、语法、上下文、行号和重复出现次数，并点击打开相关笔记；1～3 跳深度偏好在设备端持久化。
- 全局图谱节点详情同步显示关系依据；有方向边绘制方向箭头，无方向 `related` 边使用虚线，类型和方向不再只存在于后端数据中。
- 反向链接上下文改用统一解析器，修复旧实现按 UTF-8 字节切片时可能在中文内容上触发边界异常的问题。
- Rust 测试新增普通/别名引用、代码忽略、Unicode 上下文、frontmatter 类型、重复证据合并和无方向关系测试。

FR-GRAPH-004 保持“部分完成”：Markdown 关系证据和类型展示已经形成闭环，后续仍需提供关系类型可视化编辑、全部证据展开定位以及 Canvas/PDF 等跨格式关系适配。

#### S1-2：全局与局部图谱统一筛选（已完成）

- 图谱节点补充 `tags`、知识库相对目录、最后修改时间和对象类型元数据；关系边沿用 S1-1 的关系类型，形成筛选所需的统一数据输入。
- 新增共享 `GraphFilters` 模型，统一支持标题/路径/标签搜索、标签、目录、最近 7/30/365 天、对象类型、关系类型和孤立笔记开关。
- 全局图谱和编辑器局部图谱使用同一响应式筛选状态；任一视图修改条件后另一视图立即沿用，状态保存在设备端并可一键清除。
- 局部图谱筛选时始终保留当前笔记中心节点；关系类型筛选只保留对应边和端点，避免出现没有关系依据的误导节点。
- 新增复用型 `GraphFilterControls.vue`，全局工具栏与局部侧栏使用同一控件；筛选结果实时更新节点数、边数、布局和关系依据列表。
- 新增图谱节点元数据回归测试，验证标签去重、相对目录、修改时间、对象类型和类型化关系输出；Rust 测试总数由 27 项增加到 28 项。

FR-GRAPH-005 验收完成。

#### S1-3：断链、歧义链接和孤立笔记治理（已完成）

- 新增知识库级健康扫描命令，统一识别已解析链接、断链、重名导致的歧义链接和无有效关系的孤立笔记，并返回来源、行号、原始语法与上下文。
- 断链结合标题、文件名和路径进行 Unicode 安全的相似度匹配，输出带置信度的候选项；歧义链接列出全部真实目标，不替用户猜测最终指向。
- 新增显式修复命令，支持单条修复和最多 100 条明确建议的批量修复；保留 Wiki Link 别名，并对知识库边界、文件扩展名、行号、原始语法和目标存在性重新校验。
- 修复在全部请求通过预校验后才写入，沿用可靠写入服务；源文件无法读取、内容已变化或目标越界时拒绝操作，避免静默覆盖与路径逃逸。
- 全局图谱新增“知识治理”面板，提供问题总览、分类切换、来源定位、候选目标、单条/批量修复、孤立笔记入口和修复后图谱刷新。
- Rust 新增断链/歧义/孤立识别、别名修复以及越界和陈旧语法拒绝测试，测试总数由 28 项增加到 31 项。

FR-GRAPH-006 验收完成。下一步进入 S1-4，完善 Canvas 对齐吸附、参考线和键盘微调，强化专业思维导图编辑体验。

#### S1-4：Canvas 吸附参考线与键盘微调（已完成）

- 单节点和多选节点拖拽均按整体边界计算吸附，可对齐其他节点的左/右/上/下边缘及水平/垂直中心。
- 吸附阈值随画布缩放比例换算，保持不同缩放级别下接近一致的屏幕手感；同时只选择水平和垂直方向各自最近的候选，避免拖拽跳动。
- 命中时显示贯穿移动对象与参照对象的动态参考线，并标记“边缘”或“中心”；松开鼠标、关闭吸附或按住 Alt 临时绕过时立即清除。
- 工具栏提供可发现的吸附开关，开关偏好只保存在设备端，不向开放 JSON Canvas 文件写入私有字段。
- 选中节点后，方向键每次移动 1 px，Shift+方向键每次移动 10 px；长按按键合并为一次撤销起点，并沿用自动保存机制。
- 拖拽吸附和键盘微调均支持多选节点、60 步撤销/重做以及可靠写入，不改变现有互操作数据结构。

FR-CANVAS-005 验收完成。下一步进入 S1-5，实现 Canvas 边标签、方向、端点、颜色和关系类型编辑。

#### S1-5：Canvas 边标签与关系属性（已完成）

- 选中任意连线后显示独立属性面板，可直接查看起止节点并编辑标签、关系类型、方向、起止端口和颜色。
- 方向提供正向、反向、双向和无方向四种模式，映射到 JSON Canvas 1.0 标准 `fromEnd/toEnd` 字段；端口使用标准 `fromSide/toSide` 的上、右、下、左或自动模式。
- 颜色支持 JSON Canvas 六种预设色、默认主题色和自定义十六进制颜色；标签、颜色、端口和箭头均直接渲染到画布。
- 关系类型采用可选 `relationType` 扩展字段，预置链接、相关、依赖、支持、反驳和引用，同时允许自定义值；第三方工具可忽略该字段，本软件会完整往返。
- 新建连线默认使用 `links-to` 关系；从知识图谱生成 Canvas 时同步带入关系类型，并将有向/无向语义转换为标准箭头端点。
- 文本编辑按一次会话进入撤销栈，选择类属性每次变更均可撤销，并沿用自动保存与可靠写入。
- Canvas 后端校验新增端口、端点、颜色、标签长度和关系类型长度检查；Rust 测试总数由 31 项增加到 32 项。

FR-CANVAS-006 验收完成。下一步进入 S1-6，实现思维导图分支折叠与不覆盖手工位置的自动布局。

#### S1-6：思维导图分支折叠与自动布局（已完成）

- 有子节点的卡片新增分支控制，可直接折叠/展开整棵后代子树并显示隐藏节点数量；工具栏在选中节点时提供同样的可发现入口。
- 分支层级根据连线箭头方向解析：正向、双向和无方向连线沿 `fromNode → toNode` 建立稳定层级，反向连线沿 `toNode → fromNode`；遍历使用已访问集合防止环形关系导致死循环。
- 折叠后隐藏后代节点及相关连线，框选、全选、吸附、适应内容和连线计数均只使用可见对象，避免操作到不可见内容。
- 折叠状态按 Canvas 路径保存在设备端，重新打开画布后恢复；状态不写入 `.canvas`，不会污染 JSON Canvas 互操作文件。
- “整理分支”只处理当前选中节点的可见子树：根节点保持原位，画布其他分支完全不动；每一层按节点实际宽高和原有纵向顺序进行水平树形排布。
- 自动布局前会为所有被移动节点建立设备端手工位置恢复点，并提供“恢复手工位置”入口；恢复点跨重新打开画布有效，布局和恢复操作也都可撤销。
- 自动布局只写入标准节点坐标，折叠状态、恢复点和算法状态均不进入用户文件；最终保存继续使用可靠写入链路。

FR-CANVAS-007 验收完成。下一步进入 S1-7，实现节点与内部连线的复制粘贴及跨画布复用。

#### S1-7：Canvas 复制粘贴与跨画布复用（已完成）

- 工具栏和 `Ctrl+C / Ctrl+X / Ctrl+V` 均支持复制、剪切和粘贴；文本框与属性输入框保持系统原生剪贴板行为，不劫持文字编辑。
- 复制片段包含全部选中节点以及两端都在选择集内的连线，不复制连接到未选节点的外部边；节点、边及未知 JSON Canvas 字段均完整保留。
- 粘贴为每个节点和连线生成新 ID，并通过映射重建内部关系，避免与目标画布现有对象冲突；新片段定位到当前视口中心，连续粘贴自动错开。
- 剪贴板内容使用标准 `nodes/edges` JSON 结构，可接受本软件片段和完整 JSON Canvas 文档；会话剪贴板作为系统剪贴板权限不可用时的降级方案，并支持同一应用会话内跨画布复用。
- 跨目录画布粘贴文件节点时，根据知识库相对来源自动重定位 `file` 路径，确保仍指向原知识对象；剪贴板不记录知识库绝对路径。
- 粘贴前校验 5 MB 上限、1,000 节点、5,000 连线、节点类型与必填字段、ID 唯一性、内部引用、端口、箭头、标签和关系类型；无效外部内容不会修改画布。
- 粘贴和剪切均进入 60 步撤销/重做与自动保存链路；粘贴后自动选中新节点，便于继续移动、布局或编辑属性。

FR-CANVAS-008 验收完成。下一步进入 S1-8，针对千节点 Canvas 实施视口裁剪、计算缓存和拖拽性能优化。

#### S1-8：千节点 Canvas 渲染与交互性能（已完成）

- 新增响应式节点 ID 索引和方向邻接索引，将连线端点查找、分支判断和层级遍历从重复全图扫描改为 O(1) 查询与 O(N+E) 索引构建。
- 当可见节点超过 180 个时启用视口裁剪，仅挂载当前视口及 260 屏幕像素缓冲区内的节点；平移、缩放和窗口尺寸变化会实时更新裁剪范围。
- 大画布连线同步按几何包围盒裁剪；视口外节点与连线仍保留在文档和选择模型中，只是不创建无用 DOM/SVG 元素。
- 连线曲线路径使用坐标、尺寸、端口、箭头和显示属性签名缓存，拖动单个节点时仅重新计算受影响连线；删除或切换画布后自动回收失效缓存。
- 节点拖拽热路径只遍历实际移动的节点，不再在每次鼠标事件中扫描全画布；框选、折叠、自动布局和复制粘贴继续使用完整数据模型。
- 使用 `ResizeObserver` 跟踪画布视口尺寸；大画布标题实时显示“实际渲染节点/连线数”和采样 FPS，便于在目标硬件上直接核验 ≥45 FPS 指标。
- 新增 1,000 节点、999 连线的 Canvas 结构回归测试，并设置 2 秒宽松上限防止格式校验复杂度退化；Rust 测试总数由 32 项增加到 33 项。

FR-CANVAS-009 验收完成。下一步进入 S1-9，实现知识图谱布局持久化与 PNG/SVG 导出。

#### S1-9：知识图谱布局持久化与 PNG/SVG 导出（已完成）

- 全局关系网络的节点坐标按知识库隔离保存在设备状态；重新进入图谱时优先恢复已有坐标，新增节点继续参与增量布局，不向 Markdown 或其他互操作文件写入私有字段。
- 思维导图按“知识库 + 中心节点 + 展开深度”分别保存手动调整位置，切换中心或深度不会覆盖其他脑图布局。
- 节点拖拽、力导向布局收敛及离开图谱时都会保存布局；设备状态限制为最多 40 个布局、每个布局最多 5,000 个位置，损坏状态自动降级为重新布局。
- 重置按钮只清除当前关系网络或当前中心/深度脑图的已保存布局，并立即生成可继续编辑的新布局。
- 新增 SVG/PNG 导出：导出当前模式和筛选条件下的完整图谱边界，不受当前平移、缩放或屏幕裁剪影响；SVG 保留节点文字、关系方向和非定向边样式，PNG 使用最高 2 倍像素密度并限制在 8,192 像素/3,200 万像素内。
- 导出通过系统保存对话框选择目标位置，使用现有 Tauri dialog/fs capability，不新增任意路径写入 IPC 命令。

FR-GRAPH-007 验收完成，Milestone 1“视觉知识闭环”退出条件已满足。下一步进入 Milestone 2 / S2-1，实现 FR-PDF-001 内置 PDF 阅读器基础能力。

#### S2-1：内置 PDF 阅读器基础能力（已完成）

- 引入并锁定 `pdfjs-dist 5.7.284`，主解析模块与 Worker 均由 Vite 打包为本地资源；阅读器离线运行，不依赖 CDN。
- 知识库文件树和文件名搜索开始识别 `.pdf`，统一打开到独立 PDF 阅读路由；重命名、选中文件后新建内容等路径判断同步覆盖 PDF，避免把 PDF 当作目录处理。
- 阅读器支持连续滚动、当前页识别、页码输入、前后翻页、50%–250% 缩放、100% 恢复、适合宽度和常用缩放快捷键。
- 左栏提供懒加载缩略图和 PDF 内置书签目录；目录目标解析为实际页码后导航，没有目录的文件显示明确空状态。
- 正文页和缩略图均使用 `IntersectionObserver` 按可见区域及预加载缓冲区渲染，离开缓冲区后释放 Canvas 像素内存，同时保留页面占位尺寸以避免滚动跳变。
- 最近阅读页按“知识库 + PDF 路径”保存在设备状态，最多保留 100 个文档位置；重新打开后恢复并滚动到上次页码，不修改原 PDF。
- 新增受 `WorkspaceGuard` 约束的只读 PDF 命令，只接受当前知识库内的 `.pdf`，拒绝越界路径、其他扩展名和超过 64 MB 的文件；Rust 路径回归测试由 33 项增加到 35 项。
- 当前 64 MB 上限用于控制一次性 IPC 字节传输风险；100 MB 大文件的流式/分段加载、首屏性能和内存基准属于 FR-PDF-007，不在本阶段假装完成。

FR-PDF-001 验收完成。下一步进入 S2-2，实现 FR-PDF-002 文本层、复制、全文搜索、命中计数与定位。

#### S2-2：PDF 文本层、复制与全文搜索（已完成）

- 正文懒渲染页新增 PDF.js `TextLayer`，与页面 Canvas 使用同一 viewport 和缩放比例；文字保持视觉透明但可使用鼠标选择，并通过系统剪贴板原生复制。
- 每页 `getTextContent()` 结果由阅读器共享缓存：当前可见页按需请求，全文搜索复用同一份内容，不重复解析页面文本；切换 PDF 后缓存和未完成任务自动失效。
- 搜索输入使用 220 ms 防抖，优先索引当前页，再逐页渐进构建全文索引；界面在索引期间显示已发现命中数和完成页数，结束后显示精确的“当前/总数”。
- 支持 `Ctrl/Cmd+F` 聚焦搜索、Enter 下一处、Shift+Enter 上一处、循环导航和 Escape 清除；定位命中时自动跳到对应页并滚动到高亮文本。
- 搜索文本按页建立字符区间，命中可跨越多个 PDF 文本片段；页面文本层把同一命中映射为一个或多个高亮区段，当前命中使用更强颜色区分。
- 缩放、页面离开预加载区或组件销毁时会取消旧 TextLayer 并释放 DOM；再次进入时使用缓存文本重建，保持复制与高亮能力而不常驻全部页面 DOM。
- 扫描型 PDF 没有可提取文本时返回 0 个命中，不伪造搜索结果；OCR 能力仍属于 FR-PDF-006。

FR-PDF-002 验收完成。下一步进入 S2-3，实现 FR-PDF-003 sidecar 高亮、区域和评论批注模型。

#### S2-3：PDF sidecar 高亮、区域和评论批注（已完成）

- 新增版本化 `filename.pdf.annotations.json` sidecar 格式，记录源 PDF 文件名、大小、修改时间、PDF.js 指纹和批注数组；批注位置使用与缩放无关的页面归一化坐标，后续可迁移、索引和生成稳定引用。
- 支持三类批注：选择 PDF 文本后创建四色高亮并保留引用原文；在页面拖拽创建矩形区域批注；为当前页创建独立评论。区域模式可由工具栏切换并使用 Escape 退出。
- 阅读器页面渲染高亮、区域和评论标记；侧栏集中列出批注，点击后定位到对应页，支持修改评论、切换颜色和删除，损坏 sidecar 会显示明确错误而不会静默覆盖。
- 保存采用 280 ms 防抖、快照队列和修订号：连续编辑不会被较早完成的异步写入误判为已保存，切换 PDF 时先冲刷当前文档且每个任务固定绑定原路径，避免跨文档串写。
- Rust 命令只接受当前知识库内的 PDF，sidecar 路径由后端基于 PDF 文件名派生；写入复用可靠写入与中断恢复机制，并限制为 5 MB、5,000 条批注、单条 200 个矩形和每字段 20,000 字符。
- 后端在落盘前覆盖可信的文件名、大小和修改时间，同时保留 PDF 指纹；重新打开时若发现指纹变化，以只读方式展示旧批注并警告坐标可能失效，避免把旧位置错误写回新 PDF。
- 原 PDF 全程只读；命令往返测试会比较写入前后 PDF 字节。新增合法/损坏 sidecar fixtures，Rust 回归测试共 39 项通过，覆盖格式校验、重复 ID、越界坐标、缺失高亮原文、损坏数据和原文件不变。

FR-PDF-003 验收完成。下一步进入 S2-4，实现 FR-PDF-004：把 PDF 批注以稳定引用插入 Markdown，并从引用精确返回页码和批注位置。

#### S2-4：PDF 批注引用与 Markdown 精确回跳（已完成）

- 定义可读的普通 Markdown 引用块：摘录或评论作为 blockquote，来源使用 `longedit://pdf?path=<library-relative-path>&page=<page>&annotation=<id>` 链接；链接只保存知识库相对路径，不泄露设备绝对路径，移动整个知识库后仍可使用。
- 引用内容由受 `WorkspaceGuard` 保护的 Rust 命令从现有 sidecar 批注生成；后端验证 PDF 位于当前知识库、批注 ID 确实存在，并对路径、ID 和 Markdown 标签进行编码，前端不能为任意路径伪造可信批注引用。
- PDF 批注编辑区新增“复制引用”和“插入到当前笔记”：创建后尚未落盘的批注会先冲刷可靠保存队列；直接插入时以编辑器内存中的最新笔记内容为基础写盘，避免覆盖尚未触发自动保存的编辑。
- Markdown 编辑器通过事件委托识别批注链接，严格拒绝绝对路径、盘符、空路径段、`.`/`..` 跳转、非法页码和超长批注 ID；合法链接解析为当前知识库内的 PDF 路径，再交由原有后端边界校验读取。
- 回跳时阅读器优先使用链接页码而不是最近阅读位置，sidecar 加载后自动选中批注、打开批注侧栏并滚动到归一化位置；若批注已删除，仍定位到原页并显示失效说明，不静默跳到错误位置。
- 同一 PDF 页面内切换不同引用无需重新加载文档；路由页码或批注 ID 变化会重新定位。选择器使用转义后的批注 ID，避免特殊字符破坏 DOM 查询。
- Rust 回归测试验证生成 URI 不包含知识库绝对路径、Markdown 包含摘录和来源、缺失批注被拒绝；完整回归仍为 39 项。当前链接可随知识库整体迁移，但 PDF 文件在库内被单独重命名后还不会自动改写旧链接，统一对象 ID 与重命名引用修复归入后续对象索引阶段。

FR-PDF-004 验收完成。下一步进入 S2-5，实现 FR-PDF-005：让 PDF 标题、正文文本、批注及 Markdown 对 PDF 的引用进入统一搜索和知识图谱。

#### S2-5：PDF、批注统一搜索与知识图谱（已完成）

- 引入并精确锁定 MIT 许可的 `pdf-extract 0.12.0`，在 Rust 后端按页提取未打开 PDF 的可选文本；搜索不再依赖阅读器会话缓存，命中结果携带真实页码。扫描件、加密或损坏 PDF 提取失败时保留标题搜索并明确提示正文不可提取，不伪造 OCR 内容。
- 新增统一知识搜索命令，覆盖 Markdown、JSON Canvas、PDF 文件名、PDF 正文和 sidecar 批注；结果包含对象类型、命中类型、上下文、页码和批注 ID，并按标题、批注、正文分级排序，最多返回 200 项。
- PDF 文本索引限制单文件 64 MB、最多保留 200 万字符；进程内缓存按 PDF 与批注 sidecar 的大小和纳秒级修改时间自动失效，最多缓存 24 个 PDF。缓存完全可重建，不写回 PDF，也不成为用户数据事实源。
- 知识库搜索界面从简单文件树替换为结构化结果卡片，区分标题、正文、批注和标签命中；点击 PDF 正文结果直接到页，点击批注结果直接选中批注，快速连续搜索使用代际号防止旧请求覆盖新结果。
- 全局知识图谱现在同时创建 Markdown 和 PDF 节点；PDF 使用独立对象类型和琥珀色视觉编码，可被对象类型筛选、全文关键词筛选、双击打开、设为局部脑图中心，并可连同邻居发送到 JSON Canvas。
- Markdown 解析器识别 S2-4 的 `longedit://pdf` 引用，跳过代码围栏和行内代码，拒绝绝对路径及 `.`/`..` 跳转；合法引用建立从 Markdown 到 PDF 的 `annotates` 有向边，保留原始 URI、显示文本、上下文和行号作为可审计关系证据。
- Rust 回归测试由 39 项增加到 41 项：真实双页 PDF fixture 验证正文命中定位到第 2 页，sidecar fixture 验证批注命中定位到批注 ID；图谱测试验证 PDF 节点和 `annotates` 边，解析测试验证非法路径及代码示例不会生成关系。

FR-PDF-005 验收完成。下一步进入 S2-6，实现 FR-PDF-006 OCR 适配接口；本阶段只定义可替换 provider、任务状态和结果 sidecar，不默认引入云端 OCR 或修改原 PDF。

#### S2-6：离线 OCR provider、任务与索引接入（已完成）

- 精确锁定 Apache-2.0 许可的 `tesseract.js 7.0.0`，并随应用打包 MIT 许可的简体中文、英文快速语言模型；PDF 页面由现有 PDF.js 在内存画布中渲染，识别链路不请求云端、不上传内容，也不要求用户安装系统 Tesseract。
- OCR 引擎通过独立 provider 工厂按需动态加载；正常打开 PDF 不加载 Tesseract 代码、WASM 或语言数据。首个 provider 标识为 `tesseract-wasm`，后续可在不改变 sidecar 和索引契约的前提下替换成本地原生引擎或经用户授权的其他 provider。
- 阅读器新增 OCR 工作面，支持重新识别当前页、批量识别尚无结果的页面、模型加载与逐页进度、取消、失败后重试、已识别页摘要和页码跳转。任务按页串行执行以约束峰值内存，最长边限制为 3,000 像素；取消会终止 Worker，但已完成页面不会回滚。
- 识别结果逐页可靠写入版本化 `filename.pdf.ocr.json`，记录源文件指纹、provider、版本、语言、页码、文本、置信度、处理时间和渲染尺寸。sidecar 限制 24 MB、单页 50 万字符、页码不可重复；源指纹变化时显示过期警告，始终不修改原 PDF。
- 统一 PDF 索引缓存签名加入 OCR sidecar 大小和纳秒级修改时间；OCR 文本与原生正文分开保存和检索，搜索结果使用独立 `OCR` 命中类型并携带页码，图谱的 PDF 节点搜索文本同时包含 OCR 内容。即使页面已有原生文本，用户主动生成的 OCR 结果也不会被丢弃或错误标成正文。
- 构建资源只复制 LSTM 所需的三个自适应 WASM core 入口及两个快速语言模型；Tesseract API 代码被拆为按需异步 chunk，PDF 阅读器主 chunk 保持低于 500 KB。当前已知边界：首轮模型初始化耗时取决于设备，印刷体效果优于手写体，暂不生成可选择的空间文字层。
- Rust 回归测试由 41 项增加到 44 项，覆盖 schema 合法性、重复页与置信度拒绝、OCR sidecar 往返且原 PDF 字节不变，以及扫描件 OCR 文本作为独立命中进入统一搜索。

FR-PDF-006 验收完成。下一步进入 S2-7，实现 FR-PDF-007 大文件渐进加载：解除当前 64 MB 整文件读取瓶颈，建立受控的范围读取、首屏优先和内存回收策略，并以 100 MB PDF 的首屏时间作为验收指标。

#### S2-7：大文件 PDF 渐进加载与内存回收（已完成）

- 阅读器不再通过单次 IPC 获取所有 PDF 字节。新增只读描述符与范围读取命令：4 MB 以内保留一次读取的低延迟路径；更大文件首批只读取 256 KB，后续由 PDF.js `PDFDataRangeTransport` 按需请求 256 KB 对齐分块，单次 IPC 强制限制为 1 MB，文档安全上限提升到 2 GB。
- 所有元数据和范围命令继续通过 `WorkspaceGuard` 校验知识库边界、`.pdf` 扩展名与符号链接；描述符签名由文件长度和纳秒级修改时间组成，每次范围读取前后都复核签名，文件在阅读期间变化会立即失败并要求重新打开，避免混合两个版本的字节。
- PDF.js 使用自定义 Tauri range transport，显式关闭流式全量读取和自动预取；范围响应会核验精确长度，切换文件、关闭阅读器或加载失败时中止 transport，已经返回但属于旧会话的数据不会再交给 PDF.js。
- 页面仍由 IntersectionObserver 首屏优先渲染，正文页使用 900 px 预加载窗口、缩略图使用 200 px 窗口；离屏 Canvas 立即缩为 1×1 并取消渲染/TextLayer。正文 `TextContent` 新增 48 页 LRU，上百页全文搜索不再永久保留所有页面结构，当前页与当前命中页受到保护。
- 工具栏显示“快速读取/渐进读取”和真实首个页面 Canvas 完成时间，便于在目标设备持续观测，而不是把 `getDocument()` 完成误当作首屏完成。
- 新增可重复的 `npm run benchmark:pdf-range`：在内存中基于真实双页 PDF 构造合法的 100 MB 增量 PDF，使用与产品一致的 256 KB range、禁用自动预取，取得第一页并提取预期正文。当前开发机多次基准为 50–66 ms、额外请求 255.9 KB/1 次，满足 `< 2 秒` 门槛；基准已加入 `ci:check`。
- Rust 回归测试由 44 项增加到 47 项：覆盖小文件完整读取、100 MB 文件只返回首块且描述符低于 2 秒，以及范围长度精确、1 MB 上限和源文件变化拒绝。大型 PDF 的后台全局正文索引仍保留 64 MB 防护上限；超过该值可在阅读器内按需搜索，但全局搜索只保证标题、已有批注和 OCR sidecar，后续索引阶段再做磁盘型分段索引。

FR-PDF-007 验收完成。PDF 研究工作面本轮需求闭环。下一步进入 S3-1，实现 FR-DATA-001：CSV/TSV 原生查看与编辑，先完成格式嗅探、二维数据模型、虚拟滚动、排序筛选和可靠写回，再复用该模型承载 Excel 与图表工作面。

#### S3-1：CSV/TSV 原生查看与编辑（已完成）

- CSV/TSV 已成为统一知识对象：资料库目录、重命名、拖入导入、路径路由和全局知识搜索都识别两种格式；搜索结果使用独立 `table` 类型，单元格内容可命中并直接返回表格工作面。
- 新增独立数据表工作面，支持表头和单元格直接编辑、增加行列、删除行、全字段筛选、按列升降序、清除排序、冻结首列和类型提示；当前推断整数、数值、布尔、ISO 日期、文本与空列。
- 表头固定，数据行使用 34 px 定高虚拟滚动，只渲染可视区域和上下缓冲；5 万行 fixture 的 Rust 解析基准在当前开发机约 0.13 秒以内完成，首屏 DOM 规模与总行数解耦，满足 `< 2 秒` 门槛。
- 后端精确锁定 `csv 1.4.0`，支持带引号分隔符、引号内换行和不规则行；读取上限 32 MB、20 万数据行、512 列和单元格 100 万字符，短行在内存中补齐但不会伪造额外数据列。
- 读写保留 CSV/TSV 分隔符、检测编码、UTF-8 BOM 和 LF/CRLF 风格。保存复用中断恢复与可靠替换机制，提交前用“长度 + 纳秒修改时间 + 全文件内容指纹”复核版本，同尺寸外部改写也会触发冲突而不会被覆盖。
- 图谱为数据表创建独立青色节点，支持对象类型筛选、关键词筛选、双击打开、设为思维导图中心及发送到 Canvas；表格仍以用户原始 CSV/TSV 为唯一事实源，没有复制进内部数据库。
- Rust 回归测试由 47 项增加到 53 项：新增复杂 CSV、TSV 类型推断、BOM/CRLF 往返、同尺寸冲突、5 万行性能和表格全文搜索测试，并扩展既有图谱测试覆盖表格对象。前端生产构建通过，表格工作面独立按需加载约 8 KB JavaScript（gzip 约 3.7 KB）。

FR-DATA-001 验收完成。下一步进入 S3-2，实现 FR-DATA-002：定义开放的内部 Table 格式，把数据与排序、筛选、列宽、类型等视图配置分离，并提供 CSV 导入导出边界，为看板、图表与后续 XLSX 适配建立稳定数据模型。

#### S3-2：开放 Table 格式与交换边界（已完成）

- 冻结 Open Table 1.0 基础规范，文件扩展名为 `.table.json`、UTF-8 编码。顶层 `data` 保存稳定列 ID、行 ID 和单元格值，`views` 单独保存筛选、排序、冻结列和列宽；完整字段、限制与版本策略见 `docs/Open_Table_Format_Spec.md`。
- 资料库的新建菜单可直接创建空白开放 Table；CSV/TSV 工作面可一键转换为同目录 Table，原文件保持不变，重名目标自动使用递增后缀。
- Table 与 CSV/TSV 共用同一编辑工作面。内部 Table 会持久化全字段筛选、基于列 ID 的排序、冻结首列和 60–600 px 拖动列宽；编辑表头不会改变列 ID，新增或删除数据不会重建其他行 ID。
- 新增 CSV、TSV 和 XLSX 导出。XLSX 使用精确锁定的 `rust_xlsxwriter 0.96.0`（MIT/Apache-2.0），完全离线生成；表头加粗、首行/配置列冻结、列宽映射，数值和布尔列写为 Excel 原生值。导出只创建新文件，不覆盖现有文件。
- Table schema 后端强校验 64 MB、20 万行、512 列、64 视图、稳定 ID 唯一性、类型、引用、列宽和单元格限制；未知 schema、损坏 JSON、悬空列引用及无效 activeView 都会拒绝写入。
- Table 保存延续可靠替换和全内容指纹冲突检测；统一搜索与图谱只抽取列名和数据值，不把 JSON 结构噪声或视图配置当作正文。`.table.json` 可作为图谱节点、思维导图中心和 Canvas 文件节点打开目标。
- 新增格式往返、非法引用、视图持久化、CSV/XLSX 字节、命令边界与路径越界测试；前端生产构建通过，Table 工作面仍独立按需加载。

FR-DATA-002 验收完成。下一步进入 S3-3，实现 FR-DATA-003：让同一 Open Table 数据源切换表格、看板与图表视图，不复制行记录；先完成视图注册、看板分组字段、卡片字段和配置持久化，再进入图表字段映射。

#### S3-3：Table 表格、看板与基础图表多视图（已完成）

- Open Table 1.0 的视图注册扩展为 `grid`、`board` 和 `chart`；每个视图具有稳定 ID、名称、类型和独立配置，`activeView` 记录当前视图。视图数组与唯一的 `data` 同级，schema 测试明确验证序列化结果只出现一次 `rows`。
- 工作面新增视图标签栏，可创建任意数量的表格、看板或图表视图，支持切换、双击重命名和删除；删除视图会明确说明不删除数据，文件至少保留一个视图且最多 64 个。
- 看板支持选择分组列、标题列和最多 8 个卡片字段；分组值为空时进入“未分组”。卡片字段可直接编辑，跨列拖动卡片只修改原行在分组列上的值，行 ID 和其他视图引用保持不变。
- 基础图表视图直接聚合当前数据，支持分类列及计数、求和、平均值；最多显示前 100 个聚合项。它用于证明同一数据源可以切换到图形表达，完整柱/线/饼/散点类型、字段面板和样式编辑仍由 FR-DATA-004 承担。
- 每个视图分别持久化筛选、排序、冻结列、列宽、看板字段和图表聚合映射；切换视图前先捕获当前配置，保存时一次提交完整视图注册表和当前视图，仍使用内容指纹冲突检测及可靠替换。
- 后端拒绝未知视图类型、缺失看板分组列、缺失图表分类列、悬空字段引用、超过 8 个卡片字段及无效聚合方式；旧版只有 grid 的 Open Table 文件通过 serde 默认值保持向前兼容。
- Table 工作面继续独立按需加载；新增看板和基础图表不引入前端图表运行库，也不扩大主入口包。

FR-DATA-003 验收完成。下一步进入 S3-4，实现 FR-DATA-004：建设专业图表编辑器，提供柱、线、饼、散点类型，明确的分类/数值/系列字段映射、聚合与空值策略，并把配置继续保存为引用同一 Table 数据源的 chart 视图。

#### S3-4：专业图表编辑器（已完成）

- Table 图表视图升级为独立编辑器，侧栏提供柱状、折线、饼图和散点四类图表切换，并明确展示分类/X、数值/Y、系列、聚合、空值策略和图例配置。
- 柱状、折线和饼图支持计数、求和、平均值；散点图直接映射数值 X/Y。系列字段统一控制分组或颜色，缺失/非数值记录可选择跳过或按 0 处理。
- 图形使用本地 SVG 渲染，提供坐标轴、刻度、负值基线、分组颜色、图例、数据提示、空状态和大数据截取提示；未引入重量级在线或图表运行库，离线可用且没有扩大主入口包。
- 图表继续消费当前视图筛选后的同一 `data.rows`，不创建派生数据副本。`chartType`、`seriesColumn`、`nullStrategy` 和 `showLegend` 随 chart 视图持久化，并通过稳定列 ID 与源数据关联。
- Open Table 校验新增图表类型、系列字段、空值策略和散点图双轴约束；旧文件缺少新字段时分别按柱状图、跳过空值、显示图例兼容读取。
- 为避免浏览器一次绘制过多 SVG 节点，柱/线最多渲染 60 个聚合项、饼图最多 40 项、散点最多 2,000 点，并在截取时明确提示；完整图表导出和嵌入不属于本阶段。

FR-DATA-004 验收完成。下一步进入 S3-5，实现 FR-DATA-005：让 Markdown 与 Canvas 嵌入可编辑的 Table 图表引用，源数据或视图配置变化后自动刷新，且不把截图作为唯一事实源。

#### S3-5：Markdown 与 Canvas 实时图表引用（已完成）

- 建立 `Table Chart Reference 1.0`：引用只保存 `.table.json` 源路径和稳定 chart 视图 ID，不保存数据副本、聚合快照、SVG 或位图。
- Markdown 使用可读的 `longedit-chart` fenced JSON；编辑器工具栏在插入前读取 Table 并验证可选 chart 视图。文档内引用集中显示在可折叠实时图表工作区，无效代码块会局部提示且不会改写正文。
- Canvas 继续使用标准 JSON Canvas `file` 节点，通过可忽略扩展字段 `longeditViewId` 指定 chart 视图。图表节点可移动、缩放、复制、连接和分组，其他软件仍能把它识别为普通文件节点。
- Markdown 与 Canvas 共用 `TableChartEmbed` 和 S3-4 的 SVG 渲染器，读取源视图的筛选、排序、字段映射和图例配置；支持手动刷新、窗口重新获得焦点刷新、返回页面重新加载及 Table 保存事件刷新。
- 两种载体都提供“编辑源图表”，直接跳转 Table 工作面。源文件缺失、越界或视图删除只影响当前引用卡片，宿主文档仍可打开编辑。
- Canvas schema 校验拒绝非 file 节点、非 `.table.json` 源和非法 `longeditViewId`；路径读取继续通过 `WorkspaceGuard`。引用规范见 `Table_Chart_Reference_Spec.md`。

FR-DATA-005 验收完成。下一步进入 S3-6，实现 FR-DATA-006：增加 XLSX 导入、工作簿预览、多 Sheet 与常用公式结果读取，同时保留“基础兼容”边界，不宣称完整复刻 Excel。

#### S3-6：XLSX 基础工作簿兼容（基线已完成）

- 知识库新增 `.xlsx` 对象入口和独立工作簿路由，支持多 Sheet 标签、实际行列统计、公式/结果切换、刷新和选中 Sheet 转换为 Table。
- 后端使用精确锁定的纯 Rust `calamine 0.36.0`。工作簿元数据、分页工作表读取和 Sheet 导入均通过独立命令执行在线程池；文件必须位于知识库且不超过 128 MB。
- 工作表按需返回最多 5,000 行，前端以 2,000 行为一页缓存并使用二维滚动与行虚拟化；预览最多返回 256 列，超限时明确提示，避免一次跨 IPC 传输整个大工作簿。
- 公式单元格同时保留公式文本与文件内缓存结果，用户可切换查看。LongEdit 不重新计算公式；缓存缺失时不伪造结果。
- “转换当前 Sheet 为 Table”使用第一行作为字段名，读取缓存结果并创建新的 `.table.json`；原 XLSX 和已有文件不覆盖，转换后进入完整 Table 编辑/看板/图表闭环。
- 自动化 fixture 验证多 Sheet、数值、公式文本、公式缓存结果与选中 Sheet 转 Table。兼容边界见 `XLSX_Compatibility_Boundary.md`。
- 当前未还原样式、合并单元格、图片、Excel 图表和条件格式，也不直接编辑或回写 XLSX，因此 FR-DATA-006 保持“部分完成”。

S3-6 基线验收完成。下一步进入 S3-7 / FR-DATA-007：对 Univer 等完整工作簿方案做许可、离线、包体、性能和 XLSX 往返失真验证，再决定补强自有工作面还是引入完整引擎。

#### S3-7：完整工作簿引擎技术闸门（已完成）

- 完成 Univer OSS/Pro、Handsontable、Luckysheet 与当前原生方案的许可、离线、能力和维护性审计；正式报告见 `Workbook_Engine_Evaluation.md`。
- 最小 Univer Sheets Core preset 试验在未加入 Pro 能力时已产生 223 个安装包、198 MiB `node_modules`、约 5.85 MB minified / 1.65 MB gzip 主 JS，并引入 React/Radix 运行时。
- 官方 XLSX 导入导出、Charts、Pivot、打印和协作位于 Univer Pro 边界；导入导出依赖服务端能力，未授权评估存在水印和额度限制。
- 技术决策为“不进入默认构建”：继续使用原生 XLSX 只读预览和 Sheet 转 Open Table，不为获得网格编辑重复替换现有 Table 工作面。
- 保留可选 `WorkbookEngine` 适配边界；只有目标客户、商业许可、部署模型、真实 XLSX 往返 fixture 和独立包体预算全部明确后，才允许启动路由级隔离的 Univer Pro POC。
- 本步骤未向产品依赖加入 Univer、Handsontable 或 Luckysheet，不改变普通本地文件作为事实源的原则。

FR-DATA-007 验收完成。下一步进入 S3-8 / FR-DATA-008：基于开放 Table 建设多图表仪表盘、共享筛选和布局持久化，增强数据工作面的专业管理能力。

#### S3-8：共享筛选仪表盘（已完成）

- Open Table 新增 `dashboard` 视图，可引用同一文件中最多 24 个既有 chart 视图；只保存图表视图 ID，不复制行数据或图表字段配置。
- 仪表盘顶部全字段筛选生成统一的行索引，柱状、折线、饼图和散点卡片同时消费该结果；筛选条件随仪表盘视图独立持久化。
- 卡片使用 12 栅格布局，支持拖动调整顺序，并在 4、6、8、12 四种宽度间切换；数组顺序和宽度写入 `dashboardItems`。
- 可从仪表盘添加/移除图表并跳回源图表编辑。删除 chart 视图时自动清理所有仪表盘引用，避免保存悬空关系。
- Rust schema 限制卡片数量、宽度、唯一性和引用类型，拒绝不存在或非 chart 的目标；往返测试覆盖共享筛选、图表引用和布局保存。
- 聚合继续复用 chart 的分类、系列、计数、求和和平均能力，相当于面向可视化的数据透视；不生成隐藏的聚合数据副本。

FR-DATA-008 验收完成，Milestone 3“结构化数据与图表”形成闭环。下一步进入 S4-1 / FR-DIAGRAM-001：建设 Mermaid 图表工作室，提供源码、实时预览、错误定位和模板选择，为软件内专业流程图编辑建立入口。

#### S4-1：Mermaid 图表工作室（已完成）

- 新增独立 Diagram 路由和 `.mmd` / `.mermaid` 文件入口；知识库支持创建、扫描、搜索、重命名和打开，不把图表源码伪装成 Markdown 笔记。
- 工作室提供带行号源码编辑器和实时 SVG 预览；输入停止 350 ms 后解析，错误显示摘要和可用行号，点击错误可选中对应源码行。
- 内置流程图、时序图、思维导图、类图、ER 图和甘特图六类模板，以及默认、中性、森林和深色主题、50%–200% 缩放。
- 精确锁定 Mermaid `11.16.0`，通过独立路由动态导入，图类型继续按需加载；使用 `securityLevel: strict`，禁用图内点击和未受信任 HTML。
- Rust 新增独立 diagram 格式和命令模块。文件限制为知识库内、UTF-8、非空、无 NUL、最大 2 MB；保存复用可靠写入，并通过内容签名拒绝静默覆盖外部修改。
- 六类内置模板均通过真实浏览器解析和 SVG 渲染验证；后端测试覆盖创建、读取、写入、陈旧签名、越界路径和格式限制。边界见 `Mermaid_Diagram_Workspace.md`。

FR-DIAGRAM-001 验收完成。下一步进入 S4-2 / FR-DIAGRAM-002：为常用流程图建立节点/连线属性表单与可读 Mermaid 源码双向同步，保留无法结构化编辑的高级语法。

#### S4-2：流程图结构表单与源码双向同步（已完成）

- 图表工作室新增可开关的结构面板；解析常用 `flowchart` / `graph` 节点、六类形状、三类连线及标签，并在源码每次变化后刷新表单。
- 节点标签/形状与连线标签可通过表单修改。后端只替换被编辑元素的精确字符区间，不重新序列化整图，因此 frontmatter、注释、`classDef`、主题指令和未知高级语法保持原样。
- 不可安全结构化的图类型明确降级为源码模式；隐式节点只读，一行多段链式连线提示仅编辑第一段，避免界面能力与实际语法支持不一致。
- 后端继续执行 2 MB、UTF-8、空内容和 NUL 校验；新增结构分析与更新命令，并补充高级语法保留、frontmatter 识别及无标签连线插入测试。
- 桌面与窄屏均保留“结构”入口；结构修改沿用现有脏状态、实时预览、冲突检测和可靠保存链路。

FR-DIAGRAM-002 验收完成。下一步进入 S4-3 / FR-DIAGRAM-003：提供安全、可复现的 SVG/PNG 导出，保留矢量文本并支持 PNG 倍率选择。

#### S4-3：Mermaid SVG/PNG 专业导出（已完成）

- 图表工作室新增导出面板，支持 SVG 矢量图和 1×、2×、3× PNG，背景可选透明、当前主题或白色；目标路径由系统“另存为”对话框明确授权。
- SVG 使用 Mermaid 当前有效渲染结果，规范化 `viewBox` 与数值宽高并保留矢量文字；PNG 从同一份规范化 SVG 栅格化，确保主题和布局一致。
- 导出状态绑定已成功渲染的源码与主题。源码、主题或预览仍在变化时禁止导出，避免防抖窗口内写出旧图。
- 在 Mermaid 严格安全模式之上增加导出清理层：移除脚本、事件属性、外部链接、外部 CSS URL、图片、嵌入对象及 `foreignObject`，不允许导出文件继续访问外部资源。
- SVG 尺寸上限为 16384 px；PNG 最大边 8192 px、最多 3200 万像素，超限时明确提示而不是静默降低用户选择的倍率。
- 导出复用已有 Tauri dialog/fs capability，不增加任意路径写入 IPC 命令；Diagram 路由仍保持按需加载。

FR-DIAGRAM-003 验收完成。下一步进入 S4-4 / FR-DIAGRAM-004：让 Mermaid 文件以可编辑源码引用进入 Canvas，并保持源文件为唯一事实源。

#### S4-4：Canvas Mermaid 源文件引用节点（已完成）

- Canvas 工具栏新增 Mermaid 文件节点入口，添加前通过知识库受控读取验证 `.mmd` / `.mermaid` 源文件；默认节点尺寸适合流程图、思维导图和时序图预览。
- 节点使用标准 JSON Canvas `type: "file"` 与 `file` 路径，不保存 SVG 缓存、不复制 Mermaid 源码，也不添加私有图表类型字段，确保其他 Canvas 工具仍可识别为普通文件引用。
- 可见节点按需读取并以 Mermaid 严格安全模式渲染；复用 20 万字符预览上限，视口外节点由现有画布虚拟化卸载，避免所有图表常驻 WebView。
- 双击节点或点击“编辑源图表”进入同一源文件的图表工作室；源文件保存、窗口重新聚焦和手动刷新均可更新节点预览，源文件始终是唯一事实源。
- Mermaid 文件节点参与现有拖拽、缩放、连线、分支整理、撤销重做、跨画布复制粘贴和相对路径重定位，不建立独立交互孤岛。
- Canvas 格式回归测试验证 Mermaid 引用仍是合法开放 JSON Canvas；前端生产构建验证组件、路由和按需 Mermaid 渲染链路。

FR-DIAGRAM-004 验收完成。Milestone 3 的 Mermaid 图表闭环已经形成：源码编辑、结构表单、专业导出与 Canvas 组合使用均可用。下一步进入需求路线图中尚未完成的高优先级能力审计与排序。

#### S5-1：系统凭据存储与明文 Key 迁移（已完成）

- 高优先级复审将 FR-BASE-003 排在下一位：它是现存 P0 数据安全缺口，并直接阻塞 AI 能力进入专业工作台。
- 精确锁定 keyring `3.6.3`，显式启用 Windows Credential Manager、macOS Keychain 和 Linux Secret Service；所有凭据访问在阻塞任务中执行。
- 设置页只维护一次性输入草稿与“已配置”状态，不回显密钥；Vue/Pinia 和 AI IPC 不再持有或传递已保存 Key，Rust 请求端在发送前读取系统凭据。
- 旧 `config.json` 的 `aiApiKey` 支持一次性读取迁移，并立即通过可靠写入清除；`skip_serializing` 回归测试确保后续配置和 IPC 永远不输出兼容字段或明文。
- 远程地址缺少 Key 时明确拒绝；本机回环地址允许无 Key，保留 Ollama 等本地服务能力。安全边界见 `Credential_Storage_Security.md`。

FR-BASE-003 验收完成。下一步优先处理 FR-BASE-004：继续拆分仍有 2,000 余行的 Rust `lib.rs`，把配置、AI、文件和 Git 命令迁入独立模块，降低后续统一对象与索引改造风险。

#### S5-2：Rust 单体拆分第一批（已完成）

- 新增 `commands/config.rs`，集中 App/Library 配置模型、可靠读写、旧明文凭据迁移和系统凭据 IPC；配置防泄漏测试随业务迁入模块。
- 新增 `commands/ai.rs`，集中兼容 OpenAI 的聊天请求与凭据读取；将回环地址判定提取为独立测试，并修复 `[::1]` IPv6 回环地址被误判为远程服务的问题。
- 新增 `commands/git.rs`，集中仓库状态、初始化、提交、拉取和推送命令；前端 IPC 名称及参数保持兼容。
- `lib.rs` 从 2,257 行降至 1,889 行，新增模块均通过编译，Rust 回归测试增加至 73 项。

FR-BASE-004 已进入“部分完成”。下一批拆分文件系统、历史版本与搜索命令；完成标准仍是 `lib.rs` 仅保留 Tauri 应用装配和跨模块公共类型，不因行数下降提前标记完成。

#### S5-3：历史版本、搜索与数据迁移模块化（已完成）

- 新增 `commands/history.rs`，集中历史版本保存、去重、裁剪、列出、删除、清空和影子副本；换行符归一化规则拥有独立回归测试。
- 新增 `commands/search.rs`，集中单库搜索、跨库搜索、标签统计和标签检索；可索引格式白名单变为独立函数并覆盖大小写及伪扩展名测试。
- 新增 `services/data_migration.rs`，把应用标识符和产品目录迁移从 Tauri 装配层移入服务层，并统一目录迁移过程。
- `lib.rs` 从上一批的 1,889 行继续降至 1,527 行；命令 IPC 名称、参数和返回结构保持兼容。

FR-BASE-004 继续保持“部分完成”。下一批迁移文件创建、读取、移动、删除、导入、目录排序及文件统计命令；随后再拆图谱构建遗留逻辑与平台集成代码。

#### S5-4：文件系统命令模块化与附件边界修复（已完成）

- 新增 `commands/files.rs`，集中 Markdown 创建与可靠读写、文件夹创建、重命名、单项/批量移动删除、目录扫描与排序、图片读取保存、文档导入、HTML 导出及文件统计。
- 文件模型 `FileContent`、`FileEntry`、`FolderOrder` 和 `FileStats` 随业务迁入文件模块；根模块只保留兼容现有 Canvas、Search 等模块的最小重导出。
- Markdown 与 HTML 图片引用提取统一去重；删除 Markdown 时只清理文档目录边界内的本地附件。
- 修复导入 Markdown 附件时 `../` 引用可能把资源写到目标知识库目录之外的问题：源附件和目标附件现在都经过 `WorkspaceGuard`。
- 新增文件名净化、父级穿越拒绝、伪扩展名拒绝、附件引用去重及导入目录越界测试；Rust 回归测试增加至 79 项。
- `lib.rs` 从上一批的 1,527 行继续降至 1,070 行，文件 IPC 名称和返回模型保持兼容。

FR-BASE-004 仍为“部分完成”。下一批迁移遗留的图谱构建、反向链接和知识库统计逻辑；之后将平台关联、托盘、URI 协议等应用装配边界收口。

#### S5-5：图谱查询边界收口（已完成）

- WikiLink 提取、反向链接检索和知识库 Markdown 统计迁入 `commands/graph.rs`，与图谱健康检查和链接修复共享统一模块边界。
- 反向链接入口现在通过 `WorkspaceGuard` 校验知识库根目录和目标 Markdown，拒绝知识库外目标。
- 反向链接与统计递归扫描主动跳过符号链接、隐藏目录和资源目录，避免沿链接读取知识库之外的文件。
- 新增路径/扩展名/大小写反向链接匹配，以及隐藏目录统计隔离测试；Rust 回归测试增加至 81 项。
- `lib.rs` 从上一批的 1,070 行继续降至 954 行。

FR-BASE-004 继续保持“部分完成”。下一批把全局/局部图谱构建器和相关模型完整迁入图谱模块，再收口系统关联、URL 标题、托盘与 URI 协议等平台代码。

#### S5-6：图谱构建与平台边界收口（已完成）

- 全局/局部图谱命令、图谱模型、关系合并与深度过滤完整迁入 `commands/graph.rs`，Markdown、PDF、CSV/TSV 和内部表格的节点适配器由图谱模块统一管理。
- 图谱构建测试随业务迁入图谱模块，覆盖局部深度、未知中心节点、重复关系证据合并、Frontmatter 双向关系、多格式元数据和 PDF 标注关系；合并冗余用例后 Rust 回归测试稳定为 79 项。
- 新增 `commands/system.rs`，收口默认文件关联、关联状态检测、URL 标题读取和应用退出命令；原有 IPC 名称与调用方式保持兼容。
- Canvas 等消费者继续通过最小公共类型导出使用图谱数据，不依赖图谱模块内部实现。
- 按项目既有代码行统计口径，`lib.rs` 从上一批的 954 行降至 314 行，相比拆分前的 2,257 行减少约 86%，当前仅保留 Tauri 应用装配、托盘/窗口事件、URI 协议注册、命令注册和必要的跨模块公共导出。

FR-BASE-004 验收完成。下一阶段回到基础安全闭环，优先审计并完成 FR-BASE-001 的 WorkspaceGuard 全命令覆盖，再进入知识图谱交互与可视化能力增强，避免新能力建立在不一致的文件访问边界上。

### S6-1：外部文件授权与统一格式注册（进行中）

- 知识库 Markdown 读写现在强制携带 `libraryRoot` 并经过 `WorkspaceGuard`；临时编辑改用独立外部读写命令。
- 外部 Markdown 只有通过应用启动参数、单实例打开事件或 Rust 后端原生文件选择器后才进入授权集合，前端直接提交任意路径会被拒绝。
- Markdown 与 HTML 导出分别使用后端“另存为”、知识库守卫或外部授权路径，避免恢复安全边界时破坏既有导出流程。
- 新增前端统一格式注册表，集中声明 Markdown、Canvas、PDF、Table、Workbook 和 Mermaid 的扩展名、路由与外部编辑能力；知识库打开路由和重命名扩展名识别已开始消费该注册表。
- Rust 回归测试增至 83 项，新增外部文件未授权、错误格式和缺失文件拒绝测试；前端生产构建与严格 Clippy 通过。

FR-BASE-007 和 FR-INDEX-004 保持“部分完成”。下一批继续收口导入、图片和历史等剩余文件能力，并把格式创建、索引适配与编辑能力声明迁入注册机制；随后进入 FR-DATA-009 工作簿内核接口与 XLSX fixture 基线。

### S6-2：文件能力收口与工作簿内核基线（已完成）

- 系统拖拽事件在 Rust 层登记可导入文件，`import_to_library` 拒绝前端直接提交的未授权路径，并限制为已注册工作区格式。
- 历史版本命令以 `WorkspaceGuard` 规范路径或已授权外部 Markdown 作为缓存键；外部临时历史使用独立命令。
- 移除可读取任意本地图片的自定义 URI 协议和未使用的任意路径图片写入命令；图片读取要求安全格式、50 MB 上限且被当前 Markdown 精确引用。
- 新增 `WorkbookEngine`、稳定工作簿数据模型和机器可读能力矩阵；当前 `calamine-preview-v1` 明确只声明读取与公式缓存结果能力。
- 新增可重复生成的 XLSX 兼容 fixture 和 JSON 预期，覆盖多 Sheet、基础类型、公式缓存、样式、数字格式、合并单元格和列宽；Rust 回归测试增至 86 项。

FR-BASE-007 的主要文件入口已闭环，FR-FORMAT-001 与 FR-DATA-009 保持“部分完成”。下一开发入口是可编辑工作簿文档/单元格/样式模型、工作副本、冲突检测和第一批可靠 XLSX 写回。

### S6-3：已有单元格编辑与保真 XLSX 写回（已完成）

- 新增 `workbook_ooxml` 局部补丁层，直接修改目标 Sheet 的既有单元格 XML；ZIP 包内未编辑的样式、图表、关系和未知部件保持原始字节，不通过重建工作簿静默丢失功能。
- 工作簿签名加入文件内容摘要；保存要求携带读取时签名，外部程序修改后旧编辑会话被拒绝。最终写入复用同目录临时文件、恢复备份和原子替换机制。
- 首批支持已有单元格的文本、数字、布尔、清空和公式写回；编辑请求限制为 10,000 个单元格，并校验 Sheet、坐标、文本和公式长度。该阶段尚不存在的空白单元格会被明确拒绝，已由 S6-4 补齐。
- 工作簿工作面新增单元格选择、公式栏、双击编辑、未保存标记、撤销/重做、`Ctrl+S` 保存、刷新/离开保护；虚拟分页读取保持不变。
- 能力矩阵新增“已有单元格编辑”“冲突检测”“OOXML 未编辑部件保真”并标记为 `supported`；完整单元格编辑、样式、公式重算和完整 XLSX 往返继续为 `planned`。
- Rust 回归测试增至 88 项，覆盖文本/数字/公式写回、未编辑 `styles.xml` 字节保真、过期签名拒绝和不存在单元格拒绝；前端生产构建通过。

FR-DATA-009 继续保持“部分完成”。下一开发入口是空白单元格与区域粘贴、选择模型扩展和基础样式 ID/格式编辑；随后接入公式依赖图与重算能力。

### S6-4：空白单元格、区域选择与剪贴板（已完成）

- `workbook_ooxml` 支持在已有行中按列坐标插入新单元格，也支持按行号创建全新行；插入保持 OOXML 行列有序并同步扩展工作表 `dimension` 的起点和终点。
- 工作簿网格在实际数据之外提供额外空白行列；空白单元格可通过公式栏直接创建，保存后重新读取仍位于原坐标。
- 选择模型支持拖拽、Shift 扩展、方向键与 Shift + 方向键；地址栏显示单元格或连续区域范围。
- 支持 TSV 区域复制、剪切、粘贴和 Delete 清空；单次区域最多 10,000 个单元格，批量粘贴或清空作为一个撤销/重做步骤。
- 机器能力矩阵将基础单元格编辑、空白单元格创建、区域编辑和 TSV 剪贴板标记为 `supported`；日期/错误单元格改写、样式和公式重算仍未开放。
- 合并区域仅允许编辑左上角锚点，命中其他成员单元格时整批拒绝，避免生成与合并语义冲突的 OOXML。
- XLSX 回归覆盖同一行插入、公式单元格前插入、全新行创建、`dimension` 扩展、合并区域保护和保存后分页重读；Rust 回归总数保持 88 项，前端生产构建和严格 Clippy 通过。

FR-DATA-009 继续保持“部分完成”。下一开发入口是基础样式读取与样式 ID 模型，以及数字格式、字体、填充、边框和对齐编辑。

### S6-5：基础样式读取、渲染与区域编辑（已完成）

- 新增稳定 `WorkbookCellStyle` 与增量 `WorkbookStylePatch` 契约，前端不直接依赖 OOXML 的 `fontId`、`fillId`、`borderId` 或 `xfId`；引擎标识升级为 `calamine-ooxml-v4`。
- 分页读取同步解析 `styles.xml` 和目标工作表样式 ID，恢复字体、字号、粗体、斜体、下划线、RGB 文字色、RGB 填充色、基础边框、水平对齐、换行及常用数字格式；工作表范围与样式合并为单次受限解包。
- 工作簿新增紧凑格式工具栏，支持连续区域字体/字号、粗斜体、下划线、文字色、填充色、所有边框、水平对齐、换行和常用数字格式编辑；网格实时渲染，区域格式作为一次撤销/重做操作。
- 样式写回以源样式为基线，复用已有组件并只向 `fonts`、`fills`、`borders` 和 `cellXfs` 追加去重记录；样式单独修改不会重写单元格值或公式，空白单元格也可仅创建样式节点。
- 后端限制每批 10,000 个样式单元格和 256 种新样式，验证坐标、字号、字体名、颜色、边框、数字格式与对齐；合并区域保护、内容签名冲突检测、ZIP 解压限额和可靠原子写入继续生效。
- XLSX 回归覆盖提交 fixture 的标题/货币样式读取，以及数字、公式、空白单元格样式写回和公式 XML 保留；Rust 回归增至 89 项，工作簿专项 6 项和前端生产构建通过。

基础 `formatting` 能力升级为 `supported`，FR-DATA-009 继续保持“部分完成”。当前不把主题色/索引色、渐变填充、混合边框、自定义格式编辑、行高列宽或保护属性计入完整样式等价；下一开发入口是整行整列、多区域选择、填充柄和公式相对引用迁移。

### S6-6：工作区选择模型、填充柄与公式引用迁移（已完成）

- 选择模型升级为焦点单元格与区域集合分离，支持当前工作区整行/整列/全选、Shift 扩展和最多 32 个 Ctrl/Cmd 多区域；格式化与清空对重叠坐标去重并保持单次撤销语义。
- 网格增加稳定填充柄和拖动预览，支持单值/二维模式复制、一维数字序列外推及基础样式复制，单次仍受 10,000 单元格门禁保护。
- 新增受限公式迁移命令，以 `a1 1.0.2` 解析 A1 引用并显式处理相对、绝对和混合引用；支持范围与 Sheet 前缀，跳过字符串和函数名，越界输出 `#REF!`，不冒充公式计算引擎。
- `WorkbookCapabilities` 增加 `row_column_selection`、`multi_area_selection`、`fill_handle` 和 `formula_reference_translation`，均升级为 `supported`；引擎标识升级为 `calamine-ooxml-v5`。
- Rust 新增 5 项公式迁移回归，总测试增至 94 项；Vue 类型检查、完整生产构建和桌面端构建纳入本阶段门禁。

FR-DATA-009 继续保持“部分完成”。下一开发入口是公式依赖图与重算引擎；行高列宽、合并区域、命名样式和高级样式继续按兼容矩阵推进，新格式编辑器与主题预设保持并行排期。

### S6-7：公式依赖求值与按需重算（已完成）

- 精确锁定 `ironcalc 0.7.1` 作为计算适配器，保留 Calamine 读取和现有 OOXML 可靠写回链路；引擎标识升级为 `calamine-ooxml-ironcalc-v6`。
- 新增受签名保护的重算命令：从内存 XLSX 构建临时模型，应用最多 10,000 个当前草稿，递归计算同 Sheet 与跨 Sheet 依赖，并最多返回 10,000 个目标公式结果。
- 工作簿工具栏增加显式“重算”，结果和错误仅驻留当前会话；内容再次变化时立即失效，保存不会把非完全等价结果静默写入 XLSX 缓存。
- 循环引用与不支持公式返回逐单元格诊断；外部链接不触发网络或额外文件访问。易失函数、动态数组、命名区域和全部 Excel 函数继续保留在兼容矩阵中。
- `formula_dependency_graph` 与 `formula_recalculation` 能力升级为 `supported`；新增 3 项依赖传播、跨 Sheet、循环引用与请求验证测试，Rust 回归增至 97 项。

FR-DATA-009 继续保持“部分完成”。下一开发入口是 S6-8 行高、列宽和合并区域；命名样式独立进入 S6-9，公式 fixture 和后续兼容矩阵继续分批推进。

### S6-8：工作表结构编辑（已完成）

- `WorkbookEngine` 稳定模型增加默认/自定义行高、列宽和合并区域，能力矩阵中的 `row_dimensions`、`column_dimensions`、`merged_cells` 均升级为 `supported`；引擎标识升级为 `calamine-ooxml-ironcalc-v7`。
- OOXML 写回支持行节点 `ht/customHeight`、`<cols>` 区间拆分与自定义宽度，以及 `<mergeCells>` 有序重建；重叠列宽请求、重叠合并范围、无对应区域的取消合并和超限尺寸整批拒绝。
- 合并前扫描非锚点内容，存在文本、数值或公式时拒绝写盘，避免静默数据丢失；结构写回先于单元格补丁，使同批取消合并后编辑成员单元格仍经过最终结构校验。
- 工作簿前端采用稀疏行高偏移和最多 256 列的可变宽度网格，合并区域以锚点覆盖渲染；行高、列宽、合并/取消合并进入统一撤销重做、未保存提示、签名冲突和可靠写入链路。
- 兼容 fixture 增加 `customHeight`，并对真实列宽、合并区域和能力声明做语义断言；新增结构往返与非锚点数据保护回归，Rust 测试增至 98 项。

FR-DATA-009 继续保持“部分完成”。按照阶段审计的交错排期，下一开发入口切换到 T7-1 主题预设契约；随后执行 F7-1 格式扩展契约，再回到 S6-9 命名样式与高级样式。

### T7-1：主题预设契约（已完成）

- `themePresets.ts` 升级为唯一主题注册表，集中声明主题身份、模式、核心界面色、编辑器背景、预览色、图表调色板、视觉风格与 Vditor 配置；旧配置字段保持兼容，未知值安全回退到跟随系统。
- 新增专业浅色、专业深色和高对比三套首要预设；高对比模式提供独立表面、边界、焦点和数据系列 token。
- 删除 `App.vue`、设置页和命令面板中的重复颜色/主题清单；主题命令通过 Pinia 单入口原子更新并持久化。Library/Vditor、Graph、Canvas 和 Table Chart 统一消费模式或调色板契约，其余工作面继续消费语义 CSS token。
- 新增 `npm run check:theme-contract` 并接入 `ci:check`，防止重复清单和固定图表色回归；桌面端工作面、三种窗口宽度和三套专业预设的视觉矩阵记录在 `Theme_Preset_Contract_and_Visual_Matrix.md`。

FR-THEME-001 升级为“已完成”。普通浏览器因缺少 Tauri IPC 不能作为桌面截图证据，发布验收必须在真实 Tauri WebView 中执行视觉矩阵。下一开发入口为 F7-1 格式扩展契约，随后进入 S6-9 命名样式。

2026-07-20 阶段审计确定的后续顺序为：T7-1 主题预设契约，F7-1 格式扩展契约，S6-9 命名样式，F7-2 第一种新增格式编辑器。完整范围、退出条件和后续 Excel 兼容批次见 `Development_Stage_Audit_2026-07-20.md`。

### F7-1：格式扩展契约（已完成）

- 新增 `shared/file-formats.json` 作为前后端唯一格式能力事实源，集中声明扩展名、MIME、读取/编辑/创建/索引能力、路由、大小限制、外部授权和适配器。
- Library 创建、读取、写入与打开路由，以及 Rust 工作区扫描、外部授权和索引分派均改为消费注册表；复合扩展名使用最长匹配并由后端再次校验格式与路径。
- 新增通用文本读取、写入、模板创建命令和 `.txt` 真实 fixture，证明增加同类文本格式不需要继续修改根页面或根 Rust 白名单。
- 新增 `npm run check:format-contract` 并接入 `ci:check`，防止能力声明与适配器不一致、重复扩展名和根级手工分支回归。
- 候选评分确定 F7-2 首选 OPML 大纲/思维导图；完整扩展清单、降级规则和边界见 `File_Format_Extension_Contract.md`。

FR-INDEX-004 与 FR-FORMAT-001 升级为“已完成”。格式契约只证明扩展框架完成，不代表所有声明为 `planned` 的能力已经交付；下一开发入口为 S6-9。

### S6-9：命名样式与高级样式（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v8`，读取 `cellStyleXfs/cellStyles` 并以稳定名称列出、应用命名样式，不向前端泄漏内部 `xfId`。
- 解析主题色、tint 和 Excel 64 色索引调色板；单元格四条边框独立读取、渲染和写回，保留旧“所有边框”快捷入口。
- 自定义数字格式使用 `custom:` 契约，后端验证长度与控制字符并在 `numFmts` 中分配、复用自定义 ID。
- 条件格式继续作为只读保真对象；往返测试验证样式写回不改变 `conditionalFormatting` 及其后的工作表结构。
- 工作簿工具栏新增命名样式、分边框和自定义数字格式入口；能力矩阵新增五项 `supported` 声明，Rust 回归增加到 104 项。

FR-DATA-009 继续保持“部分完成”。渐变填充、条件格式规则编辑、主题字体、垂直对齐、旋转、保护和完整 Excel 等价仍未完成；下一开发入口切换到 F7-2 OPML，之后进入 S6-10。

### F7-2：OPML 大纲/思维导图编辑器（已完成）

- 共享格式注册表新增 `.opml`，创建、扫描、打开、外部导入、语义索引和搜索结果路由进入统一能力契约。
- 新增 OPML 2.0 安全流式解析与规范化写回，保留标题、简单 head 元数据、树结构、备注、折叠状态和受支持的未知 outline 属性；内容签名冲突时拒绝覆盖。
- 新增“思维导图/树形大纲”双视图，支持根/子/同级主题创建、缩进、减少缩进、拖拽改变父子层级、删除、折叠、搜索、备注、100 步撤销重做和自动保存。
- OPML 可投影为标准 JSON Canvas：文件节点作为入口，主题成为 text 节点，父子层级成为 `contains` 边；投影创建新文件，不建立隐藏双向同步。
- 单文件限制 8 MB、10,000 节点和 64 层；真实 fixture 与 Rust 回归覆盖语义往返、陈旧写入拒绝、Canvas 校验和无 XML 噪声索引。完整边界见 `OPML_Mind_Map_Editor_Design.md`。

FR-MINDMAP-001～003 升级为“已完成”。下一开发入口回到 S6-10 命名区域、日期/错误 fixture 与公开兼容矩阵。

### S6-10：命名区域、日期/错误 fixture 与公开兼容矩阵（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v9`，直接解析 `workbook.xml` 中的工作簿级与 Sheet 级命名区域，保留名称、公式、作用域和隐藏标记；安全单一 A1 引用生成稳定导航坐标。
- 工作簿名称框支持跨 Sheet 跳转、分页加载、完整区域选择与滚动定位；常量名称、多区域和外部引用不会被误报成可导航区域。名称创建、修改和删除仍明确为 `planned`。
- IronCalc 回归新增命名区域公式重算，验证未保存草稿能通过 `SUM(AmountRange)` 传播结果；外部引用继续禁止网络和隐式文件访问。
- 提交的真实 fixture 新增日期、日期时间、`#DIV/0!` 错误值、全局/局部命名区域、常量名称和命名公式缓存；普通单元格写回后再次读取仍保持日期、错误与名称语义。
- 新增 `shared/xlsx-compatibility-matrix.json`、公开文档 `XLSX_Public_Compatibility_Matrix.md` 和 `npm run check:workbook-contract`，按读取、显示、编辑、计算、往返五个维度防止能力夸大。

FR-DATA-009 继续保持“部分完成”。下一开发入口为 S6-11 冻结窗格、排序筛选、Excel Table 与数据验证。

### S6-11：冻结窗格、排序筛选、Excel Table 与数据验证（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v10`，稳定页面模型增加冻结窗格、自动筛选、Excel Table 和数据验证结构；Table 通过 Sheet 关系安全解析，不跟随 `xl/` 之外的目标。
- 冻结窗格支持读取、按当前单元格写回与取消冻结；独立结构命令复用工作区守卫、内容签名、可靠替换和写后校验，存在未保存内容草稿时前端禁止执行。
- Excel Table 范围、列、汇总行和样式名称进入工作面视觉提示；自动筛选/Table 数据区提供最多 50,000 行的非破坏性会话筛选与升降序，不改写源行顺序或持久化筛选条件。
- 数据验证显示规则与输入提示，并在内容补丁前执行字面量列表、整数/小数比较和文本长度约束；公式、命名范围与自定义规则不在本地伪计算，继续原样保留。
- 真实 fixture 新增首行首列冻结、自动筛选、列表验证和 Inventory Table；回归覆盖冻结写入/取消、签名冲突、无效值拒绝和 Table 部件保真，公开矩阵及结构门禁同步升级。

FR-DATA-009 继续保持“部分完成”。Table 结构编辑、持久化排序筛选、公式/自定义验证和 Excel 完整等价仍未完成。交错批次已由 G7-2 图谱关系创作闭环完成，随后进入 S6-12 图表与绘图对象。

### G7-2：图谱关系创作闭环（已完成）

- 节点详情提供七种语义关系的目标选择、创建和可验证删除，关系变更后重新构建图谱并保持当前节点上下文。
- 用户关系写入源 Markdown 的 `relations` Frontmatter，保留正文、已有元数据和 CRLF/LF 风格；普通正文链接和 PDF 批注关系保持只读证据，不做破坏性推断。
- 图谱节点增加内容签名，写回命令校验知识库边界、Markdown 类型、关系白名单、源文件签名、删除行号和原始 Wikilink 语法，并通过可靠写入替换原文件。
- Rust 回归覆盖 Frontmatter 增删往返、重复关系、陈旧签名和越界目标；前端生产构建与桌面命令注册纳入统一门禁。

该批次基于用户“增强知识图谱存在感”的核心优先级替代原计划中的纯主题插槽，使图谱从查看和治理进一步进入日常关系创作。下一开发入口回到 S6-12 图表与绘图对象分级读取、编辑与保真。

### S6-12：图表与绘图对象分级读取和保真（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v11`，沿工作表 Drawing 关系安全读取两点/一点/绝对锚点，并识别图表、图片、形状及未知绘图对象；外部关系和 `xl/` 之外的目标不跟随。
- 图表结构读取覆盖类型、标题、系列名称、分类公式和值公式；柱形/条形方向分开识别。工作面以绘图对象卡片显示结构、范围和系列数并支持跳转定位。
- 真实 fixture 新增 Inventory 柱形图与嵌入图片；回归固定验证锚点、标题、系列公式、图片替代文本，并在普通单元格写回后逐字节比对 Drawing XML、Drawing 关系、Chart XML 与媒体部件。
- 公开能力严格标为“读取支持、显示受限、编辑计划、往返保真”：当前不复刻 Excel 图表样式和布局引擎，也不开放对象创建、移动、缩放或编辑。

FR-DATA-009 继续保持“部分完成”。下一开发入口为 S6-13 数据透视表、切片器与外部链接边界。

### S6-13：数据透视表、切片器与外部链接安全边界（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v12`，检查阶段从包内读取 Pivot Table、Pivot Cache Definition、Slicer、External Link、Connections 和关系元数据，建立工作簿级高级数据对象清单。
- 透视表公开名称、所属 Sheet、缓存 ID、来源类型/区域、连接 ID 与刷新标记；切片器公开名称、缓存和所属 Sheet；外部链接仅公开类型、缓存数量和目标类别。
- `TargetMode="External"` 关系不跟随、不联网、不读取知识库外文件。连接字符串、查询命令、凭据与完整外部路径不进入前端或 IPC；工作面明确显示“安全模式”和未执行刷新。
- 真实 fixture 新增透视表/缓存、切片器、外部工作簿关系和数据连接。回归覆盖结构语义、敏感占位信息不泄露，以及单元格写回后相关 XML/关系逐字节保真。

FR-DATA-009 继续保持“部分完成”。透视刷新、聚合计算、切片器交互和外部查询仍未开放；下一开发入口为 S6-14 打印区域、页面布局、页眉页脚与保护。

### S6-14：打印区域、页面布局、页眉页脚与保护（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v13`，读取单连续打印区域、页边距、纸张、方向、缩放/适页、打印选项和页眉页脚文本，并在工作面提供结构化摘要。
- 工作簿与 Sheet 保护只公开启用状态、是否受密码保护及受限操作类别；密码校验值、哈希和盐不进入 IPC，也不提供绕过或解锁能力。
- 内容、样式、尺寸、合并和冻结窗格写回触及受保护 Sheet 时整批拒绝；普通写回继续逐字节保留打印设置、保护和未编辑 OOXML 部件。
- 真实 fixture 增加 A4 横向适页、打印区域、页边距、打印选项、页眉页脚、独立受保护 Sheet 和工作簿结构保护；回归覆盖读取、脱敏、拒绝写入和保真。

FR-DATA-009 继续保持“部分完成”。打印预览、分页引擎、布局编辑、保护设置修改和密码解锁仍未开放；下一开发入口为 S6-15 真实复杂工作簿差异门禁、性能预算和完整兼容声明。

### S6-15：复杂工作簿差异、性能预算与兼容声明（已完成）

- 工作簿引擎升级为 `calamine-ooxml-ironcalc-v14`；公式分页读取从第二次 Calamine 全表解析迁移为受限 OOXML 扫描，并覆盖普通公式、共享公式及页面范围过滤。
- 新增 `xlsx-progressive-editing-v1` 机器发布档案。复杂 fixture 复合写回不得增删 ZIP 部件，只有目标 Sheet 与样式部件允许变化，其他部件必须逐字节一致，并重新读取语义验证。
- 新增 4 Sheet、10,000 行 × 12 列、1,000 行公式的 Debug CI 性能负载，对检查、分页、写回、总耗时和文件体积增长建立硬预算。
- 正式发布口径限定为“XLSX 渐进式编辑（以公开兼容性矩阵为准）”；总体 `xlsxRoundTrip` 继续为计划，禁止宣传完整 Excel 等价。

FR-DATA-009 继续保持“部分完成”。按连续工作簿批次上限，下一开发入口切换为 G7-3 / FR-GRAPH-008：让 OPML 节点、Canvas 节点、PDF 批注、Table/图表等成为可寻址的细粒度图谱对象。

### G7-3：跨格式细粒度关系（已完成）

- 建立统一 KnowledgeObject 定位字段：文件对象保持路径 ID，文件内部对象使用格式、路径摘要和源格式原生 ID 生成稳定 ID，并携带 `parentId`、`locator` 与位置标签。
- PDF 批注、Open Table 视图、Canvas 节点和 OPML 主题成为独立图谱节点；文档包含关系、OPML 层级、Canvas 原生边、Dashboard 到 Chart、Canvas 文件节点到 Table 视图均进入统一关系图。
- Markdown 的 `longedit://pdf` 引用精确指向批注；批注已删除时安全降级到 PDF 文件对象，保留历史关系。
- 图谱对象可精确打开并定位到 PDF 页码/批注、Table 视图、Canvas 节点或 OPML 主题；对象类型筛选和详情位置同步扩展。
- 跨格式 fixture 覆盖 Markdown→PDF 批注、Canvas→Table Chart、Dashboard→Chart 和 OPML 父子关系；对象模型、安全边界和规模限制见 `Knowledge_Object_Relation_Contract.md`。

FR-GRAPH-008 与 FR-INDEX-001 验收完成。当前图谱仍在请求时从文件全量构建，下一开发入口为 I7-1 / FR-INDEX-002：建设可删除、可重建、可报告进度且不成为唯一事实源的本地索引。

### I7-1：可重建本地知识索引（已完成）

- 在 Tauri 应用缓存目录建立 schema v1 快照，按知识库规范路径摘要隔离；知识库目录、Git 和公开文件格式不承载私有索引。
- 快照保存统一 KnowledgeObject、Relation、可索引源文件清单和 PDF 批注/OCR sidecar 签名；纯文本与 Mermaid 通过格式注册表 `text` indexer 输出文件级对象。
- 提供 `missing/building/ready/stale/corrupt/error` 状态、构建进度、对象/关系/源数量、显式重建和删除；源文件大小或纳秒修改时间变化后索引标记为过期。
- 重建采用可靠原子写入，构建中拒绝并发重建和删除；快照限制为 128 MB、100,000 源、250,000 对象和 500,000 关系。
- 资料库侧栏提供紧凑状态条和重建/删除操作；回归覆盖可靠往返、源变化失效、损坏识别和删除后从零恢复。

FR-INDEX-002 验收完成。下一开发入口为 I7-2 / FR-INDEX-003：增加 PDF 页/OCR 页等搜索段，让跨格式全文搜索优先消费有效快照，并在缺失或过期时安全回退实时扫描。

### I7-2：索引化跨格式全文搜索（已完成）

- schema v1 快照新增向后兼容的 `searchSegments`：Markdown、纯文本、Canvas、Table、OPML 保存格式适配后的全文，PDF 保存正文页、OCR 页和批注段。
- PDF 搜索段保留页码、批注 ID、提取失败状态和匹配类型；索引结果继续复用现有精确打开链路，不牺牲页级定位。
- 查询仅在快照版本、工作区指纹、源摘要均有效且搜索段存在时消费索引；缺失、旧快照、构建中、错误、损坏或 `stale` 均自动回退实时扫描。
- 搜索结果保持标题、正文、OCR、批注评分和最多 200 条结果边界；批注每文件最多返回 5 条，正文和 OCR 各返回首个匹配页。
- 搜索段上限为 500,000；超过 128 MB 快照或对象/段数安全上限时重建明确失败，旧快照不被替换，实时搜索继续可用。
- 回归覆盖 ready 索引命中、旧快照回退、源变化回退，以及 PDF 正文页、OCR 页和批注 ID 定位。

FR-INDEX-003 验收完成。下一开发入口为 W7-1 / FR-WB-001：建设专业主页/工作台，聚合最近、收藏、待办、知识库健康度和常用 Canvas。

### W7-1：专业主页/工作台（已完成）

- `/workspace` 成为默认入口，提供知识库身份、格式统计、收藏、最近文件、Markdown 待办、常用 Canvas、图谱健康和索引状态。
- 新增受 `WorkspaceGuard` 约束的概览命令，只扫描格式注册表中的文件，跳过符号链接、隐藏项和附件目录；最多扫描 100,000 个条目、24 个待办，单 Markdown 最大 20 MB。
- 概览、图谱健康和索引状态独立加载，局部失败不阻断其他工作区信息；所有打开操作继续复用统一格式路由。
- Rust 回归增至 124 项；工作台行为、安全和桌面视觉边界见 `Professional_Workspace_Home.md`。

FR-WB-001 验收完成。下一开发入口为 W7-2 / FR-WB-002：实现可保存查询、动态集合和可重复打开的智能视图。

### W7-2：保存的搜索、集合和智能视图（已完成）

- 资料库全文搜索增加可索引格式多选过滤，并可把当前查询和过滤条件保存为智能集合。
- 集合只保存条件；从侧栏或专业工作台打开时重新调用索引优先搜索，结果随用户文件动态更新，索引不可用时继续实时扫描降级。
- 保存项进入设备配置而非知识库，最多 64 个；名称、查询、格式数量、唯一 ID、已登记知识库归属和可索引格式均由 Rust 后端验证。
- 配置可靠写入失败时前端状态回滚；删除集合不删除任何用户文件。Rust 回归增至 125 项，完整边界见 `Saved_Search_Collections.md`。
- 质量门禁发现并修复 Sass 传递依赖 `immutable 5.1.5` 的高危拒绝服务公告，锁定 `5.1.9` 后生产依赖审计恢复为 0 漏洞。

FR-WB-002 验收完成。下一开发入口为 W7-3 / FR-WB-003：扩展重复文件、未处理批注和索引风险，形成完整知识库健康治理面。

### W7-3：完整知识库健康治理（已完成）

- 专业工作台新增独立治理队列，把既有断链、歧义、孤立笔记和索引状态与重复文件、未处理 PDF 批注汇总为可行动风险。
- 重复文件只认定字节完全一致的已登记格式文件：大小和 MD5 仅用于候选分组，最终逐字节复核；只提供导航，不自动删除或合并。
- 未处理批注定义为有效 PDF sidecar 批注未被任何 Markdown `longedit://pdf` 精确引用；点击后直接打开 PDF 页码和批注对象。
- 扫描继续受 `WorkspaceGuard`、格式注册表、100,000 条目、单文件 64 MB、累计哈希 512 MB、24 组重复项和 48 条批注边界约束；截断结果明确标记。
- 概览、图谱健康、索引和治理扫描独立失败隔离。Rust 回归增至 126 项，完整契约见 `Knowledge_Workspace_Health.md`。

FR-WB-003 验收完成。统一对象、索引和专业管理工作台阶段收尾；下一开发入口为 S8-1 / FR-DATA-009：补齐 XLSX 工作表行列插入、删除、隐藏和分组能力，并按公开兼容矩阵独立验收。

### S8-1A：工作表行列隐藏与大纲分组（已完成）

- 整行或整列选择可执行隐藏、取消隐藏、建立分组和取消分组；支持 OOXML 0–7 级 `outlineLevel`。
- 隐藏行列在虚拟网格中保留压缩恢复轨道，表头标记隐藏状态和分组层级；普通单元格选择不会误触整行整列结构命令。
- 独立 Rust 事务校验知识库、文件签名、Sheet、数量、坐标和层级，并对受保护 Sheet 整批拒绝。
- `<col>` 区间按原列宽和属性安全拆分，`<row>` 只更新状态属性；真实 fixture 证明除目标工作表 XML 外其他 ZIP 部件逐字节不变。
- Rust 回归增至 127 项，完整边界见 `Workbook_Row_Column_Outline.md`。

S8-1A 验收完成。下一开发入口为 S8-1B / FR-DATA-009：建设整行整列插入删除与坐标迁移事务。

### S8-1B1：行列插删引用迁移内核（已完成）

- 新增独立结构变更模型，统一声明目标 Sheet、行/列轴、插入/删除动作、零基起点和数量；所有边界由 Rust 校验。
- 结构迁移与复制填充公式分离：绝对引用同样随插删移动，插入可扩展范围，删除可收缩范围或生成 `#REF!`。
- 支持二维范围、整行范围、整列范围、跨 Sheet 限定和 Excel 网格上限；字符串、函数名、Table 结构化引用及非目标 Sheet 保持不变。
- 新增有界 Tauri 预览命令，单次最多处理 10,000 条公式和引用，不写文件；Rust 回归增至 132 项。
- 完整规则和未开放边界见 `Workbook_Structure_Migration.md`。

S8-1B1 验收完成。

### S8-1B2A：普通工作表整行插删事务（已完成）

- 新增 `update_workbook_structure` Tauri 命令，在知识库路径守卫、文件签名、包大小、ZIP 安全解析和可靠写回约束下执行整行插入/删除。
- 目标 Sheet 的行节点、单元格坐标和维度，与所有 Sheet 的相关公式及工作簿定义名称共用 B1 迁移内核；插入后再删除具有往返回归。
- 当前只接受行轴；受保护 Sheet、计算链以及合并、冻结、验证、筛选、条件格式、超链接、Table、绘图/批注、透视表等复杂目标结构在写入前整批拒绝。
- 后端事务尚未接入工作簿工具栏，发布口径仍为渐进式编辑；完整边界见 `Workbook_Structure_Migration.md`。

S8-1B2A 验收完成。

### S8-1B2B1：工作表内复杂行引用载体（已完成）

- 整行插删事务迁移合并区、自动筛选、冻结窗格、选择状态、数据验证、条件格式和内部超链接；`sqref` 多区域与验证/条件格式公式继续复用 B1 内核。
- 冻结边界同步更新 `ySplit`、`topLeftCell`、`activePane` 与 selection；普通拆分窗格和完整取消冻结的删除仍明确拒绝。
- 真实 XLSX fixture 覆盖冻结、筛选、合并、验证和条件格式的插入、删除与语义重读；完整对象删除在父节点计数同步完成前取消事务。
- Table、图表、drawing anchors、计算链、整列事务和前端入口仍未开放。

S8-1B2B1 验收完成。下一开发入口为 S8-1B2B2 / FR-DATA-009：迁移关系部件和计算链，补齐复杂对象删除，再进入前端行插删。

### S8-1B2B2：关系部件、计算链与对象删除（已完成）

- 包级事务沿工作表关系迁移 Excel Table 的 `ref/autoFilter`、目标 Sheet 的 Drawing 行锚点，以及关联 Chart 的系列公式；图片和形状继续保留原对象内容。
- `calcChain.xml` 按 Sheet ID 迁移公式单元格坐标，删除消失条目并在前序条目移除时补回继承的 Sheet 上下文。
- 完整删除合并区、自动筛选、数据验证、条件格式或超链接时移除对应节点，并同步 `mergeCells/dataValidations` 父节点计数。
- 真实 XLSX fixture 覆盖 Table、Chart 与 Drawing；直接 OOXML fixture 覆盖计算链和多类对象删除。完整 Table 删除、透视表、批注、嵌入对象、未知扩展和整列事务仍明确拒绝。
- 后端事务仍未暴露为前端命令入口；完整边界见 `Workbook_Structure_Migration.md`。

S8-1B2B2 验收完成。下一开发入口为 S8-1B3 / FR-DATA-009：接入前端整行插入/删除、提交后重算与重载，再继续整列事务。

### S8-1B3：前端整行插入删除工作流（已完成）

- 工作簿工具栏只在单一连续整行选择时开放“在所选行上方插入”和“删除所选行”，单次上限 10,000 行；受保护 Sheet、保存中、结构事务中和存在未保存草稿时禁用。
- 前端复用签名保护的 `update_workbook_structure` 原子事务；删除前明确展示影响范围和不可由当前撤销栈恢复的确认，后端拒绝边界继续原样呈现。
- 提交成功后清空旧坐标撤销/重做历史，重载目标 Sheet 与对应分页，恢复整行选区并对已加载公式触发现有重算链路。
- 工作簿源码契约门禁固定入口、签名、草稿保护、删除确认、历史清理、重载和重算链路；总体 XLSX 能力仍为渐进式编辑。

S8-1B3 验收完成。下一开发入口为 S8-1C / FR-DATA-009：将同一坐标迁移和包级保真规则扩展到整列插入/删除。

### S8-1C：整列插入删除事务与前端工作流（已完成）

- `update_workbook_structure` 扩展为行列共用的签名保护事务；整列插删迁移单元格坐标、公式、定义名称、列宽/隐藏/大纲区间、冻结列、选区和工作表范围对象。
- Table、Chart、Drawing 与计算链复用同一轴向迁移：Chart 系列公式和 Drawing 横向锚点同步移动；会改变 Table 列数或包含活动筛选条件的列事务明确拒绝。
- 真实 XLSX 往返覆盖自定义列宽、冻结列、筛选范围、合并、验证、跨 Sheet 公式、定义名称、Chart/Drawing 关系部件，以及插入后删除恢复；命令层覆盖旧签名拒绝和连续行列事务。
- 前端整行整列菜单按当前连续轴向选择显示“上方/左侧”操作，成功后清空旧坐标历史、重载、恢复对应轴选区并重算已加载公式。

S8-1C 验收完成。下一开发入口为 S8-2A / FR-DATA-009：建设 Excel Table 创建、范围扩缩与列结构编辑事务，逐步解除当前 Table 列数变化拒绝边界。

### S8-2A：Excel Table 创建、范围扩缩与列结构编辑（已完成）

- 新增签名保护的独立 Table 包级事务；创建时原子维护工作表 `tableParts`、Sheet 关系、`[Content_Types].xml` 和新 Table 部件，保存后执行包验证与语义重读。
- 工作面支持从单一连续选区创建普通 Table，也支持把选区覆盖的已有 Table 扩缩到目标范围；首行表头同步为唯一列定义，成功后清空旧坐标历史、重载选区并重算已加载公式。
- 后端拒绝空白或重复表头、非法/重复 Table 名称、重叠 Table、合并单元格，以及包含活动筛选、汇总行、计算列或扩展元数据的高风险调整。
- 真实 XLSX 往返回归覆盖首次创建关系和内容类型、范围扩列、列定义重读与包验证；公开矩阵将 Excel Table 编辑升级为“受限”。

S8-2A 验收完成。下一开发入口为 S8-2B / FR-DATA-009：补齐 Table 改名、样式选项与转换为普通区域/删除生命周期，再进入持久化筛选排序。

### S8-2B：Excel Table 名称、样式与生命周期（已完成）

- Table 名称可在工作面直接修改；后端按工作簿全局、不区分大小写校验名称唯一性，并拒绝会破坏现有结构化公式引用的改名。
- 工作面提供内置 Table 样式选择，以及首列、末列、行条纹和列条纹开关；写回只更新目标 Table 的 `tableStyleInfo`。
- 转换为普通区域和删除 Table 定义均保留单元格数据，并原子移除 Table XML、Sheet `tablePart`、关系项和内容类型声明；受保护 Sheet、活动筛选、汇总行、计算列、结构化引用和扩展元数据整批拒绝。
- 真实 XLSX 往返回归覆盖创建、扩列、改名、样式重读、转区域、删除后重建和包验证；前端生产构建与 Rust 全量单元测试通过。

S8-2B 验收完成。下一开发入口为 S8-2C / FR-DATA-009：持久化 Table/AutoFilter 筛选条件、排序状态和清除条件。

### S8-2C：Excel Table 与 AutoFilter 持久化排序筛选（已完成）

- 工作簿公开模型读取 Table 和工作表 AutoFilter 的筛选列、包含文本、排序列、方向及是否可安全编辑；重载工作表时恢复对应数据视图。
- 新增签名保护的筛选事务，支持单列“包含”条件、单列升/降序状态和清除全部条件；Table 与工作表 AutoFilter 共用同一验证和 OOXML 写回内核，不重排源数据行。
- 多列组合、Top10、动态、颜色、图标和日期分组筛选标记为只读；应用新条件时拒绝覆盖，只有用户明确确认“清除条件”才移除复杂条件。
- 真实 XLSX 往返回归覆盖 Table/工作表筛选写入、通配符转义、排序状态重读、清除与包验证；前端提供应用、清除、保护/草稿拦截和失败提示。

S8-2C 验收完成，Table 子阶段收口。下一开发入口为 S8-3 / FR-DATA-009：命名区域、公式/自定义数据验证与条件格式规则编辑。

### S8-3A：命名区域生命周期编辑（已完成）

- 名称框支持基于单一连续选区创建工作簿级或 Sheet 级命名区域，并可更新引用、改名和删除；操作成功后直接重读工作簿公开模型。
- 独立 Tauri 命令使用内容签名、工作区守卫、可靠替换、写后检查与 OOXML 包验证；工作簿结构保护、非法名称、同作用域重名和越界区域整批拒绝。
- 只开放非隐藏、非 `_xlnm.*` 系统名称的安全单一 A1 区域。更新引用不修改公式；被公式引用的名称拒绝改名或删除，当前不实施不可靠的公式自动迁移。
- Rust 回归覆盖全局/局部同名、创建、引用更新、改名、删除、公式引用保护、签名冲突和包验证；前端生产构建通过。

S8-3A 验收完成。下一开发入口为 S8-3B / FR-DATA-009：公式及自定义数据验证规则编辑；随后进入 S8-3C 条件格式执行与规则编辑。

### S8-3B：公式及自定义数据验证规则编辑（已完成）

- 工作面支持从单一连续选区创建列表、整数、小数、文本长度和自定义公式验证规则；已有规则可编辑公式、比较方式、错误提示与输入提示，可重新应用到当前选区或删除。
- 新增签名保护的数据验证 Tauri 命令和 OOXML 事务，维护 `dataValidations/@count`、规则属性、`sqref` 与 `formula1/formula2`，写回后执行包验证和语义重读。
- 受保护 Sheet、重叠规则范围、外部工作簿引用和包含扩展子节点的复杂规则拒绝编辑；公式、命名区域与自定义规则只负责可靠写入和往返，不冒充本地 Excel 求值。
- 修复验证公式 XML 实体拆段读取，完整恢复 `>`、`<`、`&`；回归覆盖创建、更新、第二规则、删除、重叠/外部引用拒绝、签名冲突和包验证。

S8-3B 验收完成。下一开发入口为 S8-3C / FR-DATA-009：条件格式执行与规则编辑。

### S8-3C1：基础条件格式执行与规则编辑（已完成）

- 工作面读取并执行单规则 `cellIs` 数值条件，支持大于、小于、等于、不等于、区间及反区间；按优先级叠加填充色、文字色和粗体，并遵守 `stopIfTrue`。
- 连续选区可创建、编辑、重新应用范围或删除基础规则，提供红/黄/绿填充与红/绿文字预设；成功后通过语义重读刷新工作表。
- 独立 Tauri 命令使用内容签名、工作区守卫、Sheet 保护校验、OOXML 包验证和可靠替换；写回维护 `conditionalFormatting`、`cfRule`、优先级与 `styles.xml/dxfs`。
- 公式表达式、色阶、数据条、图标集、主题/索引 DXF 及多规则组保持可见、只读和往返保真，不把当前数值子集宣传为完整 Excel 条件格式引擎。
- Rust 回归覆盖创建、更新、删除、XML 实体公式读取、复杂规则只读、签名冲突和包验证；前端生产构建与契约门禁通过。

S8-3C1 验收完成。下一开发入口为 S8-3C2 / FR-DATA-009：公式表达式、色阶、数据条和图标集的可验证执行与分级编辑。

### S8-3C2A：直接引用表达式条件格式（已完成）

- 支持 `A1/$A1/A$1/$A$1` 与数字、文本或布尔字面量的单次比较，例如 `$D2="逾期"`、`A1>=60`；相对行列按规则首个范围左上角迁移。
- 表达式规则复用条件格式视觉样式、优先级、`stopIfTrue`、范围重应用、删除、签名保护、包验证和语义重读链路。
- 后端和前端使用同一受限语法边界；文字和布尔只允许等于/不等于，越界引用不执行。函数、区域、跨 Sheet、外部引用及复合表达式保持只读保真。
- 真实 XLSX 回归验证表达式读取、编辑、XML 实体、`type="expression"` 写回及复杂公式拒绝；前端生产构建通过。

S8-3C2A 验收完成。下一开发入口为 S8-3C2B / FR-DATA-009：双色/三色色阶的阈值插值、渲染与安全编辑。

### S8-3C2B1：固定数值双色/三色色阶（已完成）

- 条件格式模型读取 `colorScale/cfvo/color`，固定数值阈值的双色和三色色阶支持分段 RGB 线性插值、端点截断和非数值跳过。
- 工作面可创建、编辑、重新应用范围和删除固定阈值色阶，校验 2/3 个严格递增有限数值及 `#RRGGBB` 颜色。
- 色阶颜色直接写入工作表规则，不追加或污染 `styles.xml/dxfs`；写回继续使用优先级、签名保护、Sheet 保护、包验证和语义重读。
- 动态 `min/max`、百分位、百分比和公式阈值因需要完整范围统计而保持可见、只读、往返保真；不使用分页样本伪造全范围颜色。
- 回归覆盖三色色阶创建、两色色阶更新、删除、动态色阶只读、样式部件不变和包验证；前端生产构建通过。

S8-3C2B1 验收完成。下一开发入口为 S8-3C2B2：后端全范围统计与动态阈值色阶；随后进入数据条和图标集。

### S8-3C2B2：动态阈值色阶与全范围统计（已完成）

- 后端直接扫描完整工作表 XML 中目标 `sqref` 的数值单元格，不依赖当前分页；为每个色阶返回稳定的 `resolvedValue`。
- 支持 `min`、`max`、固定数值、百分比和百分位阈值；百分比按完整范围最小值/最大值插值，百分位采用包含端点的线性排名插值。
- 工作面可创建和编辑动态阈值标记，重载后使用后端解析值渲染；无数值范围保持可编辑但不着色，解析后阈值逆序则降级只读。
- 单 Sheet 最多解析 64 条动态色阶，百分位累计最多保留 1,000,000 个数值；超限规则降级只读，避免不受控内存和排序成本。
- 公式阈值继续只读保真；回归覆盖 `min/percentile/max`、`min/percent/max`、默认动态双色、完整范围解析、DXF 不变和包验证。

S8-3C2B2 验收完成。下一开发入口为 S8-3C2C / FR-DATA-009：数据条读取、渲染与分级编辑。

### S8-3C2C1：非负固定阈值数据条（已完成）

- 条件格式模型读取标准 OOXML `dataBar/cfvo/color`，开放两个固定数值阈值且满足 `0 ≤ minimum < maximum` 的非负数据条；颜色、是否显示数值及最短/最长条长进入稳定公开模型。
- 工作面以完整单元格宽度为基准渲染半透明数据条，按阈值截断比例并保留原值显示；`showValue=false` 时隐藏单元格文字，但值仍可通过选择与公式栏访问。
- 连续选区可创建、编辑、重新应用范围和删除该子集；写回直接维护 `cfRule/dataBar`，不追加 `styles.xml/dxfs`，并继续执行签名保护、Sheet 保护、包验证和语义重读。
- 解析器区分基础 OOXML 与 `x14:` 扩展条件格式，避免扩展层缺少基础 `sqref` 属性导致整个工作表读取失败。动态 min/max/percent/percentile、负值轴、方向、边框/渐变及图标集仍只读保真。
- 回归覆盖创建、更新、删除、颜色与显示参数、DXF 不变、默认动态数据条只读，以及 `x14:` 扩展共存读取；前端生产构建通过。

S8-3C2C1 验收完成。下一开发入口为 S8-3C2C2 / FR-DATA-009：动态阈值解析与负值轴数据条；随后进入图标集。

### S8-3C2C2：动态阈值与自动正负轴数据条（已完成）

- 数据条复用完整工作表范围统计，支持 `min`、`max`、固定数值、百分比和百分位阈值，分页工作面只消费后端返回的 `resolvedValue`。
- 固定数值阈值允许负数和跨零区间；工作面分别处理全正、全负和跨零数据，在跨零区间按解析后的最小值/最大值计算自动轴位置，正负条从轴线向两侧延伸。
- 创建与编辑入口接受 `min/max/num/percent/percentile` 两端阈值；写回继续使用标准 OOXML `dataBar`，不追加 DXF，并保留签名保护、Sheet 保护、范围统计上限、包验证和语义重读。
- `x14:` 的负值颜色、边框、渐变、方向和显式轴位置需要扩展规则 ID 与兼容回退，当前继续只读保真；本阶段的“负值轴”是由标准阈值语义计算的自动轴，不冒充完整 Excel 2010+ 数据条样式编辑。
- 回归覆盖固定跨零阈值、`min/max` 全范围解析、百分位解析、创建/更新/删除、DXF 不变及带 `x14:` 的默认高级数据条只读。

S8-3C2C2 验收完成。该 XLSX 历史分支的下一入口为 S8-3C2D / FR-DATA-009；当前产品主线以文档顶部 DOCX/PPTX 路线为准。

### S8-3C2D：标准图标集读取、渲染与分级编辑（已完成）

- 条件格式模型覆盖 Excel 2007 标准 3/4/5 图标族、阈值、反向顺序和“仅显示图标”；`num/percent/percentile` 阈值由完整范围统计解析，分页工作面不以可见样本推断。
- 工作面以轻量 Unicode 符号呈现方向、信号灯、旗帜、评级和分区等图标语义，并按阈值包含性、优先级和反向顺序选取图标；隐藏数值时仍可从选择与公式栏访问原值。
- 连续选区可创建、编辑、重新应用范围和删除标准图标集；写回标准 OOXML `iconSet/cfvo`，支持 `gte`、`reverse` 和 `showValue`，不追加或修改 DXF。
- 自定义混合图标、公式阈值和仅存在于 `x14:` 的星形/三角形/方框族保持可见、只读和往返保真，不冒充 Excel 原生图标像素资源。
- 回归覆盖创建、更新、删除、完整范围阈值解析、严格边界、反向/仅图标、DXF 不变、第三方标准文件读取，以及高级变体只读降级。

S8-3C2D 验收完成。下一开发入口为 S8-3C3 / FR-DATA-009：多规则优先级管理、冲突提示与复杂规则分级开放。

### S8-3C3A：多规则优先级管理与冲突提示（已完成）

- 工作面可在当前单元格命中的多条规则间前后切换，明确显示规则序号、全 Sheet 优先级、类型和“命中即停止”状态，并解释规则按小数字优先执行的冲突语义。
- 可编辑规则支持提高/降低优先级；后端按 `(priority, groupIndex, ruleIndex)` 建立稳定顺序，交换后规范化为连续优先级，仅改写标准 `cfRule@priority` 属性。
- 基础数值与直接引用表达式规则可编辑 `stopIfTrue`；前端视觉执行继续按优先级叠加，并在匹配停止规则后终止后续规则。
- 优先级移动可跨过只读复杂规则，但不会修改其公式、视觉定义或 DXF；目标规则只读、位于边界、Sheet 受保护、存在未保存草稿或签名过期时拒绝事务。
- 回归覆盖三条重叠规则、上下移动、连续优先级规范化、边界拒绝、公式内容不变、`stopIfTrue` 保留、DXF 不变和包级重读。

当前开放的是独立单规则 `conditionalFormatting` 容器之间的管理。同一 `sqref` 容器内包含多条 `cfRule` 时仍整体只读，避免更新或删除一条规则时破坏共享范围及未知扩展。下一开发入口为 S8-3C3B / FR-DATA-009：同范围多规则组的规则级寻址、拆分/合并与安全生命周期。

### S8-3C3B：同范围多规则组的规则级安全生命周期（已完成）

- 条件格式变更契约增加可选 `ruleIndex`，以 `groupIndex + ruleIndex` 精确定位同一个 `conditionalFormatting/sqref` 容器内的规则；旧调用缺少该字段时可反序列化，但更新、删除和排序必须提供完整身份。
- 解析器不再因为同组规则数大于一而把全部规则降级为只读；每条规则仍独立执行类型、公式、DXF 和视觉阈值校验，受支持规则可编辑，未知或复杂兄弟规则继续只读。
- 更新或删除只跳过目标 `cfRule`，其余兄弟规则和未知 XML 事件逐项复制；删除至最后一条时移除空容器。规则可在组内及跨组调整优先级。
- 同组规则共享 `sqref`，因此单条规则不得直接改变范围；前端禁用“应用条件选区”并显示同范围组序号，避免隐式改变其他规则的作用域。
- 回归使用 rust_xlsxwriter 生成真实双规则同范围组，覆盖第二条更新、第一条删除、最后一条删除、兄弟公式保留、`stopIfTrue`、容器计数、范围变更拒绝和包级重读。

S8-3C3B 验收完成。下一开发入口为 S8-3C3C / FR-DATA-009：共享范围规则的显式拆分/重新组合，以及复杂表达式能力分级扩展。

### S8-3C3C1：共享范围规则显式拆分与重新组合（已完成）

- 同范围组中的可编辑规则可显式拆分到当前连续选区；后端提取目标 `cfRule` 的原始事件，先从共享容器移除，再以新 `sqref` 包装为独立组，不通过公开模型重建公式、阈值或样式。
- 独立规则存在范围完全相同的其他组时可重新组合；规则原始事件追加到稳定选择的目标组，再删除空源容器，公式、优先级、`stopIfTrue` 和 DXF 引用保持不变。
- 拆分要求源组至少两条规则、目标范围合法且与共享范围不同；组合要求源组仅一条规则且存在完全相同范围的目标组。复杂只读规则、受保护 Sheet、脏草稿和旧签名继续拒绝。
- 前端根据当前选区启用“拆分规则”，根据同范围候选启用“合并同范围”，两者均显示不可撤销的结构确认；普通单规则的范围重应用工作流保持不变。
- 回归覆盖真实双规则组、原始规则提取、拆分到新列、独立范围重新对齐、合并回单容器、公式和容器计数、DXF 不变及包级重读。

S8-3C3C1 验收完成。下一开发入口为 S8-3C3C2 / FR-DATA-009：复杂条件表达式的安全语法子集、依赖求值与只读降级。

### S8-3C3C2：复杂条件表达式安全子集与依赖求值（已完成）

- 条件格式表达式从单次直接引用比较扩展为 `AND`、`OR`、`NOT` 递归组合，比较两侧支持同 Sheet A1 相对/绝对引用以及有限数字、文本和布尔字面量；引用与引用可比较，文本和布尔仍只开放等于/不等于。
- Rust 写入边界和 TypeScript 执行器使用同一安全语法约束：公式最多 512 字符、嵌套最多 8 层、函数最多 8 个参数、引用最多 8 个；区域、跨 Sheet、名称、外部引用、算术、未知函数和超限表达式只读并保留原始 XML。
- 前端以首个 `sqref` 左上角作为相对引用锚点建立表达式 AST，执行短路逻辑和多依赖比较；越界或读取失败不触发样式。可见规则依赖其他分页时自动按需预取，草稿值和已重算公式结果继续优先参与求值。
- 创建与编辑提示已开放安全组合表达式；回归覆盖真实 XLSX 复合公式写回/重读、中文文本、引用比较、未知函数、区域/跨 Sheet、嵌套和引用上限，以及不支持公式的只读降级。

S8-3C3C2 验收完成，S8-3 条件格式规则管理批次收口。下一开发入口为 S8-4A / FR-DATA-009：图表与绘图对象的可编辑模型、对象选择及安全事务基础。

### S8-4A：绘图对象稳定身份、选择与安全锚点事务（已完成）

- 图表、图片和形状对象由 Drawing 部件路径、锚点序号和 OOXML 对象 ID 组成稳定身份；工作面可选择对象，并明确区分可编辑标准双单元格锚点与只读单点/绝对锚点。
- 标准 `twoCellAnchor` 对象可编辑名称和替代文本，并可把当前连续选区应用为新的起止锚点，实现受限移动和缩放；不重建图表、媒体或关系部件。
- 写入命令校验工作区边界、内容签名、工作表保护、Drawing 归属和对象稳定身份，写后重新解析目标语义并执行包校验与可靠替换；过期 ID、过期签名及非标准锚点均拒绝写入。
- Rust 回归覆盖真实图表元数据与锚点修改、Chart XML 和 Drawing 关系逐字节保真、语义重读及陈旧身份/签名拒绝；前端构建和工作簿契约门禁同步覆盖对象选择与编辑入口。

S8-4A 验收完成。下一开发入口为 S8-4B / FR-DATA-009：图表标题、系列引用与可验证本地预览。

### S8-4B：图表标题、系列引用与可验证本地预览（已完成）

- 已有标准标题可在对象工作面直接修改；复杂标题结构不重建，缺少可安全寻址文本容器的标题继续只读。
- 公式型系列按稳定序号编辑分类和值引用；两者必须是本工作簿已存在工作表中的单一一维 A1 区域、点数一致且不超过 10,000 点，外部引用、联合区域、二维区域和未知系列结构拒绝写入。
- 系列引用写入只改目标 `c:f` 并移除该分类/数值引用的旧缓存，随后重新解析标题与系列语义；Drawing XML 和关系部件保持不变，命令继续执行签名冲突、保护状态、对象身份和包完整性校验。
- 工作面复用统一专业图表组件，直接读取分类/数值源单元格生成柱形、折线、饼图和散点受限预览；最多读取 10 个系列并显示 60 个数据点，明确不模拟 Excel 的完整样式与布局引擎。
- Rust 回归覆盖标题和系列引用写回、外部引用拒绝、Drawing/关系保真、命令级签名保护与语义重读；前端类型检查、生产构建和工作簿契约门禁同步覆盖编辑与预览入口。

S8-4B 验收完成。下一开发入口为 S8-4C / FR-DATA-009：基础图表创建、删除与类型切换。

### S8-4C：基础图表创建、删除与类型切换（已完成）

- 连续选区第一行作为系列名、第一列作为分类，可创建标准柱形、折线、饼图和散点图；饼图限定为一个数值系列。
- 创建事务在已有包内容上分配无冲突 Chart/Drawing 部件号、关系 ID 与对象 ID，并同步工作表关系、Drawing 关系和内容类型。
- 删除事务只移除目标双单元格锚点与关系；Chart 部件仅在不再被 Drawing 关系引用时清理，同一 Drawing 中的图片、形状和其他图表保持。
- 类型切换仅开放无未知扩展、非组合且系列为安全内部一维公式的标准图表；切换为饼图时必须只有一个系列。
- 图表模板与选区系列建模已抽离到 `workbook_chart` 子模块；Rust 回归覆盖创建、柱形转折线、删除、包校验和语义重读，前端生产构建及机器契约通过。
- 真实 Tauri WebView 创建、保存、重开 E2E 仍是发布证据缺口。复杂工作簿写回性能阻断已通过选择性解压、XML 后缀保真、未修改 ZIP 部件原始复制和受控压缩关闭；未放宽既有时间或文件增长预算，完整 `ci:check` 已通过。

S8-4C 功能验收完成。下一开发入口为 S8-4D / FR-DATA-009：标准坐标轴标题、图例位置、数据标签、系列名称和有限主题色。

### S8-4D1：标准坐标轴标题与图例位置（已完成）

- 图表公开模型增加分类轴标题、数值轴标题、图例位置和表达可编辑状态；解析时区分主标题与轴标题，散点图的两条数值轴映射为语义 X/Y 轴。
- 无 `extLst`、非组合且只有一组标准语义坐标轴的柱形、条形、折线和散点图可新增、更新或移除轴标题；标准饼图保持无轴语义。
- 同一安全子集可把图例设置为左、右、上、下、右上或隐藏。写回仅修补目标 Chart XML，保留主标题、系列公式和其他图表事件，并执行包校验与语义重读。
- Workbook 工作面增加坐标轴标题和图例位置入口；复杂、多轴或扩展图表继续只读。当前本地预览尚未表现轴标题与图例方位，不把它计为完整视觉对齐。
- Rust 生命周期回归覆盖创建、类型切换、表达更新和删除，并验证主标题及系列公式不变；机器契约和公开兼容边界同步升级。

S8-4D1 功能验收完成。下一开发入口为 S8-4D2 / FR-DATA-009：数据标签与系列显示名；随后进入 S8-4D3 有限主题色与本地预览表达对齐。

### S8-4D2：基础数据标签与系列显示名（已完成）

- 图表公开模型增加基础数据标签、标签可编辑状态和系列名称可编辑状态；现有单元格型系列名称继续可读，安全写回把目标系列名称规范化为受限字面文本。
- 标准安全图表可组合显示数值、分类名和系列名，饼图额外支持百分比。事务只替换图表类型容器下的目标 `dLbls`，不修改主标题、轴标题、图例、系列分类/数值公式或其他图表事件。
- 逐点标签、自定义文本、数字格式、标签位置、形状/文本样式、引导线、系列级标签和扩展节点整体只读；独立可编辑状态确保高级标签不会阻断 D1 已开放的轴标题与图例事务。
- Workbook 工作面增加系列名称和四类标签开关；本地预览解析单元格型系列名并同步基础标签和图例显隐。为防止密集文字重叠，预览对柱形/折线/散点限制 24 个标签，对饼图限制 14 个标签。
- Rust 回归覆盖创建、类型切换、D1 表达、系列改名、数据标签和删除全生命周期，并单独验证高级逐点标签只读降级。

S8-4D2 功能验收完成。下一开发入口为 S8-4D3 / FR-DATA-009：有限主题色与本地预览表达对齐。

### S8-4D3：有限系列颜色与本地预览表达对齐（已完成）

- 图表系列公开模型增加规范化 `#RRGGBB` 颜色及独立可编辑状态；标准安全图表中没有系列样式，或仅含本阶段生成的统一直接 RGB 填充/线条样式时，可更新系列颜色。
- 工作簿工具栏提供当前应用主题的 6 色图表调色板和原生 RGB 取色器。主题预设在提交时映射为显式 RGB，避免伪造尚未支持的 OOXML `schemeClr` 主题绑定语义。
- 写回事务只替换目标系列直属 `c:spPr`，同时设置填充与线条颜色；写回后重新解析颜色、可编辑状态和目标系列身份。渐变、图片/图案填充、透明度/色调变换、主题色节点、多个不一致颜色、逐点格式、趋势线、误差线和扩展节点保持只读。
- 本地预览消费与写回模型相同的系列颜色、分类轴/数值轴标题和图例方位；左右、顶部、底部和右上图例会预留绘图区，饼图同步调整圆心与图例布局，基础标签密度上限保持不变。
- Rust 生命周期回归覆盖颜色规范化、写回、包校验和语义重读，并验证高级渐变系列样式只读降级；Vue 类型检查、生产构建和机器契约通过。

S8-4D3 功能验收完成。下一开发入口为 S8-4D4 / FR-DATA-009：柱形、折线、饼图和散点图视觉矩阵，以及真实 Tauri 创建、保存、重开 E2E。

### S8-4D4：图表桌面视觉矩阵与真实往返（已完成）

- 新增可重复生成的真实 XLSX fixture，单一工作表覆盖柱形、折线、饼图和散点图，以及标题、轴、图例、标签和直接 RGB 系列颜色。
- Rust 命令边界回归逐类修改图表标题、可靠保存、重新打开并验证语义，确保测试覆盖实际 Tauri 命令事务而不只调用底层 XML 函数。
- Debug 专用隔离知识库与主题注入允许真实 Tauri WebView 验收，不影响 Release 配置、用户知识库或已安装应用。
- 专业浅色完成四类截图；专业深色和高对比完成柱形代表场景；真实桌面完成标题编辑、保存、进程重启和重开验证。
- fixture、生成器、命令回归和八张桌面证据进入工作簿机器契约。详细记录见 `S8_4D4_Chart_Desktop_Visual_Audit.md`。

S8-4D4 功能验收完成，S8-4 图表与绘图阶段收口。下一开发入口为 S8-5 / FR-DATA-009：页面布局与保护编辑，先处理打印区域、方向、纸张、页边距和缩放。

### S8-5A：基础页面布局编辑（已完成）

- 单一连续选区可设为打印区域，已有打印区域可清除；工作簿结构保护继续阻止内置打印名称变化。
- 页面设置面板支持纵向/横向、Letter/Legal/A3/A4/A5、10%–400% 缩放或宽高适页模式，以及左、右、上、下、页眉、页脚六项英寸页边距。
- 独立事务只修改目标 Sheet 的 `pageMargins`、`pageSetup`、`pageSetUpPr` 和对应 `_xlnm.Print_Area`；未知页面设置属性、页眉页脚和其他定义名称保持。
- 写回执行内容签名、工作区、128 MB、Sheet 保护、工作簿结构保护、包校验与语义重读；命令回归覆盖创建、更新、缩放模式切换和清除打印区域。
- 真实 Tauri Debug 工作面完成横向 A4 保存与重新读取验证；普通浏览器不作为桌面证据。

S8-5A 功能验收完成。下一开发入口为 S8-5B / FR-DATA-009：奇偶页和首页页眉页脚文本的受限编辑；保护设置修改继续等待独立安全设计。

### S8-5B：页眉页脚受限编辑（已完成）

- 独立页眉页脚面板支持奇数页、偶数页和首页的页眉/页脚文本，以及奇偶页不同、首页不同、随文档缩放、与页边距对齐四个标准标志。
- 每个标准文本字段最多 255 个字符；拒绝 XML 控制字符，XML 特殊字符由写入器转义，空文本会清除对应节点。
- 独立事务只替换目标 Sheet 的标准 `headerFooter` 文本与四个公开属性；页面布局、打印区域、保护材料和工作表其他部件保持不变。
- 写回执行内容签名、工作区、128 MB、Sheet 保护、包校验与语义重读；命令回归覆盖特殊字符、字段代码、奇偶/首页切换、清空、超长拒绝和保护拒绝。
- 图片型页眉页脚、像素级打印预览、保护设置修改和密码解锁继续不开放。

S8-5B 功能验收完成。下一开发入口为 S8-5C / FR-DATA-009：打印网格线、行列标题、水平/垂直居中及基础打印选项，并完成页面阶段收尾审计。

### S8-5C：基础打印选项与页面阶段收尾（已完成）

- 独立打印选项面板支持打印网格线、行列标题、水平居中、垂直居中、黑白打印和草稿质量。
- 首页页码可显式启用或清除，允许范围为 1–32767；未启用时移除 `firstPageNumber` 并写入 `useFirstPageNumber="0"`。
- 独立事务只修改目标 Sheet 的标准 `printOptions` 四个属性，以及 `pageSetup` 的 `blackAndWhite`、`draft`、`firstPageNumber` 和 `useFirstPageNumber`；方向、纸张、缩放、DPI、页边距、页眉页脚和未知属性保持。
- 写回执行内容签名、工作区、128 MB、Sheet 保护、包校验与语义重读；命令回归覆盖创建、更新、清除、页码边界和保护拒绝。
- 页面阶段至此收尾；分页计算、像素级打印预览、图片型页眉页脚、保护设置修改和密码解锁继续不开放。

S8-5C 功能验收完成后，开发入口已切换至 S8-6A：建立公式函数族能力清单、真实 fixture 与错误传播基线，再按函数族扩大语义覆盖。

### S8-6A：公式函数族基线与错误传播（已完成）

- 建立机器可读公式能力清单，固定 IronCalc 版本、`en` locale、UTC 时区和显式按需内存重算模式。
- 真实 XLSX fixture 验证聚合、数学、逻辑和文本四族 16 个函数，并覆盖同 Sheet、跨 Sheet、命名区域、草稿依赖、除零、错误传播、`IFERROR` 恢复、未知函数和循环诊断。
- 计算结果不写回 XLSX 缓存；只有 fixture、计算模块、Tauri 命令边界和工作簿契约同时通过的函数才标记为 `verified`。

### S8-6B：条件聚合与查找/引用函数族（已完成）

- 增加 `SUMIF/COUNTIF/AVERAGEIF` 与 `VLOOKUP/HLOOKUP/INDEX/MATCH`，机器清单扩展为六族 23 个函数、31 个真实 fixture 场景。
- 验证数值比较、单字符通配条件、跨 Sheet 精确与升序近似匹配、文本结果类型保持、未找到 `#N/A` 分类和 `IFERROR` 恢复。
- 性能门禁保留原 10,000×12 负载、时间和 5% 文件增长预算；最多允许两次完整工作流尝试，不拼接不同轮次指标。

### S8-6C：多条件聚合与非易失日期函数（已完成）

- 增加 `SUMIFS/COUNTIFS/AVERAGEIFS` 与 `DATE/YEAR/MONTH/DAY`，机器清单扩展为八族 30 个函数、41 个真实 fixture 场景。
- 验证多条件、跨列条件、无匹配为零、Excel 1900 日期序列、年月日拆解、闰年和无效日期 `#VALUE!` 分类。
- 审计确认 IronCalc 0.7.1 对尺寸不一致的 `*IFS` 范围不返回 Excel 的 `#VALUE!`，因此该语义进入明确排除项，不对外承诺。

S8-6C 功能验收完成。

### S8-6D：现代查找与引用函数（已完成）

- 审计 IronCalc 0.7.1 的真实实现后，将标量结果 `XLOOKUP` 纳入第九个 verified 函数族；清单现为 31 个函数、50 个真实 fixture 场景。
- 覆盖精确匹配、next-smaller 近似匹配、跨 Sheet 列范围、行向量、反向搜索、通配符、文本结果保型、缺省值、默认 `#N/A`、`IFERROR` 恢复和未保存依赖编辑重算。
- 真实 XLSX 同时通过计算模块、Tauri 命令边界和机器契约；`XMATCH` 因依赖未实现而不开放，`XLOOKUP` 数组返回和溢出结果进入明确排除项。

S8-6D 功能验收完成。下一步进入 S8-6E，对数组、溢出、易失函数和外部引用做分级验收。

### S8-6E：数组、溢出、易失函数与外部引用分级验收（已完成）

- 公开易失函数子集增加 `OFFSET/INDIRECT/RAND/RANDBETWEEN/TODAY/NOW`，真实矩阵扩展为十族 37 个函数、56 个场景。
- 契约限定为用户显式触发、UTC、结果仅驻留内存且不写回缓存；覆盖范围引用、同/跨 Sheet 间接引用、草稿依赖、随机数边界和时钟日期关系，不宣称 Excel 自动重算时机等价。
- 计算预检识别多单元格传统数组范围和已知动态数组函数并安全拒绝；真实外部工作簿链接同样在引擎导入前离线拒绝，正常读取、缓存展示和 OOXML 保真不受影响。
- 真实 XLSX fixture、计算模块、Tauri 命令边界和机器契约同时通过。数组/溢出计算和外部数据访问继续不开放。

S8-6E 功能验收完成，S8-6 公式语义阶段收尾。下一步进入 S8-7 高级数据对象。

### S8-7A：高级数据对象审计面板与离线策略（已完成）

- 新增独立 `workbook_linked_data` 领域模块，统一计算透视表、切片器、外部工作簿链接和数据连接的对象总量、来源类型与刷新风险，避免继续把派生策略堆入 OOXML 核心文件。
- 工作簿 IPC 增加权威 `offline_read_only` 策略：允许显示脱敏元数据，禁止刷新、对象编辑、外部目标跟随和敏感字段暴露。
- 工作簿界面新增专业审计面板，按对象类型展示来源、Cache/Connection 标识、打开时刷新风险、包内缓存状态和离线保真说明；本地透视表与切片器支持定位所属工作表。
- `shared/xlsx-linked-data-capabilities.json` 固定四类高级对象的读取、定位、刷新、编辑与保真状态；真实复杂 fixture、敏感信息脱敏和相关 OOXML 部件逐字节保真继续作为门禁。

S8-7A 功能验收完成。下一步进入 S8-7B，审计本地工作表来源透视表的受限刷新/重建可行性；未通过聚合语义和缓存往返门禁前继续只读。

### S8-7B：本地透视表结构与重建候选审计（已完成）

- 独立 `workbook_linked_data` 模块开始解析本地工作表来源透视表的输出布局、Pivot Cache 字段/类型、Cache Records 记录数、行/列/筛选/值字段和聚合方式。
- 后端为每个透视表返回 `candidate_for_rebuild` 或 `inspection_only`，并给出缺失来源、缺失布局、缓存字段/记录缺失、字段数量不一致、聚合未验证和字段引用越界等稳定阻断原因。
- 首批重建候选聚合限定为 `sum/count/average/max/min/product/countNums`；该清单只表示结构审计已覆盖，不表示已实现透视计算。
- 真实兼容 fixture 已从空壳 Pivot 定义升级为含 3 个缓存字段、2 条 Cache Records、行/列/值字段、输出布局和 `sum` 聚合的最小工作表来源透视表。
- 专业审计面板展示字段角色、缓存值类型、聚合、布局、记录数和候选状态，并明确“本阶段仍不执行刷新或写回”。
- Pivot Table、Cache Definition、Cache Records 及其关系部件继续纳入普通单元格写回后的逐字节保真门禁；全局策略仍是 `offline_read_only`。

S8-7B 功能验收完成。下一步进入 S8-7C：为候选透视表实现只驻留内存的聚合预览，对照真实 fixture 验证源数据语义；通过前不修改 Pivot Cache 或工作表输出区域。

### S8-7C：本地透视表内存聚合预览与语义对照（已完成）

- 新增独立 `workbook_pivot` 领域模块和 `preview_workbook_pivot` 命令，仅对 `candidate_for_rebuild` 的本地工作表来源透视表执行内存计算。
- 来源事实固定为工作表当前值，并叠加未保存的非公式草稿；旧 Pivot Cache 不作为计算事实源，公式草稿、页面筛选字段和越界输入明确拒绝。
- 行/列字段使用带类型键分组；`sum/count/average/max/min/product/countNums` 已通过真实 fixture 与独立语义 fixture，明确空值、文本和数值参与规则。
- 工作簿审计面板增加“内存预览”，展示来源范围、草稿数、分组键、聚合结果和参与计数；预览不会覆盖工作表、Pivot Cache、透视定义或原文件。
- 命令边界验证工作簿签名冲突、文件字节不变和未保存草稿生效；机器合同固定 50,000 行、256 列、10,000 分组和 10,000 草稿上限。

S8-7C 功能验收完成。下一步进入 S8-7D：审计受限本地透视结果事务写回的完整 OOXML 重建可行性；只有 Cache、字段项、行列项、输出区域、原子回滚和真实桌面往返同时通过，才讨论开放显式刷新，否则维持预览模式。

### S8-7D：本地透视事务写回可行性审计（已完成）

- `WorkbookPivotAudit` 增加独立 `writeback` 就绪度，不增加刷新或写入命令；状态限定为 `blocked/structure_candidate`，且 `allowed` 固定为 `false`。
- 后端核对行列维度的 Pivot Field items、`rowItems`、`colItems`、页面筛选字段以及 `<location>` 对应工作表输出单元格，不再把“有 Cache 和聚合定义”误判为“可写回”。
- 当前兼容 fixture 被稳定识别为缺少字段项、行列项和输出单元格，只能继续使用内存预览；完整结构单元 fixture 可进入 `structure_candidate`，但仍不能获得写权限。
- 专业审计面板展示四项结构门禁和具体阻断原因；机器合同固定签名校验、影响预览、原子替换、回滚、未触及部件保真和 Excel/LibreOffice 往返六项发布前置条件。

S8-7D 功能验收完成。下一步进入 S8-7E：获取真实 Excel/LibreOffice 生产的完整本地透视 fixture，在临时副本中实现和验证隔离重建原型；未通过真实刷新保存往返前不增加用户文件写回命令。

### S8-7E1：真实生产者透视结构基线（已完成）

- 引入 Apache POI 官方测试数据中的真实 Excel Pivot fixture，固定上游提交、Apache-2.0 许可、文件大小和 SHA-256，避免后续样例静默漂移。
- 样例中的工作表范围透视表通过字段项、行列项和输出单元格审计，进入 `structure_candidate`；命名来源且带页面筛选的透视表保持 `blocked`。
- 普通单元格补丁后，两个 Pivot Table、两个 Cache Definition 和两个 Cache Records 部件逐字节不变，包校验和再次解析通过。
- 本阶段执行时没有可自动化的 Excel/LibreOffice，因此该 XLSX 桌面刷新、保存、重开证据仍为 `pending`；后续安装 LibreOffice 不会自动提升这项独立 XLSX 证据，所有相关写回继续禁用。

S8-7E1 功能验收完成。下一步进入 S8-7E2：在临时副本中建立影响清单和隔离重建原型，先覆盖单个本地工作表来源、无页面筛选对象；任何失败必须丢弃副本，且不增加用户文件写回命令。

### S8-7E2A：隔离重建影响清单与命令边界（已完成）

- 新增签名保护的 `preview_workbook_pivot_rebuild` dry-run 命令，只在独立内存副本中完成包校验和摘要一致性验证，不写入用户文件。
- 通过 OOXML 关系精确定位 Cache Definition、Cache Records、Pivot Table 和输出工作表四类计划影响部件，并返回未触及部件数量。
- 真实 producer fixture 中的完整候选进入 `isolated_dry_run_ready`；命名来源、页面筛选、结构或关系不完整对象保持 `blocked`。
- 审计面板提供“影响清单”入口，展示部件、计划动作、阻断原因和签名、结构、临时副本、重建、回滚、原子替换、桌面往返等门禁状态。
- 命令回归覆盖旧签名拒绝、四类部件完整、隔离副本摘要一致，以及成功/阻断/失败路径后原文件逐字节不变。

S8-7E2A 功能验收完成。下一步进入 S8-7E2B：只在临时副本中实际重建 Cache Definition 与 Cache Records，再逐步扩展 Pivot items 和输出区域；未验证完整包与回滚前，原子替换继续阻断。

### S8-7E2B：临时副本 Cache Definition 与 Records 重建（已完成）

- 只允许 `isolated_dry_run_ready` 对象进入，基于已保存的工作表连续来源区域重新生成 Cache Records，并更新 Cache Definition 记录数和类型范围元数据。
- 文本与日期维度复用已有 `sharedItems` 索引，数值字段使用直接编码；出现新共享项时安全拒绝，避免 Pivot items 尚未同步造成悬空引用。
- 来源公式、混合类型、持续时间、页面筛选、旧签名和不完整关系全部阻断。
- 真实 producer fixture 验证文本/数值/日期三类字段、3 条记录、包校验、语义复读和影响清单外部件保真，并校正陈旧日期最大值；布尔、错误与空值编码暂列已实现未验证。
- 工作簿审计面板在影响计划通过后开放“隔离重建 Cache”，展示字段编码、重建数量和各项门禁；生成包只驻留任务内存，用户文件不修改。

S8-7E2B 功能验收完成。下一步进入 S8-7E2C：在同一临时副本中重建 Pivot Field items、`rowItems/colItems` 和输出区域，并验证失败回滚；原子替换和用户文件写回继续阻断。

### S8-7E2C：临时副本 Pivot items、行列项与输出同步重建（已完成）

- 新增签名保护的 `rebuild_workbook_pivot_isolated_copy`，依次完成 Cache Definition、Cache Records、Pivot Table 和输出工作表四部件同步重建，任何失败只丢弃任务内存结果。
- 当前验证一个行字段、一个列字段、一个求和值字段、无页面筛选的标准紧凑布局；保持字段隐藏项和 sharedItems 索引，重建字段 items、`rowItems/colItems`、明细、行列总计与总计。
- 真实 producer fixture 验证 2 个可见行项、2 个可见列项和 13 个同步输出单元格；来源值从 1 改为 10 后，输出明细/总计同步为 10/13。
- 完整门禁覆盖包校验、对象语义复读、输出值复读、新摘要、四类影响范围外部件逐字节保真，以及页面筛选等不安全对象阻断。
- 工作簿审计面板新增“隔离同步透视表”和结果卡，用户文件、原子替换及真实桌面刷新保存继续禁用。

S8-7E2C 功能验收完成。下一步进入 S8-7E2D：在隔离副本中安全增加 sharedItems，并同步验证 Pivot items、行列映射、输出范围扩缩容和旧输出清理；多轴、多值字段和用户文件写回继续阻断。

### S8-7E2D：新 sharedItems 与输出布局扩缩容（已完成）

- 新增 `rebuild_workbook_pivot_expanded_isolated_copy`，在同一隔离事务协调 Cache sharedItems/Records、Pivot items/行列项、`location` 和输出工作表。
- 旧共享项按来源集合保留或移除，隐藏状态按值继承；新项追加且默认可见，记录和布局统一消费新索引。
- 输出扩展时复用表头、标签和数值样式模板；输出收缩时显式清空旧范围残留坐标，清空结果进入工作簿复读。
- 真实 producer fixture 验证 `A3:D7 → A3:E8` 扩展和 `A3:D7 → A3:C6` 收缩、新增/删除共享项、样式延伸、至少 7 个旧坐标清理及总计值。
- 工作簿审计面板新增“隔离验证布局扩缩容”，展示共享项增减、旧/新范围、输出数、清理数、样式延伸数和完整门禁；用户文件仍不修改。

S8-7E2D 功能验收完成。下一步进入 S8-7E2E：扩展六类非 sum 聚合、单轴布局和多值字段的隔离输出语义；多层轴、页面筛选、原子替换和用户文件写回继续阻断。

### S8-7E2E：聚合与布局变体（已完成）

- 七类受限聚合分别生成临时 Pivot 包并完成 Cache/Pivot/输出联合重建，非 `sum` 结果进入包级输出复读。
- 聚合实现复用预览度量累加器，行列总计和总计从来源记录独立计算，避免平均值、计数和乘积的二次聚合错误。
- 基于真实 producer 来源验证单行轴、单列轴和三度量布局的分组、度量及输出值数量语义。
- 新增签名保护命令和工作簿“隔离验证聚合与布局”结果卡；七个临时包及三类语义预览全部丢弃，不修改用户文件。
- 单轴、多度量的完整 OOXML 轴项与输出布局重写尚未开放，多层轴、页面筛选和外部连接继续阻断。

S8-7E2E 功能验收完成。下一步进入 S8-7E2F：把单轴和多度量语义落到完整隔离 Pivot 包，验证伪轴、标题、总计、样式与真实 producer fixture；原子替换和用户文件写回继续阻断。

### S8-7E2F：单轴与多度量完整隔离包（已完成）

- 单行轴、单列轴和 `sum/count/average` 三度量均生成独立临时 OOXML 包，不再只做内存语义验证。
- 完整同步 `pivotFields`、行列字段、数据字段、`rowItems/colItems`、数据伪轴 `x="-2"`、`location`、输出单元格和旧区域清理。
- S8-7E3C 真实生产者审计修正隐藏项与多级表头后，fixture 分别复读 `A3:B6`、`A3:D5`、`A3:J8`，验证 8、12、60 个输出单元格。
- 表头、标签、总计和值样式从原布局映射到新输出区域，并逐格完成样式复读。
- 三个布局包与七类聚合包合计 10 个临时包；用户文件始终不写入，多层轴、页面筛选、原子替换和桌面生产者往返继续阻断。

S8-7E2F 功能验收完成。下一步进入 X2 / S8-7 后续：建立只写新副本的 Pivot 事务合同、冲突与失败清理，并补齐 Excel/LibreOffice/WPS 刷新保存重开矩阵；生产者往返通过前不覆盖原件。

### S8-7E3A：标准 Pivot 可靠另存新副本（已完成）

- 新增 `save_workbook_pivot_copy`，只把最近一次摘要一致的完整隔离重建结果写入同目录新 `.xlsx`。
- 首批固定一个行字段、一个列字段、一个 `sum` 值字段且无页面筛选；多度量和其他布局继续只做隔离验证。
- 事务拒绝源覆盖、已有目标、路径片段、错误扩展名、旧源签名、旧隔离摘要和未保存草稿。
- 使用可靠新文件原子创建；写后复读目标字节、OOXML、Pivot 身份/布局/聚合语义，并再次验证源文件字节、签名和摘要。
- 真实 fixture 覆盖保存成功与全部关键阻断路径；应用成功后打开已验收副本。

S8-7E3A 功能验收完成。下一步进入 S8-7E3B：使用 Excel、LibreOffice、WPS 对 LongEdit 新副本执行真实刷新、保存、进程重启和重开；矩阵通过前不扩大保存白名单。

### S8-7E3B：标准 Pivot 真实生产者往返矩阵（已完成）

- 通过专用审计 CLI 调用与 Tauri 命令相同的签名、隔离摘要、原子新文件创建和写后复读事务，生成固定 LongEdit 基准副本。
- Microsoft Excel `16.0/20228`、WPS Spreadsheets `12.0/26895`、LibreOffice Calc `26.2.4.2` 分别在独立副本上完成真实刷新、保存、进程退出和新进程重开。
- 三生产者均保持 `PivotTable1`、`A3:D7` 和 `D7=4`，未观察到修复提示；输出文件版本、长度和 SHA-256 已进入机器矩阵。
- LongEdit 对基准及三份生产者输出执行包、Pivot 身份、字段布局、`sum` 聚合和输出值反向复读；CI 固定验证 3/3 证据。
- 本阶段不扩大保存白名单，不开放原件覆盖、单轴、多度量、多层轴、页面字段、切片器或外部数据刷新。

S8-7E3B 功能验收完成。下一步进入 S8-7E3C：逐项把单行轴、单列轴和三度量布局接入可靠新副本，并分别重复三生产者往返；原件覆盖继续阻断。

### S8-7E3C：布局可靠新副本与生产者矩阵（已完成）

- 单行轴、单列轴和 `sum(Field2)/count(Field1)/average(Field2)` 三度量均接入摘要绑定的同目录可靠新副本。
- 修正隐藏轴项过滤，单列轴使用生产者原生两级表头，多度量使用三级表头和数据伪轴。
- Excel、WPS、LibreOffice 对三种布局完成 9/9 刷新、保存、退出和新进程重开；十二份 XLSX 均由 LongEdit 反向复读。
- LibreOffice 单列轴规范化为 `A3:C5`，语义和重开状态稳定；不承诺跨生产者坐标完全一致。
- 原件覆盖、多层轴、页面字段、切片器和外部连接继续阻断。

S8-7E3C 功能验收完成。下一步进入 S8-7E3D：逐项评估其余单度量聚合可靠新副本，并重复三生产者往返。

### S8-7E3D：单度量聚合可靠新副本与生产者矩阵（已完成）

- `count/average/max/min/product/countNums` 六种聚合均接入源签名和独立摘要绑定的同目录可靠新副本。
- 修正跨分组 Grand Total：平均值按贡献数量加权，极值和乘积按各自规则合并。
- Excel、WPS、LibreOffice 完成 18/18 刷新、保存、进程退出和独立重开；每份输出额外复读 OOXML `subtotal`。
- LongEdit 基线 `A3:D6` 经生产者刷新规范化为稳定的 `A3:D7`，聚合、字段来源和关键总计保持不变。
- 原件覆盖、已有目标覆盖、多层轴、页面字段、切片器和外部连接继续阻断。

S8-7E3D 功能验收完成。下一步进入 S8-7E3E：建立多层轴真实 fixture、结构审计和隔离包原型，不直接开放用户文件保存。

### T8-1A：场景化主题预设契约与首批实现（已完成）

- 在唯一主题注册表中增加 `core/scenario/legacy` 发布层级、场景标签和动效节奏，不修改历史配置存储结构。
- 正式发布范围从三套核心预设扩展为七套：新增长文阅读、护眼研读、编码专注和创意图谱四个场景预设；其余已有组合归入兼容分组。
- 设置页按“专业与场景预设 / 更多外观组合”分组；命令面板继续自动消费完整注册表；应用预设同步持久化色调、风格、代码主题、编辑器背景和动效。
- 注册表对七套发布预设执行 WCAG AA 4.5:1 正文对背景/表面对比度门禁，结构检查固定 3 个核心 + 4 个场景预设。

### T8-1B：场景化主题真实桌面视觉矩阵（已完成）

- 使用隔离 WebView2 数据目录和 CDP 连接真实 Tauri Debug 页面，为四套新场景预设生成设置页 1440×900、工作台 1024×768、思维导图 760×900 共 12 张证据。
- 机器清单固定每套预设最终 `theme/style/motion` 状态；主题契约检查验证清单、三张场景证据和单文件最小体积。
- 修复 760px 图谱标题竖排、工具栏拥挤、筛选条错位和初始详情遮挡画布；紧凑页头、横向工具栏与窄宽度初始详情策略进入门禁。
- Debug E2E 模式禁止配置落盘，并支持独立指定 theme、style、code theme 和 motion，避免视觉审计污染用户配置。

T8-1 场景化主题扩展和 S8-6 公式语义阶段均已收尾，S8-7A～S8-7E2F 已完成高级数据对象审计与隔离重建矩阵，S8-7E3A/E3B 已交付标准 Pivot 可靠新副本及 3/3 往返，S8-7E3C 已交付单行轴、单列轴、三度量新副本及 9/9 往返，S8-7E3D 已交付六种额外单度量聚合及 18/18 往返。X2～X5 / S8-7 后续～S8-8 保留为完整 Excel 等价专项主线。E1B 的 ODT 解析、只读工作面、双索引和定位实现完成，Word/LibreOffice 生产者通过，但当前 WPS 仍输出 OLE，生产者门禁保持 2/3。E1B 等待可信外部 WPS ODF 环境期间，下一入口为 **S8-7E3E Pivot 多层轴结构审计与隔离包原型**。

后续基础版本顺序仍为 E1B → E1C → E2A → E2B → E2C → E3 → R。完整 Excel 专项 X1 / S8-7E2F 已完成，后续继续 X2～X5 / S8-7 后续～S8-8；历史审计继续用于追溯，不再作为恢复入口。

2026-07-30 X3-A 更新：在 E1B 与多层轴 Pivot 生产者门禁等待外部环境期间，公式专项重新进入连续交付。IronCalc 固定升级至 0.8.0，标量 `XMATCH` 已通过真实 XLSX、计算模块和 Tauri 命令边界，公式清单现为十族 38 个函数、64 个场景。动态数组、数组公式、外部工作簿计算和缓存写回仍未开放；下一批 X3-B 先建立动态数组只读结构与溢出边界。

## 12. 需求变更流程

1. 新需求先说明目标用户、场景和解决的问题。
2. 评估是否属于现有需求；属于则更新验收标准，不重复立项。
3. 不属于时分配新编号、优先级和目标里程碑。
4. 涉及数据格式、写入、安全或依赖许可时必须先完成技术审计。
5. 开发完成后更新状态、测试证据和已知限制。
6. 未满足验收标准的功能只能标记“部分完成”，不能标记“已完成”。

## 13. 关键产品决策

| 决策 | 结论 |
|---|---|
| 数据事实源 | 用户文件，不是内部数据库 |
| 图谱与 Canvas 的关系 | 图谱负责发现，Canvas 负责表达和整理 |
| 思维导图格式 | 层级事实源使用开放 OPML 2.0，自由空间投影使用 JSON Canvas；不建立封闭专有格式 |
| PDF 编辑范围 | 阅读和批注，不承诺原始排版编辑 |
| Excel 实施顺序 | 保留 CSV/Table 与渐进式 XLSX 基线；S8-7E3C 已完成三种 Pivot 布局可靠新副本与 9/9 生产者矩阵，后续按 X2～X5 / S8-7 后续～S8-8 推进聚合白名单、复杂对象和发布矩阵 |
| 图表保存方式 | 保存可编辑源码或配置，不以图片作为唯一源 |
| 索引定位 | 可重建缓存，不是唯一事实源 |
| AI 定位 | 辅助生成、提取和整理，结果必须可编辑和可追踪 |
| 1.0 协作范围 | 本地单用户优先，实时协作不进入 1.0 主线 |

---

## 14. 文档维护约定

- 本 PRD 负责“做什么、优先级和如何验收”。
- 综合设计文档负责“为什么这样设计、技术方案和竞品依据”。
- 实际代码和自动化测试负责证明“已经完成”。
- 每个里程碑结束时更新版本、状态和追踪矩阵。

统一文件管理阶段 A、G8-1/G8-2A/G8-2B、PDF B0～B2C、DOCX C0～C2E、PPTX C3A～C5D、E0 和 E1A 已完成。E1B WPS ODT 生产者门禁受外部环境阻断，基础版本仍剩 E1B、E1C、E2A、E2B、E2C、E3、R 七个门禁。完整 Excel 等价 X1 / S8-7E2F 已完成；等待可信 WPS ODF 环境期间，本地继续按 X2～X5 推进。

2026-07-29 补充：E1B 已建立 `checkpoint → released-preview` 原子发布状态机。该工作只完成发布控制，不改变阶段完成度；当前仍为 2/3 且 `.odt` 未注册。未来只有三生产者 fixture 与 `closure-candidate` 桌面证据同时通过，才能登记精确的 `preview-only` ODT 合同并转入 E1C；任何提前注册、写能力或证据不同步均由 CI 阻断。

同日补充：E1B 新增 WPS 跨机器关闭证据包，允许在可信且具备 ODF 能力的机器生成、在当前开发机按固定源/产物/manifest SHA-256 严格导入；覆盖、半包、额外 ZIP 成员、路径穿越和摘要漂移均阻断。该能力只解决证据交接，不改变当前 2/3 状态，也不替代对产出环境来源的审计。
