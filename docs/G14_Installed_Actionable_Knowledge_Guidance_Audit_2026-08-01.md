# G14 已安装客户端可执行知识建议审计（2026-08-01）

## 结论

G14 已把 G13 的可执行知识建议纳入 U2 已安装客户端生命周期，但尚未完成本次产品提交的托管执行。固定合成资料库具有 5 个已连接对象、100% 覆盖率及 4 类关系，因此安装版必须稳定显示 `network-health-on-track`，中文标题必须包含“状态良好”。

验收脚本会在真实 Tauri WebView2 中读取建议卡、截图、点击建议，并确认图谱在同一窗口、同一应用路由中打开，不产生独立编辑窗口。随后脚本返回工作台并继续验证原有的主题居中导航，避免新增建议破坏 G11 行为。

当前状态为 `installed-actionable-guidance-runner-integrated-hosted-execution-next`，`releaseCandidate=false`。

## 证据边界

本阶段使用固定合成资料库，不包含真实用户正文、文件名或路径。计划证据包括知识网络脉搏截图、建议进入图谱截图和脱敏 JSON；在 GitHub 托管生命周期成功且截图经人工复核之前，策略中的托管执行与视觉复核必须保持 `false`。

该流程仅证明未签名 Windows Server 托管环境中的安装版交互，不等于 Windows 10/11 签名客户端证据，也不能代替 G13 的真实用户授权观察。

## 下一步

推送编排提交后，以 G13 产品提交为 `product_ref` 触发 U2。成功后下载证据，核对建议代码、同一窗口路由、截图和来源提交绑定，再以独立审计提交关闭 G14。之后仍需用户自行决定是否执行真实资料库匿名观察。
