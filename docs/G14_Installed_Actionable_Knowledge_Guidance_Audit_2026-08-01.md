# G14 已安装客户端可执行知识建议审计（2026-08-01）

## 结论

G14 已把 G13 的可执行知识建议纳入 U2 已安装客户端生命周期，并由 GitHub Actions 运行 `30690425501` 完成托管执行。固定合成资料库具有 5 个已连接对象、100% 覆盖率及 4 类关系，安装版稳定显示 `network-health-on-track`，中文标题为“知识网络状态良好”。

验收脚本会在真实 Tauri WebView2 中读取建议卡、截图、点击建议，并确认图谱在同一窗口、同一应用路由中打开，不产生独立编辑窗口。随后脚本返回工作台并继续验证原有的主题居中导航，避免新增建议破坏 G11 行为。

当前状态为 `hosted-installed-actionable-guidance-passed-real-user-observation-next`，`releaseCandidate=false`。

## 证据边界

本阶段使用固定合成资料库，不包含真实用户正文、文件名或路径。原始 Actions 证据包括知识网络脉搏截图、建议进入图谱截图和脱敏 JSON；仓库只提交不含对象 ID 与路径的验收回执。人工复核确认建议清晰可见、样式与工作台一致，点击后同一窗口显示实际图谱与节点详情，无加载遮罩或意外窗口。

该流程仅证明未签名 Windows Server 托管环境中的安装版交互，不等于 Windows 10/11 签名客户端证据，也不能代替 G13 的真实用户授权观察。

## 下一步

下一阶段为 `G15-consented-real-library-guidance-observation`。仍需用户自行决定是否在真实资料库检查建议、预览匿名观察并提供其主动核验过的聚合回执；不得以本次合成安装证据代替真实用户结论。
