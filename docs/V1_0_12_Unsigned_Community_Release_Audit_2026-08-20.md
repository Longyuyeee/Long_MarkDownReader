# v1.0.12 无签名社区版发布审计

状态：**候选证据通过，待发布 GitHub Release**

渠道：`community-unsigned`

企业发布候选：`releaseCandidate=false`

## 候选范围

- P2-A：PDF 已实现的表单、永久脱敏、文字水印和文档属性能力与格式能力页重新对齐。
- P2-B：PNG/JPEG/WebP/BMP 新增亮度、对比度、饱和度和恢复原色，并通过真实像素、源摘要与另存复开验证。
- P2-C：图片新增光标锚定滚轮缩放、双轴拖拽平移、双击 100%/适应窗口和方向键平移，并通过真实 Tauri 鼠标输入与宽窄屏验证。

## 发布门禁

1. 冻结 `1.0.12` 版本事实源并通过完整 Quality Gate。
2. 从冻结提交构建无签名 EXE、MSI 与 NSIS，记录大小、SHA-256 和 `NotSigned` 状态。
3. 在 GitHub 托管一次性 Windows 环境执行 22 项安装生命周期与 18 项安装后工作区检查。
4. 回写候选证据并再次通过完整 Quality Gate。
5. 发布 GitHub Release，重新下载复核 MSI、NSIS 与 `SHA256SUMS.txt`。
6. 单独验证官方 `v1.0.11 -> v1.0.12` 应用内更新、自动重启和合成资料保留。

## 候选结果

- 冻结源码：`e069947b5a628b10887d55d1ff5e6e402de86a04`；Quality Gate `32342006759` 通过。
- 本地无签名 EXE、MSI 与 NSIS 均为 `1.0.12` 且 `NotSigned`，没有生成旧式 `.sig` 或 `latest.json`。
- 托管 U2 `32342021774` 通过 22/22 生命周期与 18/18 安装后工作区检查，失败数为 0。
- 本机已有一个用户产品实例，未终止用户进程或执行本地安装器；真实安装边界由托管一次性 Windows 证据补足。

## 当前边界

- 当前公开稳定版本仍为 v1.0.11，直到 GitHub Release 和远端附件复核完成。
- 社区安装包无 Authenticode 商业签名，Windows 可能显示未知发布者或 SmartScreen。
- 用户可以从官方 Release 手动下载安装；应用内自动更新同样必须先由用户确认并完成 SHA-256 校验。
- 候选测试和本地构建不能描述成正式发布；README 下载链接在发布前继续指向已验证的 v1.0.11。
