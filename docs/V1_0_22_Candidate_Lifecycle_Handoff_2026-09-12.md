# v1.0.22 固定候选托管安装验收

> 2026-09-22 更新：运行 `34679919233` 已成功，三包字节校验与 TXT 原图审阅已完成，关键报告已入库。完整视觉及搜索安装态仍待验收；不再重复触发同候选构建。以 [最新用户任务计划](./Development_User_Task_Plan_2026-09-22.md)为当前口径，下文 queued/运行中为历史快照。

> 新候选已单次触发：[运行 34679919233](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/34679919233)，编排提交 `4839c252a55b3849ff3b18f290ee6a92e4413672`，产品输入 `cc58aa68d1974ac2445ed4b884c7677f5a320e93`。登记时 queued，尚无验收结论。候选更新后当前开发总审计通过；下一次查询同一运行，不重复触发。完整产物名称应为 `v122-candidate-lifecycle-34679919233`，下载后执行只读回执校验、检查签名与原始截图。旧包不可通过当前政策校验，这是预期身份隔离，不应为复用旧包修改校验器。搜索安装态验收独立待办、Release 仍关闭。

> 当前接续（2026-09-12）：旧候选 `ada0cd60` 的运行 `34676269824` 虽成功但已被实际截图审阅拒绝。现固定修复候选 `cc58aa68d1974ac2445ed4b884c7677f5a320e93`，本地完整补丁门和隔离桌面普通保存/草稿复验见 [桌面复验](./V1_0_22_Text_Save_Desktop_Review_2026-09-12.md)。使用包含保存反馈检查的最新编排运行新候选，绝不复用旧包的通过结果。以下旧候选及运行中描述均为历史。

日期：2026-09-12。固定产品候选 `ada0cd607ae64b03c7f8786cef68f7326a5a8663`，运行版本 1.0.22；公开版仍为 1.0.21。

## 实际代码审查与取舍

本轮复读 v1.0.21 候选工作流、R5I 安装运行器和公开发布回执。保留既有一次性 Windows runner 的构建、隔离安装、工作面及管理回滚流程，不改变两项搜索产品范围。

旧工作流从旧 Tag 重建上一版 NSIS。本轮改为下载官方 v1.0.21 NSIS，并在安装前验证发布回执的大小、SHA-256、Tag 对应源码；避免把重新编译的旧版本当作用户已安装的公开包，亦省去旧版编译。

新工作流只接受政策登记的精确 40 位候选提交，用户输入经环境变量传入，不能拼接成 PowerShell 代码。新包从固定候选执行完整质量门后生成 MSI/NSIS；工作流仅有 contents:read，不发布、不修改 Tag。安装操作仅发生在托管一次性机器，不动本机用户安装。

## 证据合同

- 政策：`shared/v122-candidate-lifecycle-policy.json`。
- 工作流：`.github/workflows/v122-candidate-lifecycle.yml`。
- 预期沿用 R5I 22、R5J 18 + 路由 11、R5L 7 项；数量只是预期，不能预填通过。
- 制品：`v122-candidate-lifecycle-<run_id>`，包含双安装包、构建回执、前版官方包与生命周期证据，保留 14 天；应尽快下载并将脱敏证据长期入库。
- 构建回执明确记录前版来源及 SHA-256；后续导入须比对候选提交、版本、文件摘要和实测失败数，并直接复核截图。
- 搜索安装态故障恢复/长标题打开、暗色与键盘组合验收单独 pending，通用运行器不覆盖这些新产品动作。
- 官方 v1.0.21 → v1.0.22 应用内更新观察仍为发布后 pending，与本次安装升级验收分离。

## 本地验证

工作流 YAML 解析及合同测试 4/4，包括全部内嵌 PowerShell 的语法解析（不执行安装）。新测试接入当前总审计。产品候选此前本地完整质量门已通过；远端候选 Quality Gate `34676177616` 截至工作流提交前仍在运行，不能写为成功。本工作流也会在构建前重跑固定候选完整质量门，失败则不构建。

## 接续顺序

> 最新运行进展（2026-09-12）：`34676269824` 的 **Run frozen candidate release quality gate 已成功完成**，目前进入 **Build frozen candidate MSI and NSIS**。这是固定产品候选 `ada0cd60…` 的远端质量门结果，不是整个工作流成功；安装包身份、安装生命周期、搜索专项和发布均尚未通过。以下较早的“质量门仍运行”记录保留为历史。

### 制品下载后的只读身份校验（2026-09-12 补强）

新增命令 `node scripts/verify-v122-installer-receipt.mjs <解压后的完整 artifact 根目录>`，同时核对构建回执的固定候选、公开前版身份、新双包唯一目标/版本/NotSigned 声明、前版官方来源，以及三个安装文件实际大小和 SHA-256。根目录须保留完整附件结构，重名文件或符号链接拒绝；命令不写仓库、不执行安装、不导入证据。

10 项合成字节单元测试及当前总审计通过，覆盖错误候选、重建旧版、错误旧摘要、重复目标、路径穿越、版本/隐私缺失及字节/大小偏移。测试数据不是安装包，不代表真实制品已经校验。命令成功仅返回 `installer-bytes-verified-only`，安装生命周期和搜索验收明确仍为 false；还须另行验证真实 Authenticode、远端运行身份和原始生命周期结果、直接审阅截图。

本次查询运行 `34676269824` 仍无完成结论；不重复触发、不调整固定产品候选、不填写安装通过。此次新增的是下载后核对工具，不改变正在运行的编排提交或产品功能。

已用本机已有的 workflow 授权登录凭据推送编排提交 `fc0e76ff`，并且只触发一次：[运行 34676269824](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/34676269824)。产品输入仍为 `ada0cd607ae64b03c7f8786cef68f7326a5a8663`。截至登记时尚无完成结论，不要重复触发；下一次先查询此运行。当前总审计也已通过。

推送工作流后只触发一次，输入上述固定候选提交；查询运行记录确认受理，失败保留原始结果并先诊断。完成前不设置安装/发布门禁 passed。成功后下载产物、审查截图/哈希并补搜索专项验收，最后才能冻结正式制品、发布 Release 和执行官方更新观察。
