# LongEdit 用户体验收口审计与验收清单

日期：2026-08-04  
基线：`main` / `3b9855f` / v1.0.3  
来源：v1.0.3 安装版真实资料库与多格式测试反馈  
EA-5C 回写：2026-08-10 / v1.0.5 / 托管安装态运行 `31368123651`
状态约定：`已完成` 表示实现、专项合同和对应桌面或安装态证据已经闭合；`已完成（有界）` 表示目标按公开能力边界闭合，不等于完整 Office 等价或任意环境兼容。

## 1. 总体结论

本文件最初记录的五类共性缺口现已完成有界收口；以下根因保留为历史依据，逐项表已经按后续真实 Tauri、无签名安装态和机器合同证据回写：

1. 安装态权限、更新策略与运行状态存在明确故障。
2. 页面之间没有稳定保持资料库、标签和文件上下文。
3. 通用工作区壳层在高缩放、小窗口和多标签场景下会挤压或暴露原生滚动条。
4. 文本、结构化数据、表格、Office、图谱等编辑器缺少统一的视觉与交互标准。
5. 多项内部工程概念直接暴露给用户，名称和操作结果难以理解。

当前 43 类格式继续按“可靠性止血 -> 工作区统一 -> 编辑器体验 -> 专业格式 -> 全格式验收”的顺序受机器合同约束。真实跨版本自动更新和依赖系统解码器的视频兼容性作为非阻断观察保留，不回写为虚假的全环境保证。

## 2. 已确认根因

- `src-tauri/capabilities/default.json` 仅授权 `dialog:allow-open` 和 `dialog:allow-save`，没有授权 `dialog:allow-confirm`。删除索引、删除表格行、关闭脏标签等路径使用 `window.confirm`，与安装态报错完全吻合。
- `TableView.vue` 在挂载时立即要求 `store.libraryPath` 非空，却没有等待配置恢复；从图谱返回后路由重建时可能先执行表格读取，造成“表格路径无效或知识库尚未配置”。
- v1.0.3 Release 明确不发布 `latest.json` 和 `.sig`，但 `tauri.conf.json` 仍配置 Tauri Updater 的 `latest.json` 地址，设置页仍提供原生更新检查，因此当前失败是发布策略与客户端能力不一致，不是用户网络操作错误。
- `WorkspaceTabs.vue` 使用 `overflow-x: auto` 和细原生滚动条，没有滚轮横移；标签最小宽度只有 92px，无法稳定显示六个中文字符和关闭按钮。
- `WorkspaceTabs` 根元素设置 `flex: 1 1 auto`，但它同时被放入多个 CSS Grid 页面中的 `auto` 行，是 TXT/LOG 等页面高度分配异常的重要风险。
- CSV/TSV 的 `TableView.vue` 数据模型已支持 `frozenColumns: number`，UI 却退化成 `freezeFirstColumn: boolean`，所以自由冻结前 N 列不需要重做底层格式。
- CSV 的行号按钮直接执行删除并调用 `window.confirm`；“转换为 Table”立即创建文件并替换当前路由，没有事前解释、目标路径确认或结果入口。
- Markdown 默认模式来自已持久化的 `store.editorMode`，缺少对旧配置和首次启动的“所见即所得”迁移保证。
- 外部修改检测混用了磁盘时间与 `Math.floor(Date.now()/1000)`，并且只比较 `modified > lastKnownModified`，没有用读取签名、写入回执和弹窗去重，容易产生焦点切换误报。
- JSON 树形视图虽然限制为 2,000 个可见节点，但每次折叠仍同步扫描全部分析路径，且 2,000 行一次性进入 DOM，不是真正虚拟列表。
- DOCX 已进入分页直接编辑路线：安全文本、基础样式和图片说明支持内存草稿及显式覆盖保存；复杂版式对象仍保持只读，尚未达到完整页面等价编辑。
- Canvas 使用同步 `ResizeObserver(updateSize)`，未做 `requestAnimationFrame` 合并和尺寸相等短路，符合已观察到的 ResizeObserver 循环风险。

## 3. 逐项需求与验收标准

| ID | 用户需求与审计判断 | 目标与验收标准 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| UX-01 | 设置页是单列长页面，功能按开发阶段堆叠。 | 已改为八类侧栏导航；设置页在三套主题、100%/125%/150% 缩放的真实 Tauri 视觉矩阵中无全局溢出。 | P1 | 已完成 |
| UX-02 | 格式能力矩阵返回按钮固定回资料库。 | 设置入口携带来源和定位参数，矩阵按来源返回设置或资料库；格式能力页已在最新安装态真实打开。 | P0 | 已完成 |
| UX-03 | 检查更新请求一个发布策略明确不提供的 `latest.json`。 | 当前改用固定 GitHub Release、严格附件名与 SHA-256 的受控更新链，失败使用中文提示并保留下载页兜底；真实跨版本覆盖安装列为发布后观察。 | P0 | 已完成（有界） |
| UX-04 | Privacy Diagnostic 三处文案为英文。 | 已统一为“隐私诊断包”及中文保存、成功和失败反馈，脱敏合同持续拒绝正文、完整路径、API 密钥、缓存正文和凭据。 | P1 | 已完成 |
| UX-05 | “知识网络匿名观察”“真实资料库改善观察”是内部审计术语。 | 已折叠到知识能力高级区域并改为任务语言；最新安装态真实进入该设置目的地并完成本地观察流程。 | P1 | 已完成 |
| UX-06 | “专业与场景预设”和“更多外观组合”本质都是主题组合。 | 已合并为单一可筛选主题库，浅色、深色、护眼、创意和高对比共享注册表，重复展示被机器合同拒绝。 | P1 | 已完成 |
| UX-07 | 索引状态与按钮意义不明，删除又触发 ACL 错误。 | 搜索与关联自动准备，重建/清除进入更多菜单并使用已授权应用内确认；最新安装生命周期覆盖索引重建和管理恢复。 | P0 | 已完成 |
| UX-08 | 左侧文件树目前主要使用统一文件图标，扩展名辨识不足。 | 文件树已保留完整后缀；43 种注册格式按代码、结构化数据、表格、文档、演示、图形和媒体映射不同 Lucide 图标与识别色。右键“编辑显示样式”可为单个文件设置背景、文字色和标记图标，配置仅保存在本机，不修改文件。 | P1 | 已完成 |
| UX-09 | 标签固定为 176px，最小保护宽度 156px；12 个真实文件同时打开时，最短文字区域仍为 107px，格式图标、脏状态和关闭按钮不被挤压。 | 已在真实 Tauri WebView2 中完成宽屏与 1000px 窄窗口验收；超出宽度进入横向滚动，不再压窄。 | P0 | 已完成 |
| UX-10 | 标签栏已将纵向滚轮、Shift+滚轮和触控板主方向增量转换为平滑横向滚动，并只在存在横向溢出时阻止默认滚动。 | 实机输入验证三种滚动路径均改变了标签栏位置，键盘左右键也可切换并显露目标标签。 | P1 | 已完成 |
| UX-11 | CSV/TSV 行号只选择行，删除由明确命令触发并使用应用内确认；取消删除后行数与选中状态保持不变。 | 真实 Tauri WebView2 已验证选择、删除确认和取消路径，未出现 ACL Promise 错误。 | P0 | 已完成 |
| UX-12 | 图谱返回现在恢复活动文件、滚动位置及工作表上下文；Table 会先结束加载并等待滚动容器挂载，再恢复坐标。 | 真实 Tauri WebView2 已分别验证 CSV、TSV、XLSX、ODS 往返；XLSX 恢复 `Details` Sheet，ODS 恢复 `Notes` Sheet，均未出现“无法打开表格”。 | P0 | 已完成 |
| UX-13 | CSV/TSV、XLSX 与 ODS 的冻结表头、行号和冻结单元格使用不透明主题表面，并保留明确分隔边缘。 | CSV 三列冻结以及真实 XLSX 冻结窗格、ODS 固定表头/行号的计算样式 alpha 检查和截图均通过。 | P1 | 已完成 |
| UX-14 | “冻结首列”已替换为 0 至 12 的列数步进器，实际值受表格列数约束；前 N 列按累计列宽计算 sticky 偏移。 | Table 视图配置写入 `frozenColumns`，CSV/TSV 会话内保留；三列横向滚动实测位置稳定。 | P1 | 已完成 |
| UX-15 | 转换入口改为“创建 Table 副本”，创建前展示目标文件名、目录、用途、原文件不变和同名处理，可取消。 | 完成后提供“打开新文件”和“在文件树中定位”；实机生成与定位通过，CSV/TSV 哈希不变。 | P1 | 已完成 |
| UX-16 | 多个页面在安装版/高缩放下被工具栏、状态栏和面板挤压，确认操作还会报 ACL。 | 统一工作区尺寸、命令条横滑、窄屏收纳和对话框 ACL 已覆盖高风险表面；45 张壳层截图、编辑器视觉矩阵和安装态路由回归均通过。 | P0 | 已完成（有界） |
| UX-17 | 原生横向滚动轨道已隐藏，左右箭头按实际边界启停，并提供边缘阴影反馈；激活远端标签会自动滚入可视区。 | 三张实机截图已人工复核，无原生滚动条、双滚动条、重叠或编辑区挤压。 | P1 | 已完成 |
| UX-18 | 全局最右侧为文件上下文按钮预留 42px 全高条带，按钮上下形成大量空白。 | 保留文件上下文按钮，但改为贴边悬浮；关闭时不占布局列，展开时关系面板从右侧覆盖显示。 | P1 | 已完成 |
| UX-19 | Markdown 会继承旧的源码模式配置。 | 新安装、首次打开和旧配置迁移后默认使用所见即所得；只有用户主动选择源码才持久化，专项迁移合同和真实 Tauri Markdown 工作区通过。 | P0 | 已完成 |
| UX-20 | Markdown 代码块的背景与高亮主题可能来自不同明暗体系，导致低对比。 | 九主题代码块、语义色、光标与选区合同通过；Markdown 在三主题、三缩放视觉矩阵中保持可读。 | P0 | 已完成 |
| UX-21 | 顶部“孤立风险”把内部图谱健康指标暴露成主操作。 | 已删除当前文件顶部的关系数量、入链数量和“孤立风险”按钮及占位；相关信息仅在工作台、搜索结果或按需关系界面显示。 | P1 | 已完成 |
| UX-22 | TXT 页面出现编辑区一行、状态信息占据大面积的布局故障。 | 命名 Grid 区域保证编辑器占据剩余高度，状态栏保持紧凑；真实 Tauri 与最新安装态 TXT 保存复开均通过。 | P0 | 已完成 |
| UX-23 | 切换窗口焦点会误报外部修改并反复弹窗。 | 内容摘要、精确文件身份、写入回执和同签名去重已接入；真实桌面外部冲突、重新加载与保存复开通过。 | P0 | 已完成 |
| UX-24 | TXT 视觉单调，深色主题可能出现黑色光标。 | 六类 CodeMirror 工作区共享九主题语义色、行号、当前行、选区、搜索和高对比光标合同。 | P1 | 已完成 |
| UX-25 | JSON 源码和诊断区域视觉单调。 | JSON 使用统一结构化语义色和可收起诊断，三主题、三缩放真实 Tauri 视觉矩阵通过。 | P1 | 已完成 |
| UX-26 | “JSON Path”意义不明确。 | 已改为“字段路径与快速定位”，提供示例、定位、复制反馈和可理解空状态。 | P1 | 已完成 |
| UX-27 | JSON 深色主题光标可能仍为黑色。 | 光标、选区、gutter 与提示层由统一主题显式赋色，九套配色对比合同通过。 | P0 | 已完成 |
| UX-28 | 大 JSON 树同步扫描全部路径并一次渲染最多 2,000 DOM 行，折叠仍可能卡顿。 | 父子索引、按展开节点遍历、固定行高虚拟窗口和自适应串行分析已经接入。 | P1 | 已完成（有界） |
| UX-29 | LOG 固定只读，且布局/视觉与 TXT 有相同问题。 | 专业查看保留范围读取、筛选、高亮、尾随和刷新；有界编辑模式提供撤销/重做并只在明确保存后覆盖。 | P1 | 已完成（有界） |
| UX-30 | HTML 及代码类格式缺少专业编辑体验和基础提示。 | HTML 默认源码并提供净化、CSP、sandbox 三重安全预览；代码工作区提供行号、高亮、括号、缩进、有界补全和诊断。 | P1 | 已完成（有界） |
| UX-31 | YAML/XML/TOML 与 JSON 存在相同的布局、主题、光标和辅助面板问题。 | 三类结构化工作区复用主题、响应式导航、串行自适应分析和显式保存边界；真实桌面保存复开通过。 | P1 | 已完成 |
| UX-32 | XLSX 高级对象已收纳到“更多”菜单；格式、数据和图表工具按需单行展开，窄宽度切换为图标模式，冻结区域使用不透明底层。 | 真实 XLSX 在资料库嵌入布局中完成冻结窗格、紧凑工具栏、Sheet 切换、滚动与图谱返回验收，截图无挤压或透明叠层。 | P1 | 已完成 |
| UX-33 | 已完成分页画布、2–32 项多目标草稿、整批验证/覆盖/另存、安全单运行字符格式和简单超链接标签；真实 Tauri WebView 与无签名安装版均确认 Word/LibreOffice 各 2 项可编辑并通过草稿、撤销/重做、隔离验证和保存提示，WPS 字段链接全部只读；U2 生命周期 18/18 通过。 | 有界页面编辑阶段已收口；不宣称完整 Word 等价。页眉页脚、批注、脚注、域和浮动对象未来需先建立跨部件事务。 | P1 | 已完成（有界） |
| UX-34 | Canvas 尺寸观察已改为动画帧合并、相同整数尺寸短路和卸载清理；启动级与应用级边界只处理两种标准 ResizeObserver 通知，普通错误仍显示。真实 Tauri WebView2 完成 6 轮 Drawio/Canvas 切换、视口变化、缩放和拖动，运行时异常为 0。 | 开发态稳定性已收口；后续安装版回归由补丁发布门禁复核，不把可恢复通知扩展成模糊错误吞噬。 | P0 | 已完成 |
| UX-35 | 文件树节点已移除原生 `title`，鼠标悬浮与键盘方向键当前项共用带 `role="tooltip"` 的详情浮层；Esc、焦点离开和鼠标移出均可关闭。真实 Tauri WebView2 验证 2/2 文件节点无原生提示且运行时异常为 0。 | 统一详情提示阶段已收口；后续文件树交互继续复用该浮层，不重新引入浏览器原生文本提示。 | P1 | 已完成 |
| UX-36 | 资料库空白区与目录右键首项均为“新建”，按文档、数据、图表与画布、代码与配置覆盖注册表全部 18 种可创建格式；重命名显示完整文件名，同名目标明确拒绝，已注册后缀变化采用单弹窗两步确认并同步树、标签和路由。真实 Tauri WebView2 创建、冲突与改后缀验收通过，运行时异常为 0。 | 文件树新建与安全重命名阶段已收口；格式能力继续以共享注册表为事实源，扩展名变化不宣称转换文件内容。 | P1 | 已完成 |
| UX-37 | UX-37A OPML 脑图与 UX-37B 产品知识图谱专业画布均已完成。知识图谱支持五种布局、三套主题、平移缩放、适合窗口、框选多选、成组拖动、键盘移动和撤销重做；真实 Tauri 验证 6 节点交互、源文件哈希不变且运行时错误为 0。 | 已关闭；证据见 `docs/evidence/ux37a-opml-canvas` 与 `docs/evidence/ux37b-knowledge-graph-canvas`。 | P1 | 已完成 |
| UX-38 | UX-38A 至 UX-38F 已覆盖 41 类历史格式、10 套有界体验配置和 12 个维度，`pending=0`；图片与视频随后由独立媒体合同加入，总注册格式为 43 类。 | 继续保持明确能力边界：ODT 受 2/3 生产者门禁限制，旧 Office/WPS 依赖外部应用，复杂 Office 对象不扩写为等价编辑。 | P1 | 已完成（有界） |
| UX-39 | 可编辑工作区已按各自能力接入统一标签返回、内存历史、显式保存和未保存保护；只读工作区保留稳定返回与上下文恢复。 | 返回、撤销、重做不得写盘；只有明确保存命令可进入格式专用 writer，未验证结构继续只读或可靠新副本。 | P0 | 已完成（有界） |
| UX-40 | 文档切换反复显示“正在打开知识库”并可能卡死。 | 已删除全屏路由遮罩和串行退场，增加启动 IPC 超时、路由恢复和文本读取代次；12 格式 Tauri 快速切换及最新安装态路由/性能检查通过。 | P0 | 已完成 |
| UX-41 | 左侧“智能集合”含义不清，导航仅让活动标签显示文字，切换时会改变占用宽度。 | 保留可复用搜索和局部图谱能力并统一命名为“保存视图”；七个标签稳定等分，侧栏低于 460px 时仅显示图标，达到阈值后统一显示文字，活动项不改变宽度，动效响应减少动画设置。 | P1 | 已完成 |

EA-5C 汇总合同会逐行校验 UX-01 至 UX-41，不允许重新出现 `待处理`、`开发中`、`待复测` 或 `进行中`。后续追加的横向滚轮、画布右键、Table 看板、下拉控件、更新链、命令条和媒体增强以独立审计组继续受 `check:current-development-audit` 约束。真实跨版本自动更新需等下一版本附件存在后执行；MOV/MKV/AVI/MPEG/MPG 的应用内播放受系统解码器约束，这两项是非阻断观察。

## 4. 开发顺序

### 阶段 A：可靠性止血

处理 UX-02、03、07、11、12、16、19、20、22、23、27、34、39。完成后先发布内部修复构建，使用当前测试库复测，不等待视觉大改一起交付。

### 阶段 B：统一工作区壳层

处理 UX-01、08、09、10、17、18、21、35、36。统一标签、文件树、关系入口、响应式工具栏、确认服务和返回上下文。

### 阶段 C：文本与结构化编辑器

处理 UX-24 至 UX-31。抽出共享 CodeMirror 主题与编辑器壳层，再分别接入 TXT、HTML/代码、JSON、YAML、XML、TOML；LOG 保留专业查看器并增加编辑入口。

### 阶段 D：表格与 Office

处理 UX-13、14、15、32、33。先收口 CSV/TSV 和工作簿交互，再调整 DOCX 阅读与编辑路线。

### 阶段 E：图谱、脑图与全格式验收

处理 UX-05、06、37、38。术语、主题库和图形编辑一起收口，最后按格式矩阵逐项验收。

## 5. 每步必做审计

1. 代码审计：记录变更文件、根因和兼容边界。
2. 自动检查：至少执行 TypeScript/Vite 构建、相关前端检查和受影响 Rust 测试。
3. 安装态检查：不能只看浏览器开发态；必须验证 Tauri 安装版 ACL、资源、路由和主题。
4. 视觉检查：100%、150%、200% 缩放；至少 1366x768、1920x1080 和窄窗口。
5. 真实文件检查：使用 `E:\Long\Long编辑测试库-v1.0.3`，保存结果放到其 `99-保存输出到这里`，不覆盖原始样本。
6. 文档回写：只在验收条件满足后更新本文状态，并记录提交号和复测结果。

## 6. 已确认的产品决策

1. DOCX 目标为直接编辑源文件的页面式编辑器；编辑期间不自动写盘，只有用户点击保存才写回源文件。
2. LOG 保留专业日志查看器，同时提供编辑模式；允许用户明确确认后保存并覆盖原 `.log`。
3. HTML 默认进入源码编辑，同时提供隔离危险能力的安全网页预览模式。
4. 所有可编辑格式统一提供返回、撤销和重做按钮；所有格式都遵循“编辑停留在内存，点击保存才写入文件”的交互原则。

上述范围已经确认并完成阶段 A-E 的有界收口。下一阶段进入 `1.0.6` 无签名社区补丁打包，不扩大 Excel/Office 或媒体解码承诺。
