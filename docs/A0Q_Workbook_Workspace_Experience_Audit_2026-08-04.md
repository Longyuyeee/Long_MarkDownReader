# A0Q 工作簿工作区体验审计

日期：2026-08-04  
需求：UX-32  
结论：代码侧完成，状态保持“待安装包复测”。

## 本阶段完成

- 删除默认占据整行的“高级数据对象”横栏，把透视表、切片器和外部数据统一收进主工具栏的“透视表与数据连接”入口。
- 入口显示对象数量；存在刷新请求时使用风险色，并在悬浮说明中明确 LongEdit 已阻止自动刷新和外部目标访问。
- 弹窗首层改用用户语言解释对象用途和安全边界，保留原有透视表定位、内存预览、可靠新副本和高级兼容性能力。
- 主工具栏回归 48 px 共享高度，Sheet 标签缩至 35 px；1180 px 以下收起入口文字但保留图标与数量，900 px 以下仍保持入口和保存可达。
- 工作表默认单元格改用不透明 `--theme-surface`，表头与行号使用不透明 `--theme-surface-2`；冻结分隔改为清晰的主题色边界，不再透出滚动内容。

## 验证

- `npm.cmd run build`
- `npm.cmd run check:workbook-contract`
- `npm.cmd run check:workbook-workspace-experience`
- `git diff --check`

## 保留边界

本阶段没有改变 XLSX 解析、公式、透视表刷新、保存白名单或可靠副本能力。安装包仍需在九主题下滚动含冻结行列的 XLSX，并复测 100%、150%、200% 缩放和窄窗口入口可达性，因此 UX-32 保持“待复测”。

下一阶段进入 UX-33：审计 DOCX 当前编辑与预览边界，按“直接编辑源文件、仅点击保存写盘、失败不破坏原件”的要求推进页面式编辑体验。
