# M6-4 v1.0.18 原子版本迁移与候选打包审计

审计日期：2026-08-31

阶段结论：**通过；v1.0.18 版本身份、完整质量门、真实 MSI/NSIS 和当前 Tauri WebView2 烟测均已完成，下一步只进入 M6-5 托管安装生命周期。**

## 1. 目标与发布边界

44 个当前运行时身份文件从 1.0.17 原子迁移到 1.0.18，公开版本、Tag 和不可变提交仍保持 v1.0.17 / `2b6235d…`。v1.0.18 的质量、构建、安装和发布事实没有继承旧版本；本阶段 `releaseCandidate=false`，未创建 v1.0.18 Tag 或 GitHub Release。

候选源码精确绑定已推送提交 `5988c03c0167b00cb86ed9a5f3cfe85f0b280a6a`。

## 2. 真实测试：预期、实际、差异与修正

| 项目 | 预期 | 实际 | 结论 |
| --- | --- | --- | --- |
| 完整发布门 | v1.0.18 全部通过 | 6,275 模块、43 格式/91 扩展名、Rust locked check 通过，生产依赖漏洞 0 | 通过 |
| MSI | 产品版本 1.0.18，未签名 | 74,186,752 bytes；SHA-256 `f1f5c1…be01`；MSI 数据库 ProductVersion 1.0.18；`NotSigned` | 通过 |
| NSIS | 产品版本 1.0.18，未签名 | 65,934,312 bytes；SHA-256 `96db31…69f`；VersionInfo 1.0.18；`NotSigned` | 通过 |
| 当前桌面运行 | 真实 WebView2 可启动并完成文件 I/O | 6/6 检查、11/11 路由；TXT/JSON 真实保存、离开、复开均通过 | 通过 |
| 公开发布 | 必须保持未发生 | 无 v1.0.18 Tag/Release，安装生命周期未执行 | 边界正确 |

实际差异与修正共四项：缺失候选 Tag 的检查原使用会抛错的 `git rev-list`，改为安全的 `git tag --list`；历史 M5 检查器错误假设当前社区候选仍为 v1.0.17，且后继状态只枚举到 v1.0.18 质量门待跑，现改为读取冻结的历史策略/manifest 并接受合法的 v1.0.18 候选已打包状态；P1 `nextStage` 遗留旧版本，已同步为 `V1.0.18-UNSIGNED-PATCH-RELEASE`；R5G runner 原只允许历史证据目录，已增加唯一 M6-4 目录白名单并保留全部旧证据。

两张真实桌面截图已人工核对：界面显示 `v1.0.18 DEV`，TXT 为 `R5G_TEXT_SAVED`，JSON 为有效的 `R5G_JSON_SAVED`，保存提示和复开状态符合预期。

## 3. 安装包事实

| 目标 | 本地构建文件 | 公开映射名 | 大小 | SHA-256 | Authenticode |
| --- | --- | --- | ---: | --- | --- |
| MSI | `Long编辑_1.0.18_x64_zh-CN.msi` | `LongEdit_1.0.18_x64_zh-CN.msi` | 74,186,752 | `f1f5c147c9ff8b04c5f8b8a486fdb6cdd32abedd771e19d092c54c2f5185be01` | `NotSigned` |
| NSIS | `Long编辑_1.0.18_x64-setup.exe` | `LongEdit_1.0.18_x64-setup.exe` | 65,934,312 | `96db31068a1b00732ab289474ab000ee465d4565a9150d8cb8a055a8ac96869f` | `NotSigned` |

安装包文件保留在本地受忽略的 Tauri `target/release/bundle` 目录，仓库只提交可复核的大小、摘要、版本、签名状态和运行证据，不提交约 140 MiB 二进制。

## 4. 开发目标审计与接续点

- M6-3 要求的原子版本迁移：完成。
- v1.0.18 完整发布门：完成。
- 真实双安装包：完成并逐文件验证。
- 当前候选真实桌面运行与 TXT/JSON I/O：完成。
- 安装、升级、卸载、安装态路由/文件 I/O：尚未执行。
- GitHub Tag、Release、远端回下载和 v1.0.17 → v1.0.18 自动更新：尚未执行。

唯一接续点为 **M6-5 v1.0.18 托管安装生命周期**。必须在一次性 Windows 环境从精确候选提交重建 MSI/NSIS，并从公开 v1.0.17 验证安装、升级、卸载、资料/配置保留、安装态路由与 I/O；失败时保持 `releaseCandidate=false`，不得创建公开 Release。
