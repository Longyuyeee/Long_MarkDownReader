# v1.0.22 候选版本迁移审计

日期：2026-09-12；前置提交 `2c49fe50`。当前为 community-unsigned 候选，releaseCandidate=false，尚未公开发布。

原需求对齐：只包含已冻结的搜索故障恢复、紧凑侧栏结果可读性。未扩展格式、图谱或写入能力。

## 版本迁移

新迁移脚本先在内存中校验全部替换项，默认 dry run，仅显式 --apply 才写入。44 个文件统一运行版本为 1.0.22（package/lock、Cargo/lock、Tauri、37 项活动合同及两份政策）。多文件写入不是断电事务，仓库提交作为一致性交付单位；失败不得提交半迁移状态。

历史 M5/M6/M7 检查器只扩展当前候选运行身份和阶段允许范围，不修改历史 Tag、摘要或验收事实。当前审计从独立历史政策确认 M8 收口，不再向新版候选社区政策索取旧更新路径。

v1.0.21 公开 Tag、下载链接和历史证据保持不变。新版社区门禁全部重置 false，candidate/release 为 null；1.0.21 → 1.0.22 官方更新为 pending。

## 验证与接续

迁移 dry run 已确认 44 文件。新身份的完整 `npm run ci:patch-release` 已通过：类型检查与 6,279 模块生产构建、全部链式契约、Rust locked check、生产依赖 0 漏洞。新增迁移测试 6/6（默认不写入、44 文件同步、迟发现合同偏移/前版未收口/错误基线/未知参数失败不写入），接入后当前开发总审计再次通过。

审计确认 lock 文件只变更项目版本，不升级依赖；`docs/evidence` 无改动。不生成虚构安装摘要，不将历史安装证据继承为本版通过。候选政策门禁仍为 false，因为完整发布状态还要求本版运行时与安装态证据；本地质量门结果单独记录于此，不伪造制品事实。前置提交远端 CI 尚未取得成功结论，本轮提交后也需核对新 HEAD。

接续：通过新身份完整门禁后固定候选；建立/执行本版托管 MSI/NSIS 生命周期及安装态搜索、暗色和键盘验收，随后才发布 Tag/Release。社区包 NotSigned，可能提示未知发布者；发布后提供 SHA-256 校验与手动下载安装指引，官方自动更新观察独立执行。
