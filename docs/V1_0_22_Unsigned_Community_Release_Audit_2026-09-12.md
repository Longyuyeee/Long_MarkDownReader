# v1.0.22 候选版本迁移审计

## 2026-09-22 最终发布审计（下文为迁移时历史）

发布结果：Release `393850025` 于 `2026-09-22T15:09:05Z` 正式公开并成为 latest；注解 Tag 对象 `e4cc608556811bc84734a83b905d6c4001350107` 指向固定产品 `cc58aa68`。公开下载三文件与清单逐一核对大小、SHA-256 全部一致，回执保存在 `docs/evidence/v1.0.22-release/release-receipt.json`。正式发布已完成；官方应用内更新观察独立 pending，不继承候选安装成功。

发布后状态切换审计：首次总审计暴露历史 M5/M6/M7 白名单遗漏新公开版本，以及 development-version-identity 再次硬编码本地 Debug 标志。仅在活动版本分支补入实际 1.0.22 公开/1.0.23 目标状态；身份检查调用同一原始安装报告校验器，而非把 localRuntimeSmokePassed 改为 true。旧验收数、摘要及公开 Tag 均不变。纠偏后当前总审计完整通过。

切换公开状态后再次完整执行 `npm run ci:patch-release`，退出 0；包含生产构建、全部链式合同、Rust locked check、生产依赖 0 漏洞。GitHub 最终文档提交的独立 CI 需按其新运行查看，不将本地通过冒充远端通过。正式 Release 本身已回下载验真，后续不得覆盖三个公开附件。

固定产品 `cc58aa68d1974ac2445ed4b884c7677f5a320e93`，构建/质量门/安装运行 `34679919233` 成功；搜索独立运行 `35743671363` 成功，4 项自然用户任务、8 张原图审阅完成。安装生命周期 22/22、工作面 18/18、路由 11/11、回滚 7/7，均 0 失败。

最终提示观察 `35744511959` 成功：进入图谱后 301ms 时加载和上页消息仍可见，552ms 加载消失，805ms 消息清空；原始采样及前后截图保存于 `docs/evidence/v122-final-feedback/`。这是该安装环境的短时观察，不是所有大库性能承诺；未隐藏提示或只保留最终图。已复读 GraphView 的 finally 清理及实际图像，未复现持续阻断，本项不再阻止发布。

发布清单在 `docs/evidence/v1.0.22-release/`。三个待上传文件重新读取并核对字节，MSI/NSIS 均为 NotSigned，SHA256SUMS 192 bytes、摘要 `55bf0641ed465f4b2c03ec2c6d856511f002fee4e3d22aa4984be121a88318ae`。只重命名中文安装包为既有更新器要求的 LongEdit ASCII 名，不重新编译或覆盖历史制品。旧候选失败证据保留。

发布检查纠偏：旧检查固定要求 Debug 6/6，不能伪造该字段。本版改核对真实安装态 18/18 + 11 路由原报告，绑定原始摘要、版本、NSIS 和搜索产品提交，保留保存/重开和性能导出检查。8 项正反回归拒绝错误摘要、身份、失败或缺失检查；历史版本仍采用原合同。localRuntimeSmokePassed 保留 false，不冒充完成相同格式的本地 Debug 报告；真实本地保存复验与安装运行分别记录。

已发布历史检查只补充明确的 1.0.22→1.0.23 开发状态分支，旧版本证据、Tag、摘要、原始报告不变。候选已进入 ready-to-publish；正式公开、回下载与官方更新观察必须分别取得事实后填写，不能由就绪推断完成。

最终本地 `npm run ci:patch-release` 全部通过（6,280 模块生产构建、链式检查、Rust locked check、生产依赖 0 漏洞）。新增运行报告校验 8/8、历史更新状态回归 18/18。发现并披露非阻断显示问题：LibraryMode 与 releaseCapabilities 读取构建时 development-version-policy，安装包仍显示 DEV/公开 1.0.21 文案。真实运行版本和文件身份为 1.0.22；不为修正文案替换本次已验证制品，不宣称无已知缺陷。1.0.23 首项修复版本标识，需分别验证开发构建与正式安装包。

日期：2026-09-12；前置提交 `2c49fe50`。当前为 community-unsigned 候选，releaseCandidate=false，尚未公开发布。

原需求对齐：只包含已冻结的搜索故障恢复、紧凑侧栏结果可读性。未扩展格式、图谱或写入能力。

## 版本迁移

新迁移脚本先在内存中校验全部替换项，默认 dry run，仅显式 --apply 才写入。44 个文件统一运行版本为 1.0.22（package/lock、Cargo/lock、Tauri、37 项活动合同及两份政策）。多文件写入不是断电事务，仓库提交作为一致性交付单位；失败不得提交半迁移状态。

历史 M5/M6/M7 检查器只扩展当前候选运行身份和阶段允许范围，不修改历史 Tag、摘要或验收事实。当前审计从独立历史政策确认 M8 收口，不再向新版候选社区政策索取旧更新路径。

v1.0.21 公开 Tag、下载链接和历史证据保持不变。新版社区门禁全部重置 false，candidate/release 为 null；1.0.21 → 1.0.22 官方更新为 pending。

## 验证与接续

迁移 dry run 已确认 44 文件。新身份的完整 `npm run ci:patch-release` 已通过：类型检查与 6,279 模块生产构建、全部链式契约、Rust locked check、生产依赖 0 漏洞。新增迁移测试 6/6（默认不写入、44 文件同步、迟发现合同偏移/前版未收口/错误基线/未知参数失败不写入），接入后当前开发总审计再次通过。

审计确认 lock 文件只变更项目版本，不升级依赖；`docs/evidence` 无改动。不生成虚构安装摘要，不将历史安装证据继承为本版通过。候选政策门禁仍为 false，因为完整发布状态还要求本版运行时与安装态证据；本地质量门结果单独记录于此，不伪造制品事实。前置提交远端 CI 尚未取得成功结论，本轮提交后也需核对新 HEAD。

接续：通过新身份完整门禁后固定候选；建立/执行本版托管 MSI/NSIS 生命周期及安装态搜索、暗色和键盘验收，随后才发布 Tag/Release。社区包 NotSigned，可能提示未知发布者；发布后提供 SHA-256 校验与手动下载安装指引，官方自动更新观察独立执行。
