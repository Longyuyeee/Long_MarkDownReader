# v1.0.23 — 版本身份与更新提示修复（未发布）

本版候选范围已冻结，安装与发布验收尚未完成。当前稳定下载仍为 v1.0.22。

- 侧栏、设置与能力页统一显示实际运行版本，不再把下一开发目标当作已安装版本。
- DEV 只在开发前端显示；正式前端不因开发政策字段误标开发版。
- 打开官方发布页不会冒充更新检查，也不会把“已是最新”改成“发现更新”。
- 发布详情打开失败单独说明原因，不覆盖更新状态；可以重试，已有更新弹窗保持可见。

不扩大现有格式编辑范围；社区包仍未签名，Windows 可能提示“未知发布者”。只从官方 Release 下载并核对 SHA-256。自动更新需要用户确认；本说明不代表安装包或自动更新已验收。
