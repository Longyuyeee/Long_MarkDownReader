# 搜索恢复：真实桌面验收与标签后端纠偏

前置提交：`53402d47`。日期：2026-09-12。

## 对齐与纠偏

继续执行首个搜索恢复切片，服务主 PRD 的日常查找与可信反馈。实际阅读 LibraryMode 搜索 watcher、SearchFeedback、Rust search_knowledge / search_by_tag 后发现：前端已处理 reject，但标签后端在根目录不存在时返回 Ok([])，使真实离线状态仍伪装为空结果。

本次将标签搜索根目录的 read_dir 失败转换为明确错误；不改标签匹配规则、不扩展格式、不修改源文件。子目录或单文件读取失败仍是既有 best-effort 行为，本次不承诺全树错误诊断。

## 实际桌面观察

使用 computer-use 技能直接操作本地 Tauri/WebView2 调试窗口，约 1000×700、白色主题；使用独立合成资料库与 WebView profile。调试配置由 LONGEDIT_E2E_LIBRARY 提供并禁止写回真实配置。

1. 搜索“恢复样本”命中合成 Markdown。
2. 校验精确临时路径后，将合成库改名为 library-offline（不删除内容），追加查询触发真实 IPC 失败。
3. 窗口显示“搜索未完成”与“重新搜索”，没有误报“没有找到匹配内容”。
4. 恢复原目录，点击重试，保留关键词并重新命中。
5. 标签后端改动重新构建后，使用 #验收 重做目录离线与恢复，错误提示和重试命中均成立。

测试前后合成文件 SHA-256 相同，见 [回执](./evidence/search-recovery-desktop/receipt.json)。[标签失败截图](./evidence/search-recovery-desktop/tag-search-failure.jpg) 与 [恢复命中截图](./evidence/search-recovery-desktop/tag-search-recovered.jpg) 已直接查看并存档；不含用户资料。测试目录已恢复，两个测试进程与开发服务器均已结束。

## 自动化验收

- Vue 搜索回归 14/14：原有 9 项加真实 reactive/watch/effect scope 的防抖、重试保留过滤、切库清理、清空；使用 Vue 自定义无 DOM renderer 挂载/卸载执行生产 onUnmounted 回调，验证取消计时器和隔离迟到响应。宿主界面清理依赖为 stub，不冒充全编辑器卸载测试。
- Rust 搜索测试 4/4（新增 2 项）：根目录缺失、根路径为文件均失败；可读空目录正常返回空结果；标签命中/无匹配区分正确。
- 当前开发总审计通过；Rust debug build 通过。上一切片完整本地 ci:patch-release 已通过，本轮不把历史全量结果写成重新跑过。

## 收口与下一步

有界搜索恢复切片已通过“真实普通/标签故障恢复 + 自动化竞争/生命周期”组合验收，不再停留在组件预览。不是 v1.0.22 整体发布就绪；版本保持 1.0.21，未改 Tag、Release 或安装包。

下一切片建议先审计紧凑侧栏搜索结果可读性：本次真实窗口中约 200px 侧栏的结果卡片被关系徽标挤占，文件标题与上下文难以辨识。先冻结“标题、命中原因、打开入口在紧凑宽度可读”的最小范围，再决定是否修复布局；不要直接扩张为搜索算法改造或全库性能项目。
