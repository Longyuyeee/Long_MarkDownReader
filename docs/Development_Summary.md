# 胧编辑 · md助手 开发阶段总结 (2026-03-04 - 大纲与同步重构版)

## 1. 当前项目状态
项目已成功实现高度稳定的右侧大纲同步系统与可靠的影子副本存档逻辑。通过“推倒重构”方案，彻底解决了 Vditor 外部大纲渲染空白及多文档并发保存冲突的疑难杂症。

### 已实现核心功能：
- **实时大纲系统 (Manual Extraction)**：
    - **原理**：放弃 Vditor 官方 `outlineRender`，改用原生 DOM 扫描 + `MutationObserver` 监听。
    - **亮点**：自动为缺失 ID 的标题注入锚点，支持 H1-H6 阶梯缩进，毫秒级响应输入变化。
- **影子副本 (Shadow Copy) 3.0**：
    - **路径校验锁**：引入 `lastLoadedPath` 原子锁，确保自动保存的内容与当前激活的文件 100% 匹配。
    - **内容归一化**：比对时自动忽略 Windows/Unix 换行符差异，杜绝冗余气泡。
- **知识库智能关联**：修复了启动时因配置加载延迟导致的“未关联”显示 Bug。

## 4. 主题同步修复与交互细节优化 (2026-03-04)
修复了多主题切换中的背景同步失效问题，并针对长文件名的展示与导航进行了深度优化。

### 已实现核心功能：
- **主题系统深度同步**：
    - **属性上移**：通过 Vue `watch` 强制将 `data-theme` 同步至 `document.body`，彻底解决背景颜色变量无法渗透至父级元素的问题。
    - **深色模式增强**：优化了暗色主题下的文字对比度 (`#f5f5f7`) 与 UI 边界感。
- **长文本排版优化**：
    - **文件树截断**：为侧边栏文件/文件夹名称添加智能省略号 (`...`)，防止长名称破坏单行布局。
    - **标签页截断**：将顶部标签页限制在 `200px` 以内，并同步实现超长标题截断。
- **标签导航自动化**：
    - **自动居中滚动**：实现 `activeTab` 自动对齐功能。当切换或打开新文档时，标签栏会自动平滑滚动，使激活标签始终处于可视区域中心。

### 2. 踩坑点总结 (The Hard Way)
| 坑点场景 | 根本原因 | 最终处理方式 |
| :--- | :--- | :--- |
| **背景色不改变** | `data-theme` 定义在子容器上，`body` 无法向上读取 CSS 变量。 | **属性上移**：通过 Watch 强制将属性挂载到 `document.body`。 |
| **运行时 ReferenceError** | 在 `App.vue` 中使用了 `watch` 但未引入。 | **规范引入**：补全 Vue 核心 API 的解构引入。 |
| **Naive UI 残留白块** | 组件库内置背景色优先级高于 CSS 变量。 | **主题覆盖**：在 `themeOverrides` 中将 `cardColor` 等指向变量。 |
| **标签被挤出视野** | 随着文件打开增多，激活的标签可能位于滚动区域外。 | **中心对齐**：使用 `scrollIntoView({ inline: 'center' })` 实现自动导流。 |

## 3. 待解决的挑战
- **大纲点击定位偏差**：在极长文档中，`scrollIntoView` 偶尔会被顶部固定栏遮挡 10px 左右。
- **搜索结果高亮**：全文搜索功能尚未与大纲/编辑器联动高亮。

## 4. 下一步计划 (Milestones)
- [ ] **多端路径标准化**：使用 `tauri-apps/api/path` 彻底替换硬编码的斜杠处理。
- [ ] **搜索高亮增强**：实现搜索匹配项在编辑器和大纲中的视觉联动。
- [ ] **Shadow Copy 历史回溯**：开发专门的历史版本对比（Diff）模式。
