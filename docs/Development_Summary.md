# 胧编辑 · md助手 开发阶段总结 (2026-03-04 - 大纲与同步重构版)

## 1. 当前项目状态
项目已成功实现高度稳定的右侧大纲同步系统与可靠的影子副本存档逻辑。通过“推倒重构”方案，彻底解决了 Vditor 外部大纲渲染空白及多文档并发保存冲突的疑难杂症。

### 已实现核心功能：
- **实时大纲系统 (Manual Extraction)**：
    - **原理**：放弃 Vditor 官方 `outlineRender`，改用原生 DOM 扫描 + `MutationObserver` 监听。
    - **亮点**：自动为缺失 ID 的标题注入锚点，支持 H1-H6 阶梯缩进，毫秒级响应输入变化。
- **影子副本 (Shadow Copy) 3.0**：
    - **路径校验锁**：引入 `lastLoadedPath` 原子锁，确保自动保存的内容与当前激活的文件 100% 匹配。
    - **内容归一化**：比对时自动忽略 Windows/Unix 换行符差异，杜绝冗余气泡。
- **知识库智能关联**：修复了启动时因配置加载延迟导致的“未关联”显示 Bug。

## 2. 踩坑点总结 (The Hard Way)
| 坑点场景 | 根本原因 | 最终处理方式 |
| :--- | :--- | :--- |
| **外部大纲空白** | Vditor 在 WYSIWYG 模式下默认不为标题生成 `id`，导致官方渲染器找不到锚点。 | **手动提取**：直接 query 标题元素并动态注入唯一 ID 标识。 |
| **SetToC 崩溃** | 禁用内部大纲组件后，传入 `vditor` 实例给渲染函数会触发内部空指针引用。 | **移除实例依赖**：调用渲染函数时不传第三参数，改用自定义跳转逻辑。 |
| **切换文件存错内容** | 标签页切换是同步的，但编辑器内容渲染是异步的，计时器在空窗期抓取了旧内容。 | **路径一致性锁**：在内容完全填入 DOM 并经 `nextTick` 确认后再释放保存许可。 |
| **Windows 下疯狂保存** | 文件系统的 `\r\n` 与编辑器的 `\n` 导致文本比对永远不相等。 | **换行符清洗**：在 Rust 端比对前统一去掉 `\r`。 |

## 3. 待解决的挑战
- **大纲点击定位偏差**：在极长文档中，`scrollIntoView` 偶尔会被顶部固定栏遮挡 10px 左右。
- **搜索结果高亮**：全文搜索功能尚未与大纲/编辑器联动高亮。

## 4. 下一步计划 (Milestones)
- [ ] **多端路径标准化**：使用 `tauri-apps/api/path` 彻底替换硬编码的斜杠处理。
- [ ] **搜索高亮增强**：实现搜索匹配项在编辑器和大纲中的视觉联动。
- [ ] **Shadow Copy 历史回溯**：开发专门的历史版本对比（Diff）模式。
