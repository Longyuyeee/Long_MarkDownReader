# A16 产品知识图谱专业画布审计

## 本阶段结论

UX-37B 已完成产品知识图谱画布升级，UX-37 总项可以关闭。

- 新增自动网络、树状、组织、放射、时间线五种布局，以及专业、多彩、专注三套画布主题。
- 支持空白区平移、鼠标中心缩放、适合窗口、Shift 框选、Ctrl 多选、成组拖动、方向键移动和统一撤销重做。
- 布局模式、主题和节点坐标保存在本地工作区状态，不写入 Markdown 源文件；SVG/PNG 仍只在用户明确导出时写入目标文件。
- 单选显示节点详情，多选保留完整画布，避免详情侧栏挤压多节点操作区。

## 验证

- `npm.cmd run build`：通过，6213 个模块。
- `npm.cmd run check:ux37b-knowledge-graph-canvas`：通过。
- `npm.cmd run check:current-development-audit`：通过。
- 真实 Tauri Debug WebView2：6 节点、5 布局、3 主题、缩放、6 节点框选、成组拖动、键盘移动及撤销重做均通过，运行时错误为 0。
- 测试前后 6 个 Markdown 源文件 SHA-256 全部一致。
- 证据：`docs/evidence/ux37b-knowledge-graph-canvas`，绑定产品提交 `17ad313fdf467c857856ffa6698b549315bb8f43`。

证据只使用临时生成的隔离资料库，不含用户资料和完整本机路径，`releaseCandidate=false`。

## 下一步

进入 UX-38 全格式验收，按格式能力矩阵逐项检查真实文件的打开、编辑、显式保存、撤销重做、主题表现、窄窗口布局与返回上下文；先从尚未形成统一真实桌面证据的格式开始，不重复已关闭阶段。
