# UX-38E 图形工作区审计

## 结论

UX-38E 已完成，覆盖 Canvas、Draw.io、Mermaid 与 OPML。四类工作区均采用内存草稿和显式保存，支持离开保护，并能在知识图谱往返后恢复当前缩放、平移、选择或结构面板上下文。

Canvas 已移除延迟自动保存和离开自动保存；Draw.io 与 Mermaid 已补齐撤销、重做、快捷键和窄容器布局。OPML 的专业画布与历史能力继续引用 UX-37A 已接受证据，本阶段补测显式保存、返回恢复与窄窗口。

## 桌面证据

真实 Tauri Debug WebView2 在隔离临时资料库中完成四类格式验证：

- 编辑后等待期间，四个源文件 SHA-256 均保持不变。
- 点击保存后，四个隔离副本 SHA-256 均发生变化。
- Canvas、Draw.io、Mermaid 在本轮完成撤销和重做；OPML 引用 UX-37A 的真实历史操作证据。
- 四类工作区从知识图谱返回后均恢复上下文，760x720 窄窗口无页面级横向溢出。
- 运行时错误 0，意外确认框 0，阻断错误界面 0。

证据位于 `docs/evidence/ux38e-graphics-workspace`，绑定产品提交 `b45574cdc8f6304bb98b4fef492479a5354c06c3`。审计仅使用仓库 fixture 的临时副本，不包含用户资料或完整本机路径。

## 能力边界

Draw.io 仍是受控结构化基础编辑器，不宣称完整 diagrams.net 等价能力。UX-38 总项仍未关闭，`releaseCandidate=false` 保持不变。

## 下一步

进入 UX-38F 外部依赖格式收口，核对 `.doc/.xls/.ppt/.wps/.et/.dps` 的外部应用发现、不可用状态、打开失败说明、返回上下文和窄窗口。不得把外部打开能力写成 LongEdit 内部等价编辑或转换能力；完成后再执行 UX-38 总矩阵最终审计。
