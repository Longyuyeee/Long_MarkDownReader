# v1.0.23 版本身份修复：候选收口审计

最新：构建/安装运行已成功，但原始设置截图正文空白；不计视觉通过，复用不变安装包补查。见 [安装原图复审](./V1_0_23_Installed_Visual_Review_2026-09-23.md)。

频道 `community-unsigned`，`releaseCandidate=false`；公开下载仍为 v1.0.22。

## 冻结范围

只修复实际运行版本、开发目标和远端检查结果的混淆，以及打开官方发布页覆盖更新状态的问题。正式前端不显示静态 DEV；浏览器打开失败单独提示，保留原更新弹窗与状态。不新增图谱特效、格式能力或退出行为改动。

产品修复提交截至 `ca8a6a0d`。实际页面成功路径见 `V1_0_23_Local_Validation_Recovery_2026-09-23.md`，五项故障/恢复的原生操作与外部 API 注入见 `V1_0_23_Fault_State_Observation_2026-09-23.md`。这些使用运行版本 1.0.22 的隔离程序，不是 1.0.23 安装成功证据。

## 原子迁移

脚本 `scripts/apply-v123-version-transition.mjs` 先校验全部目标，再改动 44 项当前身份：package/lock、Cargo/lock、Tauri、37 个现行共享合同和两份当前政策。dry-run 和六项隔离回归通过；遇到错误基线、未收口前版、晚期合同漂移或未知参数时不写任何文件。

公开 Tag/Release、历史安装证据不变。1.0.23 的质量、构建、安装、发布各门全部重置为未通过，不能继承 1.0.22 的绿色结果。

迁移已执行，六项回归、当前开发审计、社区发布合同和 UI 发布事实合同通过。完整 `npm run ci:patch-release` 最终退出码 0：6,283 模块生产构建、全部串联合同/回归、Rust locked check 和生产依赖审计通过，0 漏洞。首次完整运行在旧 M4F-2 当前候选状态枚举处失败，补齐候选身份后完整重跑通过；不抹除这次失败，不把历史日志中的“已通过”当成本轮重新进行 GUI 测试。

迁移过程中历史 M5/M6/M7 检查的“当前运行版本”枚举补充 1.0.23，历史阶段检查的候选状态枚举补充 `v1.0.23-quality-gate-pending`；实际历史摘要、Tag、检查项数量与证据门槛不变。v1.0.21/v1.0.22 更新观察不再要求新候选沿用旧版更新路径，新增回归确认 1.0.23 仍必须保持自己的 pending，不能冒称升级通过。生产输出未发现故障注入桥接或场景端点。

本次未构建 MSI/NSIS，没有安装/升级/发布。当前政策门仍保持未晋级：本地质量通过不等于候选安装通过。接手时从本次迁移提交冻结候选，创建独立 v1.0.23 编排与回执；既有 `v122-candidate-lifecycle.yml` 绑定历史提交，不可直接拿来宣称新版本通过。

## 发布前剩余步骤

安装候选已固定为 `403a5122d169dfb238cd0a424d83e356f644fb08`。新增 v1.0.23 专属工作流，内容只读权限、完整质量门在构建前执行、旧包只采用官方字节，失败也上传证据。新增安装态专项通过鼠标输入点击侧栏版本号进入设置、手动检查官方更新，并读取实际 Tauri 版本与能力页，保留三张截图；不改响应式状态、不注入网络返回、不执行更新安装。独立报告为 `installed-version-identity.json`，四项检查不冒充原 18 项工作面检查。十项测试和嵌入 PowerShell 解析已通过；仅代表编排准备完成，实际安装结果仍待执行及原图审阅。

1. 在迁移后的精确提交执行完整 `npm run ci:patch-release`，记录结果。
2. 冻结候选提交，只构建一次该提交的 MSI/NSIS，并记录字节数、SHA-256 与签名状态；若产品修复则重冻，不冒用旧包。
3. 在隔离安装环境验证首次安装、v1.0.22 覆盖升级、真实安装态版本入口与更新反馈、资料/配置保留、卸载；不能覆盖用户当前安装来省略隔离。
4. 通过后冻结附件清单，更新 README、创建 Tag/Release，回下载核对三项附件；发布后单独验证官方应用内更新。

社区安装包未签名，可能显示“未知发布者”。仅从官方 Release 手动下载安装，核对 SHA-256；自动更新仍须用户确认。本文不是已发布声明。

## 下一版方向

发布前安装态校验已扩展到 1.0.23：沿用真实安装工作面 18 项和路由 11 项，另要求同一提交/安装包摘要的版本专项四项及其实际观察内容。17 项校验器回归通过（其中新分支是内存合成数据，仅测试拒绝条件，不是安装证据）。旧版本摘要与检查保持有效；新版不能回退到历史 debug 烟测。此变更只影响后续证据核验，不改变正在构建的固定产品提交。

附件下载后的独立核验命令：`node scripts/verify-v123-installer-receipt.mjs <解压后的运行附件目录>`。核对候选提交、旧公开 Tag、三个安装包大小与 SHA-256、唯一 MSI/NSIS、NotSigned 回执和隐私边界；拒绝路径穿越、重复附件及重建旧包。十项正常/负向测试通过。该命令只证明包字节与回执一致，不等于安装、截图或版本入口通过；签名回执仍需对下载的真实文件另行复核。

候选执行已启动：[35754325807](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/35754325807)，编排提交 `00a0470a5e94caa995e2e5bb4db240820847e5f7`，产品固定提交不变。接手先查此运行，不重复构建；此条仅是运行登记，不是通过结论。

优先验证并统一 Alt+F4 与前端关闭按钮对退出偏好的处理，已有真实现象及 Rust CloseRequested 路径依据；仍需覆盖提示、直接退出、后台运行和未保存内容保护。其次辨认近似搜索结果的路径/命中位置；图谱恢复只有实际复现后才立项，不再堆叠特效。不要把这些加入 1.0.23。
