# M5-5 v1.0.17 原子版本迁移与候选打包审计

审计日期：2026-08-31

阶段结论：**通过；v1.0.17 版本身份、完整质量门、真实 MSI/NSIS 和当前 Tauri WebView2 烟测均已完成，下一步只进入 M5-6 托管安装生命周期。**

## 1. 目标与发布边界

44 个当前运行时身份文件从 1.0.16 原子迁移到 1.0.17，公开版本、Tag 和不可变提交仍保持 v1.0.16 / `757d543…`。v1.0.17 的质量、构建、安装和发布事实没有继承旧版本；本阶段 `releaseCandidate=false`，未创建 v1.0.17 Tag 或 GitHub Release。

候选源码精确绑定已推送提交 `2b6235d420ceffd291dab72c4af17caffe464333`。

## 2. 真实测试：预期、实际、差异与修正

| 项目 | 预期 | 实际 | 结论 |
| --- | --- | --- | --- |
| 完整发布门 | v1.0.17 全部通过 | 6,275 模块、43 格式/91 扩展名、Rust locked check 通过，生产依赖漏洞 0 | 通过 |
| MSI | 产品版本 1.0.17，未签名 | 74,186,752 bytes；SHA-256 `961184…d6e`；MSI 数据库 ProductVersion 1.0.17；`NotSigned` | 通过 |
| NSIS | 产品版本 1.0.17，未签名 | 65,922,301 bytes；SHA-256 `099238…886`；VersionInfo 1.0.17；`NotSigned` | 通过 |
| 当前桌面运行 | 真实 WebView2 可启动并完成文件 I/O | 6/6 检查、11/11 路由；TXT/JSON 真实保存、离开、复开均通过 | 通过 |
| 公开发布 | 必须保持未发生 | 无 v1.0.17 Tag/Release，安装生命周期未执行 | 边界正确 |

实际差异与修正共四项：历史 v1.0.16 审计错误绑定当前候选策略，改为读取冻结 manifest/receipt；P1 `nextStage` 遗留 v1.0.16，已同步为 v1.0.17；NSIS 缓存缺件，由 Tauri 官方下载并校验后重建；R5G 原本只允许覆盖历史证据目录，现增加唯一 M5-5 目录白名单，从而保留旧证据并生成当前候选证据。

两张真实桌面截图已人工核对：界面显示 `v1.0.17 DEV`，TXT 为 `R5G_TEXT_SAVED`，JSON 为有效的 `R5G_JSON_SAVED`，保存提示和复开状态符合预期。

## 3. 安装包事实

| 目标 | 本地构建文件 | 公开映射名 | 大小 | SHA-256 | Authenticode |
| --- | --- | --- | ---: | --- | --- |
| MSI | `Long编辑_1.0.17_x64_zh-CN.msi` | `LongEdit_1.0.17_x64_zh-CN.msi` | 74,186,752 | `96118462661e7b0eb2370aed49352b9db980fa42b7f8c27444382e1c788b4d6e` | `NotSigned` |
| NSIS | `Long编辑_1.0.17_x64-setup.exe` | `LongEdit_1.0.17_x64-setup.exe` | 65,922,301 | `09923846c2ef19b31eb44bfa2bacfdadc6e03de2e50c05a2c03b4e432acc5886` | `NotSigned` |

安装包文件保留在本地受忽略的 Tauri `target/release/bundle` 目录，仓库只提交可复核的大小、摘要、版本、签名状态和运行证据，不提交约 140 MiB 二进制。

## 4. 开发目标审计与接续点

- M5-4 要求的原子版本迁移：完成。
- v1.0.17 完整发布门：完成。
- 真实双安装包：完成并逐文件验证。
- 当前候选真实桌面运行与 TXT/JSON I/O：完成。
- 安装、升级、卸载、安装态路由/文件 I/O：尚未执行。
- GitHub Tag、Release、远端回下载和 v1.0.16 → v1.0.17 自动更新：尚未执行。

唯一接续点为 **M5-6 v1.0.17 托管安装生命周期**。必须在一次性 Windows 环境从精确候选提交构建或导入同摘要安装包，验证安装、升级、卸载、资料/配置保留、安装态路由与 I/O；失败时保持 `releaseCandidate=false`，不得创建公开 Release。
