# 当前开发状态与 M3 入口审计（2026-08-27）

## 1. 审计结论

项目当前没有发生方向性偏移。`main` 是唯一有效开发基线，M0 基线、M1 高频格式深化、M2 工作台 2.0 和 10,000 文件索引基线均已完成并有真实证据。当前尚未进入版本冻结，下一阶段固定为 **M3 知识图谱 2.0 选择审计**，不是打包或单纯视觉美化。

运行时与公开稳定版保持 `1.0.15`，开发目标为 `1.0.16`，`releaseCandidate=false`。在 M4 发布冻结前，不修改 package、Cargo、Tauri、README 下载版本或历史制品事实。

## 2. 仓库与分支事实

- 审计时本地 `main` 与 `origin/main` 均为 `67eddb5a5637164932e951984b34fbc5972ecd39`，工作区干净，`git pull --ff-only` 返回已是最新。
- 所有其他远端分支均落后于 `main`，没有需要合并的独有提交。例：`origin/codex/s8-2b-table-lifecycle` 相对 `main` 为 `648 / 0`，即主线领先 648 个提交、该分支没有独有提交。
- 因此后续直接在 `main` 延续，不创建或回收历史功能分支。

## 3. 已完成阶段

| 阶段 | 状态 | 当前事实 |
| --- | --- | --- |
| M0 真实基线 | 已完成 | 固定格式、工作台和图谱基线均通过；图谱 100/1000/5000 节点构建分别为 105/773/4853 ms，节点与边数量符合预期。 |
| 10,000 文件索引基线 | 已完成 | 首次/增量索引、查询、取消、恢复和工作台可操作性已有真实资料库证据。 |
| M1 高频格式深化 | 已完成并收口 | XLSX、DOCX、PPTX、ODS、大 JSON、视频帧工具和字幕均完成有界能力与真实桌面复验；不把有界保存误报为完整 Office 等价编辑。 |
| M2 工作台 2.0 | 已完成并收口 | “继续工作、今天要做、需要处理”职责已经收敛；真实文件写回、撤销、定位、空/错状态和窄窗口通过。 |
| M3 知识图谱 2.0 | 尚未实现，进入选择审计 | 路线图已冻结 M3A 语义、M3B 视觉、M3C 性能三个子阶段，当前只应记录修正前真实差异并选择一个最小 M3A 实现。 |
| M4 跨格式与版本候选 | 未开始 | R5E-R5N 等历史发布证据须在真实候选产生后重新采集；当前不构成 M3 开发阻断。 |

## 4. 与最初需求的对齐

### 4.1 已对齐

- **多格式编辑器**：格式注册、直接编辑、只读预览、显式转换和外部打开边界已经形成统一事实源；所有可编辑格式继续遵守“只在点击保存后写回”。
- **Excel 深化**：公式、样式、数据验证、条件格式、表格、图表和高级数据对象已形成较深的有界能力及生产者证据，方向与“完整 Excel 等价编辑器”愿景一致。
- **主题与界面**：多主题、场景预设、代码可读性、管理页和窄窗口适配均已进入持续门禁，不再是只存在于截图中的效果。
- **工作台**：已从格式统计和重复治理卡片转为可继续工作、处理待办和解决问题的行动入口，符合此前删除无用能力、强化工作流的要求。
- **真实性原则**：关键阶段均记录预期、修正前实际、修正后实际、源文件摘要和真实 Tauri 运行结果，没有用静态合同替代全部验收。

### 4.2 必须继续保持的边界

- 当前 XLSX/DOCX/PPTX/ODF 是经过证据约束的专业编辑与可靠副本能力，仍不等于 Microsoft Office 全对象、宏、外部数据和任意高级对象的完全等价实现。
- 旧二进制 Office/WPS、系统解码媒体和外部生产者能力继续保持显式依赖或安全降级，不得扩大宣传。
- 没有真实签名，后续 M4 只能按无签名社区版事实完成发布与更新生命周期。

## 5. M3 修正前代码审计

现有 `GraphView` 并非空白原型，已经具备以下能力：

- 关系网络与思维导图两种视图，五种布局和三种画布主题；
- 搜索、类型/关系筛选、孤立对象治理、关系证据和 Markdown 语义关系编辑；
- 节点邻居列表、拖拽、多选、框选、右键菜单、滚轮缩放、适应视图和撤销/重做布局；
- 布局位置保存与恢复、页面失焦暂停动画、卸载时清理动画帧与监听器；
- 自定义力导向布局已使用空间网格降低局部碰撞计算，并按缩放级别减少标签和边细节。

与 M3 退出条件相比，当前缺口是：

1. 对象类型和关系类型虽已存在，但缺少稳定、统一且用户可见的语义图例与编码合同。
2. 现有邻居遍历主要服务思维导图层级，没有面向用户的一至三层聚焦、双节点比较、邻居固定、选择历史和独立验证的最短路径流程。
3. 尚无成熟社区发现算法、稳定社区 ID、社区摘要和社区筛选。
4. 尚无社区远景、语义缩放、缩略导航图、聚类展开/折叠和路径方向流动等 M3B 完整镜头系统。
5. M0 的 100/1000/5000 结果证明数据构建可完成，但还没有证明当前图谱 UI 在三档规模下的稳定布局、平移、缩放、选择、返回、长任务和 20 次进入退出生命周期。

因此，当前最薄弱且最值得投入的部分确实是知识图谱；但应先补语义和可验证交互，再增加炫酷视觉，避免把视觉变化误当成功能完成。

## 6. 下一步固定执行顺序

### M3-0：选择与性能基线审计

1. 使用 M0 固定 100/1000/5000 节点图谱，在真实 Tauri 中记录首次可见、布局稳定、平移、缩放、节点选择、返回资料库和运行时错误。
2. 在 100 和 1000 节点图中验证邻居、边方向与证据定位；在 5000 节点图中验证不会卡死、空白或失去返回能力。
3. 连续进入退出图谱 20 次，记录监听器、动画帧、主线程长任务和内存趋势。
4. 明确写出预期、修正前实际和差异；此步骤不先改视觉。

### M3A：第一个最小语义闭环

优先选择“**稳定对象/关系语义注册表 + 图例**”作为第一个实现。该能力是最短路径、社区发现、语义缩放和主题视觉的共同基础，且可用固定输入独立验证。

随后按顺序推进：一至三层邻居聚焦与返回、双节点最短路径及独立算法对照、关系证据回跳、社区发现与稳定性、双节点比较、邻居固定与选择历史。完成 M3A 退出审计后才进入 M3B 视觉镜头。每个子步骤使用同一真实图谱记录预期与实际差异，完成审计、文档、提交和推送后再进入下一项。

### M3B、M3C 与 M4

- M3A 通过后进入 M3B：社区轮廓、语义缩放、路径表达、缩略图、聚类镜头、主题与减少动效。
- M3B 通过后进入 M3C：Worker/成熟布局评估、可见区域绘制、脏帧刷新、5,000 节点和 20 次生命周期收口。
- M3 全部退出条件通过后才进入 M4，统一跨格式定位、清理临时资产、重采发布证据并判断是否提升版本。

## 7. 接手检查命令

```powershell
git pull --ff-only
npm run check:development-version-identity
npm run check:post-v115-m0-baseline
npm run check:post-v115-m1-closure
npm run check:post-v115-m2-closure
npm run check:current-development-audit
```

若上述命令通过且工作区干净，直接开始 M3-0；不要重复 M1/M2，也不要提前提升版本。

## 8. 2026-08-27 M3-0 修正前基线完成

M3-0 已使用真实 Tauri 和 100/1000/5000 个 Markdown 节点完成。首次可见分别为 231/638/19378 ms，布局稳定分别为 2931/11654/115802 ms；1000 节点出现 84 个长任务，5000 节点最长任务 9334 ms。三档的中心选择、缩放、返回资料库和源文件逐字节不变均通过，1000 节点 20 次进入退出保持可导航，运行时错误为 0。

视觉复核确认 1000/5000 节点标签严重重叠，且当前没有统一对象/关系语义图例。下一阶段已选择为 **M3A-1 稳定对象/关系语义注册表 + 图例**；不得先做社区动画、背景光效或版本冻结。完整证据见 `Post_v1.0.15_M3_0_Knowledge_Graph_Real_Baseline_Audit_2026-08-27.md`。

## 9. 2026-08-27 M3A-1 进度回写

M3A-1 已完成。当前代码由统一注册表解释 11 类对象和 12 类关系，全局图谱、局部图谱、SVG/PNG 导出与用户图例一致；真实跨格式桌面验证覆盖全部对象类型、6 类实际关系、宽窄窗口、返回路径、零运行时错误和源文件不变。

当前阶段已前移到 **M3A-2 邻居聚焦与返回**。后续仍按原顺序推进最短路径、社区发现、视觉镜头和大图生命周期；M3A-1 没有改变公开/运行时 `1.0.15`、开发目标 `1.0.16` 或 `releaseCandidate=false`。

## 10. 2026-08-27 M3A-2 进度回写

M3A-2 已完成 1/2/3 跳邻居聚焦、持续范围说明、当前子图图例与显式返回全图。真实跨格式图谱在 1/2/3 跳下分别为 3/3、6/7、8/9，返回后恢复 17/17，零运行时错误、源文件不变。当前阶段前移到 **M3A-3 独立验证的最短路径**。

## 11. 2026-08-27 M3A-3 进度回写

M3A-3 已完成双节点最短路径、独立 BFS 距离对照、稳定并列路径、无路径状态和返回全图。真实跨格式图谱完成 17/17 → 4/3 → 17/17 往返，宽窄屏、零运行时错误和源文件不变通过。当前阶段前移到 **M3A-4 关系证据回跳**。

## 12. 2026-08-27 M3A-4 进度回写

M3A-4 已完成路径每条边的事实方向、关系类型、全部 mention、来源语法、上下文、行号和来源回跳。真实 3 边路径展示 3 条 mention，其中同一 `depends-on` 边的 2 条 mention 均保留；结构边不伪造文本证据。`Brief.md` 第 3 行定位、宽窄屏、零运行时错误和源文件不变通过。当前阶段前移到 **M3A-5 社区发现与稳定性**。

## 13. 2026-08-28 M3A-5 进度回写与顺序纠偏

M3A-5 已使用 Graphology Louvain 完成确定性无向加权投影、成员派生稳定社区 ID、主题摘要、社区筛选和返回全部。固定算法夹具连续 20 次及乱序输入结果一致；真实 17 节点/17 边跨格式图谱得到 5 个社区（4/4/3/3/3），模块度 0.670，重新进入图谱后 ID 一致，筛选 4 节点/3 边并恢复 17 节点通过。宽窄屏、零运行时错误、返回资料库和源文件不变均通过。

原始 M3A 仍包含双节点比较、邻居固定和选择历史；此前将社区发现视为 M3A 最后一步属于接续文档偏移，现已纠正。当前阶段前移到 **M3A-6 双节点比较**，之后完成邻居固定与选择历史，再进入 M3B。详见 `Post_v1.0.15_M3A5_Community_Discovery_and_Stability_Audit_2026-08-28.md`。

## 14. 2026-08-28 M3A-6 进度回写

M3A-6 已完成独立可验证的双节点属性、共同/独有邻居和直接关系证据对照。算法夹具验证端点排除、邻居去重、稳定排序、标签差异、原始方向、平行关系和全部 mention；真实 Canvas 节点对得到共同邻居 1、左右独有各 1、有向 `supports` 1 条，Markdown 节点对保留 2 条直接关系和 3 条 mention，并准确回到 `Brief.md` 第 3 行。宽窄屏、零运行时错误、返回资料库和源文件不变均通过。

当前阶段前移到 **M3A-7 邻居固定与选择历史**。完成后执行 M3A 退出审计，再进入 M3B。详见 `Post_v1.0.15_M3A6_Dual_Node_Comparison_Audit_2026-08-28.md`。

## 15. 2026-08-28 M3A-7 进度回写与固定语义纠偏

M3A-7 已完成。原设计中的“邻接固定”明确指把局部关系图固定到编辑器右栏并跟随活动标签，不是节点坐标锁定或收藏；实际代码中此前未挂载的 `LocalGraph` 已进入该工作流。全局图谱同时增加仅限当前会话、最多 20 条、连续去重、支持回退/前进和分支截断的选择历史，所有状态均不写回资料库。

真实桌面审计完成 `1 → 17 → 0 → 17 → 1 → 17` 选择与历史导航；固定的 `NorthStar.md` 局部图谱为 6 节点/7 边，并跟随活动标签切换到 `Brief.md`。审计还发现并修复了 Windows `\\?\` 路径与普通路径直接比较导致局部图谱内容为空的实际缺陷。宽窄屏、取消固定、零运行时错误、返回资料库和源文件不变均通过。

当前阶段前移到 **M3A-8 语义探索退出审计**。只有完成 M3A 全能力组合回归与退出条件复核后才进入 M3B。详见 `Post_v1.0.15_M3A7_Neighbor_Pinning_and_Selection_History_Audit_2026-08-28.md`。

## 16. 2026-08-28 M3A 语义探索退出审计

M3A-8 已完成并使 M3A 收口。组合代码审计发现社区范围会与后续邻居、路径或历史状态静默相交，现已统一互斥切换。真实同会话依次完成邻居 1→3 跳、3 边最短路径与证据、4 节点社区、双节点比较、选择历史恢复、编辑器右栏局部图谱和返回资料库，活动范围严格为 `neighbor → path → community → comparison → history → global`。

宽窄屏无页面溢出，运行时错误 0，资料库 SHA-256 前后一致；M3A-1 至 M3A-7 的独立算法与桌面证据也由退出门禁重新读取并全部通过。`FR-GRAPH-004` 仍保持部分完成，不能把路径证据的全量展开误报为所有入口均已全量展开。

当前阶段前移到 **M3B-0 专业视觉基线选择审计**：先记录社区表达、语义缩放、路径表达、缩略导航、聚类镜头、主题和减少动效的修正前真实差异，再选择最小 M3B 实现。详见 `Post_v1.0.15_M3A_Semantic_Exploration_Exit_Audit_2026-08-28.md`。

## 17. 2026-08-28 M3B-0 专业视觉基线选择审计

M3B-0 已完成。代码确认当前只用 `0.3/0.4/0.55` 三个固定阈值控制边、全部标题和字形；不按密度、重要性或社区切换信息层级。真实 17 节点桌面在 28% 远景只剩彩点，社区只裁剪为 4 节点/3 边而无轮廓，三边路径虽有完整证据但面板覆盖路径主体；关系标签、路径方向流动、缩略图、适应选择、聚类镜头、图谱全屏和图谱减少动效消费均不存在。720×680 无页面级横向溢出，运行时错误 0、源文件不变并返回资料库。

既有 1,000/5,000 节点截图进一步证明固定阈值无法解决大图标签密度，但性能指标仍属于 M3C。当前阶段前移到 **M3B-1 语义缩放与社区远景**：先冻结密度感知的远/中/近层级和社区摘要进入/返回合同，不先做装饰光效或全套镜头。详见 `Post_v1.0.15_M3B0_Professional_Visual_Baseline_Selection_Audit_2026-08-28.md`。

## 18. 2026-08-28 M3B-1 语义缩放与社区远景

M3B-1 已完成。有效缩放同时考虑当前可见节点数，17 节点实际在 100%/81%/39% 进入近/中/远景，1,000/5,000 节点的纯函数合同在默认比例直接进入社区远景。远景复用稳定 Louvain ID、占优对象语义色和真实跨社区边计数；中景只显示关键节点标题；摘要可从 Canvas 或键盘入口进入 4 节点社区并返回。

首次真实截图发现 720×680 社区摘要仍继承宽屏偏移而越界，现已增加响应式安全区域重定位和 Canvas 边界门禁；状态栏也改为单行。暗/浅/高对比三主题、三档窗口、零错误、源文件不变和返回资料库均通过。当前阶段前移到 **M3B-2 社区轮廓与语义层级**，先保持远景主题在中近景连续可辨，不提前宣称路径镜头或大图性能完成。详见 `Post_v1.0.15_M3B1_Semantic_Zoom_and_Community_Overview_Audit_2026-08-28.md`。

## 19. 2026-08-28 M3B-2 社区轮廓与语义层级

M3B-2 已完成。中近景使用稳定社区 ID 和真实成员坐标生成确定性带留白凸包，色带始终先于关系与节点绘制且不进入布局或持久化。中景显示社区名与成员数，近景改为更安静的虚线边界，远景仍使用 M3B-1 社区摘要，形成连续主题身份。

暗/浅/高对比三主题在 1280×800、1000×700、720×680 均显示 5 个包覆成员的轮廓，页面无横向溢出、运行时错误 0、源摘要不变并返回资料库。复测发现并修复安全区宽度不足时 720×680 远景摘要偶发越界。当前阶段前移到 **M3B-3 路径与关系视觉表达选择审计**，先冻结曲线、平行边、关系标签和选中路径方向表达的实际差异与最小范围，不提前扩到缩略导航、聚类镜头或 M3C。详见 `Post_v1.0.15_M3B2_Community_Contours_and_Semantic_Hierarchy_Audit_2026-08-28.md`。

## 20. 2026-08-28 M3B-3 路径与关系视觉选择审计

M3B-3 已完成选择审计。实际代码仍以直线绘制所有 network 关系，同端点多重/互反边重叠；现有箭头表达事实方向，但 Canvas 没有关系标签、路径专属高亮或方向流动。独立最短路径和全部边证据保持可靠，最新真实桌面仍为 4/17 节点、3 边、3 条证据，零运行时错误且源摘要不变；同时证据面板会覆盖大部分路径主体。设置已有 `reduced` 偏好，但图谱持续 RAF 未消费。

当前阶段前移到 **M3B-4 曲线/平行关系与静态路径标签**：先建立确定性曲线路由、平行/互反边分离、切线箭头、仅选中路径标签和面板/相机共存，再单独实现方向动画与减少动效。缩略导航、聚类镜头、状态环和 M3C 不得提前宣称完成。详见 `Post_v1.0.15_M3B3_Path_and_Relationship_Visual_Selection_and_Handoff_Audit_2026-08-28.md`。

## 21. 2026-08-28 M3B-4 曲线/平行关系与静态路径标签

M3B-4 已完成。network 屏幕与 SVG/PNG 导出现在共用按规范端点和关系事实确定的二次曲线路由；真实全图 17 条边均为曲线，2 条同端点平行/互反关系可分辨，有向箭头使用曲线切线并保持事实方向。独立最短路径仍为 4 节点/3 边和 3 条证据，Canvas 只为活动路径显示 3 个静态关系标签。

真实审计没有以首轮自动字段代替人工效果复核：先后修复零曲率中心槽、相机候选区域选择、缺失测试身份、720×680 面板实际遮挡路径和标签间距问题。暗/浅/高对比三主题在三档窗口最终均能同时看到可滚动证据面板和完整路径，运行时错误 0、源摘要不变并返回资料库。当前阶段前移到 **M3B-5 选中路径方向动效与减少动效合同**，不得提前进入缩略导航、状态外环或 M3C。详见 `Post_v1.0.15_M3B4_Curved_Parallel_Relations_and_Static_Path_Labels_Audit_2026-08-28.md`。

## 22. 2026-08-28 M3B-5 选中路径方向动效与减少动效

M3B-5 已完成。活动最短路径使用单独虚线流动表达用户选择的起点到终点，既有切线箭头继续表达关系事实方向；真实路径中的 1 段顺事实和 2 段逆事实边均正确分离。普通关系、3 个静态标签和全部路径证据不依赖动画。

暗/浅/高对比主题分别以 calm/reduced 启动真实 Tauri，在 1280×800 和 720×680 对 Canvas 前后像素、相位和帧计数做对照：calm 全部推进，reduced 全部静止；失焦暂停、恢复、零错误、源摘要不变和返回资料库通过。首轮证据的方向构成不足、reduced 字段语义相反和零时间增量归零已修正后全量重跑。当前阶段前移到 **M3B-6 导航与镜头系统选择审计**。详见 `Post_v1.0.15_M3B5_Selected_Path_Direction_Motion_and_Reduced_Motion_Audit_2026-08-28.md`。

## 23. 2026-08-28 M3B-6 导航与镜头系统选择审计

M3B-6 已完成且未修改产品运行代码。实际代码只有适配全部可见节点和立即改写视口中心，社区能力是可进入/返回的过滤子图，不是聚类折叠；适应选择、缩略导航、受控聚焦、图谱全屏均不存在。真实 Tauri 三档窗口验证社区 17 → 4 节点/3 边 → 17 往返、零运行时错误、源摘要不变和返回资料库。

预期与实际对照发现两项必须带入下一阶段：搜索聚焦后的 Canvas 在 260ms 后仍因过滤/力布局继续变化；720px 工具栏虽可滚动，但末端按钮距最大滚动约差 12px 而未完整可达。当前阶段前移到 **M3B-7 适应选择与减少动效安全聚焦**，同时修复窄屏命令可达性；缩略导航、聚类镜头、全屏和 M3C 继续延后。详见 `Post_v1.0.15_M3B6_Navigation_and_Camera_System_Selection_Audit_2026-08-28.md`。

## 24. 2026-08-28 M3B-7 适应选择与减少动效安全聚焦

M3B-7 已完成。当前可见选中节点可通过独立命令适应到面板安全区域，节点入口共享可中止且跟随活动坐标的有界相机任务；应用或系统 reduced 直接应用相同几何目标。查询变化不再误触发布局重启，结构过滤仍会重新布局。720px 增加末端安全留白，新旧两个适配命令均完整可达。

真实 Tauri calm/reduced 分别验证约 344ms/43 帧与约 38ms/0 帧；连续目标替换取消旧任务 1 次，两会话分别框选 3/17、4/17 节点并完成适应选择，最终几何与独立 oracle 一致、完成后像素稳定。运行时错误 0、源摘要不变并返回资料库。当前阶段前移到 **M3B-8 剩余导航系统选择审计**；缩略导航、聚类镜头、全屏三者先审计后只选择一个最小增量。

## 25. 2026-08-28 M3B-8 剩余导航系统选择审计

M3B-8 已完成且未改产品运行代码。100/1000/5000 节点真实证据显示大图首先失去全局方位；现有相机姿态和有界/reduced 安全过渡使只读缩略图成为最小高信息增量。社区当前仍是过滤子图往返，不是代理节点折叠；全屏只增加空间，不能恢复方位。

暗色/reduced Tauri 在 1280/1000/720 下均无页面溢出，社区完成 `17 → 4/3 → 17`，运行时错误 0、源摘要不变并返回资料库。首次测试还确认相机诊断初始为空、首次相机动作后才同步，下一阶段已把无前置操作的实时初始化列为硬合同。当前阶段前移到 **M3B-9 有界语义缩略图与视口导航**；聚类折叠、图谱全屏、状态环和 M3C 继续延后。

## 26. 2026-08-28 M3B-9 有界语义缩略图与视口导航

M3B-9 已完成。缩略图从当前可见节点派生语义色点和全图边界，不绘制标签，5000 节点纯函数输入最多采样 600 点；当前视口框实时跟随相机。点击复用有界/reduced 安全相机，拖动为直接操纵，方向键按世界视口比例移动，初始进入无需先执行相机按钮。

暗色 calm、暗色/浅色/高对比 reduced 四个真实 Tauri 会话覆盖三档窗口：calm 点击 `343ms/40帧`，reduced 为 `8～25ms/0帧`；12 次布局对详情、图例、状态栏碰撞均为 0，远景社区入口碰撞为 0。测试先后修正失效 title 入口、投影偏移缺失、Canvas 边框坐标差和离场残影取证。运行时错误 0、源摘要不变并返回资料库。当前阶段前移到 **M3B-10 剩余专业视觉系统选择审计**。

## 27. 2026-08-28 M3B-10 剩余专业视觉系统选择审计

M3B-10 已完成且未改图谱产品运行代码。真实代码确认节点 `modifiedAt` 和当前可见图度数同步可用；治理状态仍由侧栏另行异步扫描，没有逐节点外环合同。聚类仍是社区过滤而非代理节点折叠；全屏只有平台 API。

暗色/reduced Tauri 在 1280/1000/720 均无溢出，真实七档文件时间均可区分，知识脉搏为 17 对象、17 关系、100% 覆盖、运行时错误 0、源摘要不变并返回资料库。首轮证据删除临时绝对节点 ID 后重跑。当前阶段前移到 **M3B-11 克制的近期修改与关系强度节点外环**；治理外环、聚类折叠、全屏和 M3C 继续延后。
