# UX-38C2 工作簿与返回上下文审计

## 结论

UX-38C2 已完成，UX-38 数据表格格式族收口。XLSX 与 ODS 的冻结层、紧凑布局、窄窗口和图谱返回已通过真实 Tauri Debug WebView2 验收；CSV、TSV 的图谱往返也在本阶段补齐。

本阶段关闭 UX-12、UX-13、UX-32，但不扩大格式能力：XLSX 仍按既有受限写回合同执行，ODS 仍是结构化只读预览。

## 产品变更

- XLSX 工具栏按实际工作区容器宽度响应，窄工作区折为两行并保持操作可达。
- XLSX 冻结表头、行号、普通格、Table 格、选区和填充预览均使用不透明底层。
- XLSX 滚动时持续记忆滚动位置和活动 Sheet。
- ODS 记忆活动 Sheet 与双向滚动位置，冻结表头/行号改用不透明主题表面，窄工作区工具栏自动折行。
- CSV/TSV 修正加载态恢复时机，并在滚动时持续记录状态。

## 真实桌面证据

证据目录：`docs/evidence/ux38c2-workbook-context`

- 真实 XLSX：冻结窗格位置稳定且底色不透明。
- XLSX 图谱往返：`Details` Sheet 与滚动位置恢复。
- CSV/TSV 图谱往返：活动文件及横向、纵向滚动位置恢复。
- LibreOffice ODS：`Notes` Sheet 与滚动位置恢复，900x720 窄窗口无挤压。
- 运行时错误 0，阻断错误界面未出现，四个源文件 SHA-256 均未改变。

三张截图已人工复核并标记为 accepted；证据绑定产品提交 `89d1dffc4d23871e16f1c6bee9a99c6507a4393d`。

## 下一步

进入 UX-38D 文档媒体格式族，依次验收 PDF、DOCX/ODT、PPTX/ODP 的真实打开、加载/异常状态、模式切换、显式保存边界、主题、窄窗口、键盘与返回上下文。完整 Word/PowerPoint 等价能力不在本阶段内提前宣称。
