# A4 格式能力连续开发交接

更新日期：2026-07-24

分支：`codex/a4-format-closure`
基线：`main` / `3270248`

## 本轮已完成

- 纠正 JSON/JSONC 能力说明，使其与现有受保护结构编辑一致。
- YAML 完成权威解析、诊断、多文档结构提纲、源码工作面、草稿、可靠保存、统一标签、创建和文本索引。
- XML 完成安全流式解析、DTD 阻断、命名空间感知结构树、源码工作面、草稿、可靠保存、统一标签、创建和文本索引。
- 共享格式契约由 11 种格式/14 个扩展名提升到 13 种格式/17 个扩展名。
- YAML 与 XML 均达到日常管理意义上的读、编、建、搜闭环；不虚报可视化语义编辑或任意结构往返。

## 最终验证

- `npm run ci:check` 全部通过。
- Rust 功能测试 259/259 通过，性能门禁 1/1 通过。
- 主题、格式、工作簿和 XLSX 发布契约全部通过。
- 100 MiB PDF 范围读取：约 255.9 KiB 单次请求，本机 56 ms。
- 生产依赖审计：0 个漏洞。
- 已知非阻断项：Vite 仍有既有的少量 500 KiB 以上依赖分包警告。

## 下一开发入口

TOML 已于 2026-07-26 完成读、编、建、搜闭环。下一步分别处理 INI/CONF/Properties；不要用同一个宽松解析器伪装多种配置语法。

建议拆分：

1. A4-C1：INI/CONF 与 Properties 分别建立格式契约和 fixture。
2. A4-S1：`.env` 先完成遮罩、索引排除和授权边界，再开放工作面。
3. A5：真实 Tauri 进程的打开、保存、冲突、重启、大文件和安装包桌面审计。

## 明确边界

- XML 通用自动格式化未开放：混合内容空白可能有语义。
- YAML/XML 外部文件策略仍为导入；外部原位编辑授权需单独验收。
- WPS `.wps/.et/.dps` 仍需公开规范、许可和可靠转换审计；不得把 OOXML/XLSX 能力等同于原生 WPS 私有格式支持。
- 用户本地 `.claude/settings.local.json` 未进入任何提交。
