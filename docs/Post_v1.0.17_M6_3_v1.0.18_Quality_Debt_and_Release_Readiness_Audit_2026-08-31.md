# M6-3 v1.0.18 质量债与发布就绪审计

日期：2026-08-31

质量门基线：`main` / `18b1fab71cc685316da125806157cf0fb0fb752e`

阶段结论：**通过；允许进入 M6-4 原子版本迁移与候选打包，但本阶段仍未提升版本或生成安装包。**

## 1. 真实测试结果

完整执行 `cargo test --locked --manifest-path src-tauri/Cargo.toml`：**548 通过、0 失败、5 忽略**。5 个忽略项仍是明确依赖 LibreOffice、PowerPoint 或审计产物的既有测试，没有新增忽略或用聚焦测试替代全仓结果。

完整执行 `npm run ci:patch-release`：首次在 `check:v1-community-release` 失败；修正真实发布回执字段后，从生产构建开始完整复跑并通过。最终结果包括：

- Vite 生产构建 **6,275 modules transformed**；
- 文件能力合同 **43 格式 / 91 扩展名**；
- 当前开发全审计通过，M6-1 的 6/6 全屏循环与源文件不变证据仍有效；
- `cargo check --locked` 通过；
- `npm audit --omit=dev`：**found 0 vulnerabilities**。

## 2. 预期、实际、差异与修正

| 项目 | 预期 | 首次实际 | 根因与修正 | 完整复跑 |
| --- | --- | --- | --- | --- |
| 托管生命周期运行号 | v1.0.17 发布策略能与产物清单、导入证据绑定同一运行 | 策略漏存运行号，另两处均为 `33361759629` | 在发布策略补记精确运行号，继续要求三方相等 | 通过 |
| 非企业候选边界 | M5-6 导入证据的 `releaseCandidate=false` 被校验 | 检查器读取不存在的 `enterpriseReleaseCandidate` | 改为校验真实字段；运行号、提交、版本、22/18/11/7 计数和隐私约束均保留 | 通过 |

这两项不是产品功能失败，也不是通过删除断言获得绿灯；它们是历史发布回执与检查器字段不一致。修正后相同完整门从头执行并成功结束。

## 3. 版本与发布边界

当前 package、Cargo、Tauri 和运行时仍为 `1.0.17`，公开版本仍为 `v1.0.17`，`releaseCandidate=false`。M6-3 没有生成 MSI/NSIS、Tag 或 Release。

M6-4 才拥有原子迁移到 `1.0.18` 和构建候选安装包的权限。它必须复用既有 44 类版本身份清单与候选流程，记录 MSI/NSIS 的实际大小、SHA-256、`NotSigned` 状态，并完成当前 WebView2 真实烟测；随后仍需托管安装生命周期、最终产物审计、公开回下载复核和官方应用内更新观察，不能由本次质量门替代。

## 4. 阶段审计

- 开发目标：完整 Rust 和补丁发布门清零，已完成。
- 需求对齐：没有扩大产品范围；M6-1 图谱全屏能力与只读边界保持。
- 真实测试：记录首次失败、真实根因、修正和同命令完整复跑结果。
- 文档：机器政策、证据、路线图、开发对齐与 README 同步。
- 唯一接续点：**M6-4 v1.0.18 原子版本迁移与候选打包**。
