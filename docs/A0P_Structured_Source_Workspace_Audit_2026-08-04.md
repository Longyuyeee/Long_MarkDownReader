# A0P 结构化源码工作区审计

日期：2026-08-04  
需求：UX-31  
结论：代码侧完成，状态保持“待安装包复测”。

## 本阶段完成

- YAML、XML/SVG、TOML 继续共用统一 CodeMirror 九主题、光标、选区、行号和工具层合同。
- 辅助侧栏改为面向任务的“文档结构与问题”“元素导航与问题”“配置分区与问题”，明确点击条目会定位源码。
- 实时结构分析按源码规模使用 280/520/900 ms 自适应停顿窗口；上一轮分析未结束时不再并发提交新任务，结束后自动追赶最新内容。
- 三种格式继续保留 Rust 语法分析、结构导航、诊断定位、响应式侧栏和显式保存。编辑只更新内存草稿，只有用户点击保存或按 Ctrl+S 才写入源文件。

## 验证

- `npm.cmd run build`
- `npm.cmd run check:code-editor-theme-contract`
- `npm.cmd run check:structured-source-workspace`
- `git diff --check`

## 保留边界

本阶段没有把静态合同当作桌面视觉证据。安装包中仍需用小型与大型 YAML/XML/TOML 样例复测输入响应、侧栏开合、诊断定位、九主题光标可见性和保存重开，因此 UX-31 保持“待复测”。

下一阶段进入 UX-32：收紧 XLSX 工作区布局，把高级对象改成可理解的“透视表与数据连接”入口，并逐主题修复冻结区域透明问题。
