# M4F-6 v1.0.15 → v1.0.16 官方应用内更新观察

日期：2026-08-31

状态：**真实托管执行通过，M4F-6 已收口**

## 开发目标与需求对齐

本阶段只验证已经公开的两个官方 Release 之间的应用内更新，不构建新安装包、不改变产品功能，也不复用 M4F-3 的直接安装升级冒充更新器结果。真实验收必须覆盖：

1. 已安装 v1.0.15 从 GitHub latest API 发现 v1.0.16。
2. 用户确认前不得下载或启动安装器。
3. 确认后下载官方 NSIS，并在安装前核对 65,788,796 bytes 与 SHA-256 `e1a688…`。
4. 在同一安装目录静默覆盖到 v1.0.16，并由更新链自动重启。
5. 首次启动后设置页显示当前已是最新 v1.0.16。
6. 合成资料库、配置以及卸载后的用户数据保持。

所有执行只允许在 GitHub 托管的一次性 Windows 上进行，不触碰当前用户主机的已安装软件和资料。

## 预期与实际差异

入口审计发现旧工作流把 Release API 的 `target_commitish` 当作 Tag 对应提交。v1.0.16 的实际 API 字段为 `main`，但 annotated Tag `v1.0.16^{}` 正确指向产品提交 `757d54309ddb35f445344d909fa4c7ba2567bc58`。新工作流因此显式 fetch 并解引用 Tag，`target_commitish=main` 只作为实际观察记录，不再错误要求二者相等。

首轮真实运行 [`33350679455`](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/33350679455) 在更新器启动前失败。预期 Git 收到 `refs/tags/v1.0.16:refs/tags/v1.0.16`；实际 PowerShell 把 `$env:CURRENT_TAG:` 的冒号继续作为环境变量语法解析，Git 收到 `refs/tags//tags/v1.0.16`。现使用 `${env:CURRENT_TAG}` 明确变量边界；本轮没有执行安装器、没有形成更新通过证据。

修复后的第二轮真实运行 [`33350758818`](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/33350758818) 在 GitHub 托管一次性 Windows 上通过。预期与实际如下：

| 验收目标 | 预期 | 实际 |
| --- | --- | --- |
| 官方发现 | v1.0.15 发现 latest v1.0.16 | 通过，Release URL、NSIS 名称、65,788,796 bytes 与 SHA-256 全部匹配 |
| 用户边界 | 确认前不下载、不启动安装器 | 通过，`installerStartedBeforeConfirmation=false`，界面显示“下载并安装”后才继续 |
| 下载与覆盖 | 校验官方 NSIS 后在同目录静默覆盖 | 通过，下载摘要 `e1a688…`，安装根保持 `C:\LongEditManagedUpdater` |
| 二进制身份 | 安装态主程序匹配已验证安装包参考值 | 通过，108,686,336 bytes / `adfcc6…` / `NotSigned` |
| 重启与首启 | 更新链自动重启到 v1.0.16 | 通过，安装态 `tauri-app.exe` 自动启动并完成首启检查 |
| 最新版状态 | 更新后不再报告可用更新 | 通过，设置页显示“当前已是最新版本 v1.0.16” |
| 数据保留 | 覆盖与卸载均保留资料库和配置 | 通过，合成资料库和配置在覆盖、卸载后均存在 |

生命周期结果为 **12/12 通过、0 失败**。`可更新`、`下载中 33%`、`当前已是最新版` 三张截图均已人工复核，文字、按钮、进度和版本状态清晰，无明显溢出或错误覆盖。Actions 同时提示 `actions/upload-artifact@v4` 将从 Node 20 强制迁移到 Node 24；这不影响本次运行与 artifact，但登记为后续工作流维护提醒。

原始 artifact `9743531758` 的 9 个文件已导入 `docs/evidence/v1.0.16-managed-updater/`，逐文件大小与 SHA-256 固化在 `import-manifest.json`；证据不含真实用户资料。

## 开发目标审计与唯一接续点

M4F-6 没有构建新安装包、修改产品功能或触碰用户主机；它独立证明了公开 v1.0.15 → v1.0.16 的真实应用内更新链。六项原始需求与 12 项机器检查全部对齐，失败历史、修正和成功证据均保留，因此 v1.0.16 社区发布、远端附件和发布后更新链现已完整收口。

唯一接续点设为 **M5-0 v1.0.17 范围选择审计**：先根据真实代码、残余风险、用户价值与可执行测试选择一个最小开发切片，形成明确预期/现状差异后再实现。当前只登记开发目标 `1.0.17`，运行时与公开版本继续保持 `1.0.16`；不得未经选择审计直接提升二进制版本、混入多个功能或开始新 Release。
