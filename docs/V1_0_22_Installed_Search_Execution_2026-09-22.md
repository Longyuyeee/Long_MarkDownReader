# v1.0.22 安装态搜索专项执行

**当前唯一接续运行：** [35743007169](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/35743007169)，编排 `dad0b88c`，产品仍为 `cc58aa68`。已单次触发、登记时未完成；先检查该运行及 `v122-installed-search-35743007169`，不要照较早记录重复启动。发布保持关闭，等待正文/路径/源文件及生命周期完整结果和截图审阅。

第四次运行 `35742556003`：原报告明确关键词和标签的离线→键盘 Enter 重试→查询保留均 passed，事件记录包含可信 keypress/click。失败停在正文确认。直接查看 `search-failure.jpg` 已实际打开正确确认版并显示“我要先核对预算”；源码说明 Markdown 使用 `#vditor-lib`，而 `.library-embedded-editor` 仅适用于其他格式。修正专项读取实际 Markdown 工作面，仍须可见正文、准确路由和源文件不变同时成立；不是删除正文检查。新增定位契约测试后 5 项通过。原报告仍为 failed，不改写；截图及输入轨迹完整保存在 `docs/evidence/v122-search-attempt-35742556003/`。

第三次运行 `35742092552`：刷新/索引控件定位已解决，关键词基线命中与离线失败页面均实际截图确认，恢复后 Enter 重试超时。三张原图及报告存于 `docs/evidence/v122-search-attempt-35742092552/`。不能据此宣称整个搜索恢复通过。检查输入协议后补全 Enter 的字符/default-action 阶段（`keyDown` + `text: '\r'`，随后 `keyUp`），Tab 和组合键不改；同时只读记录目标按钮的可信键盘/click 事件，要求原生默认动作确实触发，再判断恢复结果。此时键盘输入不完整仍为待重跑验证的解释；不直接调用 click、搜索函数或改组件状态，产品仍不变。

第二次结果与根因纠偏：`35741444167` 仍在刷新按钮定位处超时，说明先前“过渡太快”的推断不足。直接审阅第二次原图并阅读 `appTooltipPolicy.ts` 后确认：实际应用移除原生 `title`，转成 `data-app-tooltip` 与 `aria-label`，脚本使用 `button[title=...]` 永远无法匹配已处理按钮。刷新、索引菜单和结果打开现统一按实际可访问名称定位；未改产品、等待预算、搜索预期或样本。新增回归执行实际 tooltip 采纳函数，验证属性转换并约束专项定位，共 4 项通过。第二次失败原图与报告保存在 `docs/evidence/v122-search-attempt-35741444167/`；不能记为搜索功能失败或通过。

纠偏重跑：已由 `1f8316e4` 单次触发 [35741444167](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/35741444167)，产品与安装包身份保持不变。登记时尚未完成；下一接续先检查此运行及 `v122-installed-search-35741444167`，不要重复触发。语法检查与 3 项专项合同测试通过，不作为安装搜索验收。

首次结果：运行 `35741165496` 失败，专项 checks 为空，停在搜索前的刷新按钮点击。已实际查看 [失败原图](./evidence/v122-search-attempt-35741165496/search-failure.jpg)，按钮可见；脚本当时只进行一次即时可点击检查，可能遇到导航/侧栏过渡。不能据此宣称搜索产品失败或通过。原始报告同时保存，不覆盖失败。

执行器纠偏：点击前有界等待控件可见、未禁用且命中测试不被遮挡（最多 8 秒），之后重新读取坐标再派发鼠标事件；没有直接调用组件、绕过遮挡或改查询/样本/验收断言。重跑用于验证此判断，仍须保留实际失败。发布说明同步补充纯文本保存切页修复，继续标为候选未发布。

运行登记：已从编排 `fff8549a` 单次触发 [35741165496](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/35741165496)。固定产品仍为 `cc58aa68`；首次登记时尚未完成，不能填写搜索 passed。后续先查询此运行，失败读原图和步骤定位原因，不重复触发或放宽断言。输出附件为 `v122-installed-search-35741165496`。本地 3 项工作流/隔离边界测试及当前总审计通过。

目的：补齐冻结范围内的用户任务，不添加产品功能，不再次编译安装包。

复用运行 `34679919233` 的精确修复候选 `cc58aa68`；工作流先核对原运行身份、完整三包实际摘要与官方前版回执，再在一次性 Windows runner 安装。无发布权限，不动本机安装或真实资料。暗色为正式配置字段，不用 debug 环境变量假装安装态主题。

专项通过 WebView 的鼠标/键盘输入事件进行搜索、Tab 焦点移动、Enter 重试及打开；DOM 仅用于读取位置、焦点和结果，不直接调用搜索函数、改 Vue 状态或设置输入值。资料为两份自然中文会议纪要，必须辨认确认版与内部草稿。

步骤：进入 1000×700 工作面，菜单重新准备索引；普通关键词和标签分别验证正常命中→测试根目录临时离线→明确报错→恢复根目录→Tab/Enter 重试，查询必须保持；最后 Tab/Enter 打开准确文件并核对路由及正文，源文件摘要不得变化。根目录移动仅允许固定 disposable 测试目录，finally 恢复；失败原图和失败报告保留。既有安装工作面检查数量不变，专项独立出报告。

实现：`scripts/lib/installed-search-user-scenario.mjs`，由现有安装运行器按环境开关调用；新 `.github/workflows/v122-installed-search.yml` 只下载已有产物，不重建候选。默认旧运行器主题仍为 white，新专项显式选择 dark。

本地只完成脚本语法、工作流 YAML/内嵌 PowerShell 检查及总审计；不据此填写安装搜索 passed。远端运行号受理后登记。执行结果还需直接查看原始截图，短时等待只用于状态完成，不删除错误或更换样本。当前 releaseCandidate 仍为 false；本轮不发布、不提升运行版本。

下一版本仍按 [用户任务计划](./Development_User_Task_Plan_2026-09-22.md)取舍。当前遇到的真实故障优先修复；若专项通过，不为凑新版本重做已经可用的键盘或重试能力。
