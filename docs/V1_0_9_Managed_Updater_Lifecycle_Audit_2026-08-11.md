# v1.0.9 受控更新生命周期审计

状态：**官方 v1.0.8 到 v1.0.9 托管更新 12/12 通过，自动重启已验证**

## 验收结果

- GitHub 托管 Windows 运行 [`31495885209`](https://github.com/Longyuyeee/Long_MarkDownReader/actions/runs/31495885209) 成功，执行提交为 `975ddae34d9faa744b1a204f7c054881d26f19cd`。
- 从官方 `v1.0.8` NSIS 安装开始，应用内检查发现官方 `v1.0.9`；用户确认前没有下载安装器。
- 下载的 `LongEdit_1.0.9_x64-setup.exe` 大小为 `53,795,116` 字节，SHA-256 为 `2b188fee7f30667e0df80c5389c2fc0f4a464e685c4d507645d3e0c98fe7dd19`，与 GitHub Release 一致。
- 同目录静默覆盖后，测试脚本没有手动启动新版。更新助手自动拉起 PID `1604`，进程路径为 `C:\LongEditManagedUpdater\tauri-app.exe`，版本为 `1.0.9`，并通过持续存活检查。
- 安装态可执行文件大小为 `87,442,944` 字节，SHA-256 为 `eddf08afe0527e4f14449d42f00a8ad625c8f8e6c5ef08f4b88d32a14b231582`。它由已校验的官方 NSIS 产生，不要求与独立构建 EXE 的哈希相同。
- 更新后应用报告当前已是最新版；覆盖安装与卸载后，合成资料库和配置标记均保留。

## 12 项门禁

官方旧版安装、Release 发现、用户确认边界、静默覆盖、安装器哈希、安装版本与二进制、覆盖后资料保留、自动重启、更新后首启、最新版状态、卸载和卸载后资料保留全部通过。

## 隔离与隐私

- 验收仅在 GitHub 托管的一次性 Windows 环境运行，不读取本机 LongEdit 或真实用户资料。
- 九份脱敏证据已导入 [`docs/evidence/v1.0.9-managed-updater`](./evidence/v1.0.9-managed-updater)，三张截图已经视觉复核，原始字节受 SHA-256 清单保护。
- 安装包仍为 `NotSigned`。本结论证明无签名社区版更新链可用，不构成企业签名或商业发布资格。

## 结论

v1.0.9 已修复 v1.0.8 更新后不自动重新打开的问题。发布、远端附件复核、安装生命周期和官方 `1.0.8 -> 1.0.9` 自动更新链均已收口，项目进入维护模式。
