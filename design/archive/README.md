# Design archive

This directory stores superseded visual assets that are no longer referenced by the application or current README. `legacy-background.png` is retained for provenance only; current brand assets live in [`../brand`](../brand/README.md).
